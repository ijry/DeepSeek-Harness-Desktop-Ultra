{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "启动/错误页所需的最小权限。窗口跳转由 Rust 侧发起，不需要 IPC 权限。",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open", "updater:default"]
}
