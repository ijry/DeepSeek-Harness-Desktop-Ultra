{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "启动/错误页与设置窗口所需的最小权限。主窗口跳转由 Rust 侧发起，不需要 IPC 权限；跳转后它是远程源，按设计拿不到任何权限。",
  "windows": [
    "main",
    "settings"
  ],
  "permissions": ["core:default", "shell:allow-open", "updater:default"]
}
