/**
 * dsh-plugin-otools-term client bundle - GENERATED by scripts/wrap-client.mjs.
 * Do not edit; the source of truth is src/client/*.js (npm run build).
 */
window.__ModuleLoader__.load({
  id: 'dsh-plugin-otools-term',
  factory: (require) => {
    var module = { exports: {} };
    var exports = module.exports;
    (function () {
      'use strict';
// ===== src/shared/lang.js (inlined) =====
/**
 * The two languages this plugin speaks, and how each half learns which one it is.
 *
 * Same contract as the sibling plugins: the shell hands the dsh process its
 * language in `DSH_DESKTOP_LANG`, so the Node half only reads the environment
 * (`hostLang`). The browser half runs inside dsh's own page and has no shell to
 * ask: it takes the language from the `language` field of `GET /state` and falls
 * back to `navigator.language`, so a plugin installed straight from npm — no
 * shell anywhere — still speaks the user's language.
 *
 * Locale spellings are matched the way the shell matches them (src-tauri
 * i18n.rs): case-insensitively, and only the part before the first `-`, `_` or
 * `.` counts, so `zh`, `zh-CN` and `zh_CN.UTF-8` are one language.
 *
 * @module dsh-plugin-otools-term/shared/lang
 */

/** Every language with a full string table. Chinese first — it is the default. */
const LANGS = ['zh', 'en']

/**
 * What the Node half reads, in order: the shell's own variable outranks the
 * ambient locale, and the first RECOGNISED value wins (a `LANG` of `C.UTF-8`
 * names no language, so it must not shadow a `LANG` further down).
 */
const HOST_ENV_KEYS = ['DSH_DESKTOP_LANG', 'LC_ALL', 'LC_MESSAGES', 'LANG']

/**
 * A locale string → one of [`LANGS`], or null when it names neither of them
 * (`fr`, `C.UTF-8`) or is nullish.
 */
function normalizeLang(value) {
  if (value === null || value === undefined) return null
  const base = String(value).trim().toLowerCase().split(/[-_.]/)[0]
  return LANGS.includes(base) ? base : null
}

/**
 * The language of the Node half, Chinese when nothing names one. Read at call
 * time rather than cached: module-level state resolved at import time is state a
 * test cannot set.
 */
function hostLang() {
  for (const key of HOST_ENV_KEYS) {
    const found = normalizeLang(process.env[key])
    if (found !== null) return found
  }
  return 'zh'
}

// ===== src/client/vocab.js =====
/**
 * Vocabulary shared by the whole browser bundle: ids, selectors, route paths and
 * the handful of protocol constants this side needs.
 *
 * The protocol constants are a hand-kept COPY of `src/shared/protocol.js` — the
 * bundle has no module resolution, so it cannot import it, and only the values the
 * browser actually needs are duplicated. `test/client-mirror.test.mjs` keeps the two
 * from drifting.
 */

const PLUGIN_ID = 'dsh-plugin-otools-term'
const ROUTE_PREFIX = '/dsh-plugin-otools-term'
const SSE_PATH = '/dsh-plugin-otools-term/events'
const SOCKET_PATH = '/dsh-plugin-otools-term/socket'
const VENDOR_PREFIX = '/dsh-plugin-otools-term/vendor'
const STYLE_ID = 'dsh-plugin-otools-term-style'
const PANEL_NAME = 'dsh-plugin-otools-term'
const ACTIVATE_EVENT = 'dsh-panel-activate'
const OPEN_ATTR = 'data-dsh-ot-open'
/** The entry attribute the sibling panels already list as a peer. */
const ENTRY_SELECTOR = '[data-dsh-ssh-entry]'
const LOG = '[' + PLUGIN_ID + ']'

/** Seats in the DSH shell, across the layout generations it has shipped. */
const CONVERSATION_SELECTOR =
  '[data-pane="conversation"], [class*="centerCol"], .dshDesktopConversationSurface'
const SIDEBAR_SELECTOR =
  '[data-pane="sidebar"], [class*="sidebarCol"], .dshDesktopUpstreamSidebar, .dshDesktopSidebarSurface'
/** Sibling panel plugins: their entries stay grouped and only one panel is open. */
const SIBLING_ENTRIES =
  '[data-dsh-cgtb-entry], [data-dsh-taskboard-entry], [data-dsh-repopanel-entry], [data-dsh-otools-git-entry]'

/** localStorage keys. Only browser-local choices live here; the rest is the ledger. */
const STORAGE_PREFIX = PLUGIN_ID + ':'
const STORE_KEYS = {
  clientId: STORAGE_PREFIX + 'clientId',
  selectedServer: STORAGE_PREFIX + 'selectedServer',
}

/** The id of the built-in local-shell row (mirrors shared/protocol.js). */
const LOCAL_SERVER_ID = '__local__'

/** Protocols a stored connection can have. */
const PROTOCOLS = ['ssh', 'rdp', 'vnc']

/** Default port per protocol. */
const DEFAULT_PORTS = { ssh: 22, rdp: 3389, vnc: 5900 }

/** The two SSH auth types. */
const AUTH_TYPES = ['password', 'private_key']

/** What happens to tabs when a connection is closed. */
const CLOSE_BEHAVIORS = ['close-tabs', 'keep-tabs']

/** Sidebar geometry, verbatim from the reference. */
const SIDEBAR_MIN_WIDTH = 220
const SIDEBAR_MAX_WIDTH = 420
const SIDEBAR_DEFAULT_WIDTH = 260
const SERVER_LIST_MIN_HEIGHT = 148
const SFTP_PANEL_MIN_HEIGHT = 220

/** How long the panel waits before flushing typed bytes to the host. */
const INPUT_FLUSH_MS = 8

/** How much of a session's output the browser keeps for the AI bar's context. */
const CLIENT_SCROLLBACK_CHARS = 40_000

/** Message of an unknown throwable, never `[object Object]`. */
function messageOf(error) {
  if (error === null || error === undefined) return t('err.unknown')
  if (typeof error === 'string') return error
  if (typeof error.message === 'string' && error.message.length > 0) return error.message
  return String(error)
}

/** Stable code of an error envelope, or ''. */
function codeOf(error) {
  return error !== null && error !== undefined && typeof error.code === 'string' ? error.code : ''
}

/** Localise the host's stable error codes; unknown codes keep their prose. */
function friendlyError(error) {
  const message = messageOf(error)
  const code = codeOf(error)
  if (code.length === 0) return message
  const known = t('err.' + code)
  return known === 'err.' + code ? message : known + '：' + message
}

/** The basename of a POSIX path. */
function baseName(path) {
  const text = String(path ?? '').replace(/\\/g, '/').replace(/\/+$/, '')
  const cut = text.lastIndexOf('/')
  return cut === -1 ? text : text.slice(cut + 1)
}

/** The parent of a POSIX path ('/' at the root). */
function parentPath(path) {
  const text = String(path ?? '').replace(/\\/g, '/').replace(/\/+$/, '')
  if (text.length === 0 || text === '/') return '/'
  const cut = text.lastIndexOf('/')
  return cut <= 0 ? '/' : text.slice(0, cut)
}

/** Join a directory and one segment. */
function joinPath(dir, name) {
  const base = String(dir ?? '/').replace(/\/+$/, '')
  return base.length === 0 ? '/' + name : base + '/' + name
}

/** A byte count as the panel shows it. */
function formatBytes(bytes) {
  const value = typeof bytes === 'number' && Number.isFinite(bytes) ? bytes : 0
  if (value < 1024) return value + ' B'
  if (value < 1024 * 1024) return (value / 1024).toFixed(1) + ' KB'
  if (value < 1024 * 1024 * 1024) return (value / (1024 * 1024)).toFixed(1) + ' MB'
  return (value / (1024 * 1024 * 1024)).toFixed(2) + ' GB'
}

/** A unix timestamp as a short local date-time. */
function formatTime(ms) {
  if (typeof ms !== 'number' || !Number.isFinite(ms) || ms <= 0) return '-'
  const date = new Date(ms)
  const pad = (value) => String(value).padStart(2, '0')
  return date.getFullYear() + '-' + pad(date.getMonth() + 1) + '-' + pad(date.getDate()) +
    ' ' + pad(date.getHours()) + ':' + pad(date.getMinutes())
}

/** A random id with a readable prefix (mirrors the host's `newId`). */
function newId(prefix) {
  return prefix + '-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 10)
}

// ===== src/client/i18n.js =====
/**
 * The string table: every user-visible word in the panel, in Chinese and English.
 *
 * One row per key, `[zh, en]`, so the two languages cannot drift apart — the
 * reference kept eight separate JSON files and had already let `modifiedTime`-style
 * mismatches creep in. The Chinese side is VERBATIM from otools-term's
 * `i18n/zh-CN.json` wherever the key exists there, so the port reads identically;
 * keys this port added (the AI bar, chmod, host keys, workspace transfers) are
 * marked with a comment.
 *
 * `{name}` placeholders are single-brace, like the reference.
 */

/** The active language, decided at boot (see boot.js). */
const lang = { current: 'zh' }

/** Which column of the table each language reads. */
const LANG_INDEX = { zh: 0, en: 1 }

/** Look up one key, interpolating `{name}` placeholders. */
function t(key, params) {
  const row = TEXT[key]
  if (row === undefined) return key
  const index = LANG_INDEX[lang.current] ?? 0
  const template = row[index] ?? row[0]
  if (params === undefined || params === null) return template
  return String(template).replace(/\{(\w+)\}/g, (whole, name) => (
    Object.hasOwn(params, name) ? String(params[name]) : whole
  ))
}

/** The table. */
const TEXT = {
  // ------------------------------------------------------------------ shell
  'main.title': ['墨鱼终端', 'FishTerm'],
  'main.subtitle': ['SSH 终端、远程桌面与 SFTP 工作区', 'SSH terminal, remote desktop and SFTP workspace'],
  'main.localTerminal': ['本地终端', 'Local Terminal'],
  'main.toolbarDefaultTitle': ['远程连接', 'Remote connections'],
  'main.toolbarSubtitleDefault': ['SSH、RDP、VNC 与 SFTP 工作区', 'SSH, RDP, VNC and SFTP workspace'],
  'main.collapseSidebar': ['收起左侧栏', 'Collapse sidebar'],
  'main.expandSidebar': ['展开左侧栏', 'Expand sidebar'],
  'main.favoriteCommands': ['收藏命令', 'Saved commands'],
  'main.favoriteDirectories': ['收藏目录', 'Saved directories'],
  'main.favoriteCurrentDirectory': ['收藏当前目录', 'Save current directory'],
  'main.noFavoriteCommands': ['暂无收藏命令', 'No saved commands yet'],
  'main.noFavoriteDirectories': ['暂无收藏目录', 'No saved directories yet'],
  'main.transferTasks': ['传输任务', 'Transfers'],
  'main.theme': ['主题', 'Theme'],
  'main.emptyNoTab': ['选择左侧 SFTP 文件，或新建一个终端', 'Pick a file on the left, or open a new terminal'],
  'main.emptySelectServer': ['请选择一个服务器', 'Select a connection'],
  'main.add': ['添加', 'Add'],
  'main.save': ['保存', 'Save'],
  'main.cancel': ['取消', 'Cancel'],
  'main.close': ['关闭', 'Close'],
  'main.delete': ['删除', 'Delete'],
  'main.name': ['名称', 'Name'],
  'main.command': ['命令', 'Command'],
  'main.confirm': ['确定', 'OK'],
  'main.refresh': ['刷新', 'Refresh'],
  'main.settings': ['设置', 'Settings'],
  'main.newConnection': ['新建连接', 'New connection'],
  'main.searchServers': ['搜索连接', 'Filter connections'],
  // ------------------------------------------------------- transfer drawer
  'main.fileCount': ['{done}/{total} 个文件', '{done}/{total} files'],
  'main.taskKindUpload': ['上传', 'Upload'],
  'main.taskKindDownload': ['下载', 'Download'],
  'main.taskStatus.pending': ['等待中', 'Pending'],
  'main.taskStatus.transferring': ['传输中', 'Transferring'],
  'main.taskStatus.completed': ['已完成', 'Completed'],
  'main.taskStatus.failed': ['失败', 'Failed'],
  'main.clearFinished': ['清理已完成', 'Clear finished'],
  'main.noTransferTasks': ['暂无传输任务', 'No transfers yet'],
  'main.cancelTask': ['取消传输', 'Cancel transfer'],
  'main.addFavoriteCommandTitle': ['添加收藏命令', 'Add a saved command'],
  'main.favoriteCommandNamePlaceholder': ['可选，便于识别', 'Optional label'],
  'main.favoriteCommandPlaceholder': ['输入命令', 'Type a command'],

  // -------------------------------------------------------- server context
  'main.menu.newTerminal': ['新建终端', 'New terminal'],
  'main.menu.openSftp': ['打开 SFTP', 'Open SFTP'],
  'main.menu.closeSftp': ['关闭 SFTP', 'Close SFTP'],
  'main.menu.forwarding': ['转发与代理', 'Forwarding and proxy'],
  'main.menu.editConnection': ['编辑连接', 'Edit connection'],
  'main.menu.disconnect': ['断开连接', 'Disconnect'],
  'main.menu.reconnect': ['重新连接', 'Reconnect'],
  'main.menu.deleteConnection': ['删除连接', 'Delete connection'],
  'main.menu.openDesktop': ['打开远程桌面', 'Open remote desktop'],
  'main.menu.duplicate': ['复制连接', 'Duplicate connection'],

  // ------------------------------------------------------- server dialog
  'main.serverDialog.addTitle': ['添加连接', 'Add connection'],
  'main.serverDialog.editTitle': ['编辑连接', 'Edit connection'],
  'main.serverDialog.name': ['服务器名称', 'Name'],
  'main.serverDialog.namePlaceholder': ['例如：我的服务器', 'e.g. my server'],
  'main.serverDialog.host': ['主机地址', 'Host'],
  'main.serverDialog.hostPlaceholder': ['IP 或域名', 'IP or hostname'],
  'main.serverDialog.port': ['端口', 'Port'],
  'main.serverDialog.username': ['用户名', 'User'],
  'main.serverDialog.usernamePlaceholder': ['登录用户名', 'login name'],
  'main.serverDialog.authType': ['认证方式', 'Authentication'],
  'main.serverDialog.authPassword': ['密码认证', 'Password'],
  'main.serverDialog.authPrivateKey': ['私钥认证', 'Private key'],
  'main.serverDialog.password': ['密码', 'Password'],
  'main.serverDialog.passwordPlaceholder': ['密码', 'password'],
  'main.serverDialog.passwordKept': ['留空表示沿用已保存的密码', 'Leave blank to keep the stored password'],
  'main.serverDialog.privateKeyPath': ['私钥路径', 'Key file'],
  'main.serverDialog.privateKeyPathPlaceholder': ['服务器上 dsh 能读到的绝对路径', 'absolute path dsh can read'],
  'main.serverDialog.privateKeyBody': ['粘贴私钥', 'Paste key'],
  'main.serverDialog.privateKeyBodyPlaceholder': ['-----BEGIN OPENSSH PRIVATE KEY-----', '-----BEGIN OPENSSH PRIVATE KEY-----'],
  'main.serverDialog.privateKeyStored': ['已保存一份私钥内容', 'A key body is stored'],
  'main.serverDialog.passphrase': ['私钥口令', 'Passphrase'],
  'main.serverDialog.passphrasePlaceholder': ['私钥口令（可选）', 'passphrase (optional)'],
  'main.serverDialog.useAgent': ['使用 SSH Agent', 'Use SSH agent'],
  'main.serverDialog.keepalive': ['保活间隔（秒）', 'Keepalive (s)'],
  'main.serverDialog.group': ['分组', 'Group'],
  'main.serverDialog.note': ['备注', 'Note'],
  'main.serverDialog.requiredFields': ['请填写必填项', 'Fill in the required fields'],
  'main.serverDialog.passwordRequired': ['请输入密码', 'A password is required'],
  'main.serverDialog.privateKeyRequired': ['请填写私钥路径或粘贴私钥内容', 'Give a key path or paste a key'],
  'main.serverDialog.protocol': ['协议', 'Protocol'],
  'main.serverDialog.protocolSsh': ['SSH', 'SSH'],
  'main.serverDialog.protocolRdp': ['RDP', 'RDP'],
  'main.serverDialog.protocolVnc': ['VNC', 'VNC'],
  'main.serverDialog.importSshConfig': ['从 ~/.ssh/config 导入', 'Import ~/.ssh/config'],
  'main.serverDialog.importTitle': ['导入 SSH 配置', 'Import SSH config'],
  'main.serverDialog.importEmpty': ['没有找到可导入的主机', 'No importable hosts found'],
  'main.serverDialog.importUnsupported': ['不支持：{list}', 'unsupported: {list}'],
  'main.serverDialog.importSelected': ['导入选中的 {count} 个', 'Import {count} selected'],
  'main.serverDialog.imported': ['已导入 {count} 个连接', 'Imported {count} connections'],
  // ------------------------------------------------------------ forwarding
  'main.forwarding.title': ['转发与代理', 'Forwarding and proxy'],
  'main.forwarding.portForwardTitle': ['本地端口转发', 'Local port forwarding'],
  'main.forwarding.addRule': ['新增规则', 'Add rule'],
  'main.forwarding.saveRules': ['保存规则', 'Save rules'],
  'main.forwarding.emptyRules': ['暂无端口转发规则', 'No forwarding rules yet'],
  'main.forwarding.portRuleName': ['端口 {port}', 'Port {port}'],
  'main.forwarding.running': ['运行中', 'Running'],
  'main.forwarding.stopped': ['未启动', 'Stopped'],
  'main.forwarding.ruleName': ['规则名称', 'Rule name'],
  'main.forwarding.ruleNamePlaceholder': ['例如：MySQL 转发', 'e.g. MySQL tunnel'],
  'main.forwarding.localHost': ['本地地址', 'Listen host'],
  'main.forwarding.localPort': ['本地端口', 'Listen port'],
  'main.forwarding.remoteHost': ['远程地址', 'Remote host'],
  'main.forwarding.remotePort': ['远程端口', 'Remote port'],
  'main.forwarding.allowPublic': ['允许非本机访问', 'Allow access from other machines'],
  'main.forwarding.allowPublicHint': [
    '勾选后监听地址可以填 0.0.0.0，等于把远端服务暴露给能访问这台机器的所有人。',
    'Ticking this allows 0.0.0.0, which exposes the remote service to everyone who can reach this machine.',
  ],
  'main.forwarding.start': ['启动', 'Start'],
  'main.forwarding.stop': ['停止', 'Stop'],
  'main.forwarding.delete': ['删除', 'Delete'],
  'main.forwarding.socks5Title': ['SOCKS5 代理', 'SOCKS5 proxy'],
  'main.forwarding.saveConfig': ['保存配置', 'Save'],
  'main.forwarding.socks5Listen': ['SOCKS5 监听', 'SOCKS5 listener'],
  'main.forwarding.listenHost': ['监听地址', 'Listen host'],
  'main.forwarding.listenPort': ['监听端口', 'Listen port'],
  'main.forwarding.saved': ['转发与代理配置已保存', 'Forwarding settings saved'],
  'main.forwarding.addressRequired': ['请填写完整的端口转发地址', 'Fill in the whole address'],
  'main.forwarding.started': ['端口转发已启动：{host}:{port}', 'Forwarding on {host}:{port}'],
  'main.forwarding.stoppedMessage': ['端口转发已停止', 'Forwarding stopped'],
  'main.forwarding.socks5Started': ['SOCKS5 代理已启动：{host}:{port}', 'SOCKS5 proxy on {host}:{port}'],
  'main.forwarding.socks5Stopped': ['SOCKS5 代理已停止', 'SOCKS5 proxy stopped'],
  'main.forwarding.traffic': ['↑{out} ↓{in} · {connections} 个连接', '↑{out} ↓{in} · {connections} conns'],

  // ------------------------------------------------------------- lifecycle
  'main.deleteServerTitle': ['删除服务器', 'Delete connection'],
  'main.deleteServerConfirm': ['确认删除服务器 “{name}” 吗？', 'Delete the connection “{name}”?'],
  'main.directoryAlreadyFavorited': ['该目录已收藏', 'Already saved'],
  'main.directoryFavorited': ['目录已收藏', 'Directory saved'],
  'main.currentDirectoryNotReady': ['当前目录尚未就绪', 'No current directory yet'],
  'main.switchToTerminalFirst': ['请先切换到终端标签页', 'Switch to a terminal tab first'],
  'main.terminalNotReady': ['当前终端尚未就绪', 'The terminal is not ready'],
  'main.disconnect.unsavedFiles': ['存在未保存文件', 'there are unsaved files'],
  'main.disconnect.openTerminals': ['存在已打开终端', 'terminals are open'],
  'main.disconnect.runningTransfers': ['存在运行中的传输任务', 'transfers are running'],
  'main.disconnect.confirmMessage': [
    '{warnings}，确认继续{action}并关闭对应 TAB 吗？',
    '{warnings} — continue to {action} and close those tabs?',
  ],
  'main.disconnect.confirmTitle': ['操作确认', 'Confirm'],
  'main.disconnect.continue': ['继续', 'Continue'],
  'main.sftpClosed': ['SFTP 已关闭', 'SFTP closed'],
  'main.disconnected': ['已断开连接', 'Disconnected'],
  'main.reconnectStarted': ['已发起重新连接', 'Reconnecting'],
  'main.connecting': ['连接中...', 'Connecting…'],
  'main.connected': ['已连接', 'Connected'],
  'main.copied': ['已复制', 'Copied'],
  // ------------------------------------------------------------- host keys
  'hostkey.title': ['核对主机密钥', 'Verify host key'],
  'hostkey.firstTime': [
    '第一次连接 {host}:{port}。请核对下面的指纹，与服务器上 ssh-keygen -lf 的输出一致再接受。',
    'First connection to {host}:{port}. Compare this fingerprint with ssh-keygen -lf on the server before accepting.',
  ],
  'hostkey.mismatch': [
    '警告：{host}:{port} 的主机密钥与本机记录的不一致。可能是服务器重装了，也可能是连接被拦截。',
    'Warning: the host key for {host}:{port} does not match the pinned one. The server may have been rebuilt — or the connection intercepted.',
  ],
  'hostkey.pinned': ['已记录', 'Pinned'],
  'hostkey.incoming': ['本次收到', 'Received'],
  'hostkey.accept': ['接受并连接', 'Accept and connect'],
  'hostkey.forget': ['删除记录', 'Delete pin'],
  'hostkey.forgetHint': [
    '确认服务器确实重装过之后，删除旧记录再重连。',
    'Delete the pin only once you know the server was legitimately rebuilt.',
  ],
  'hostkey.listTitle': ['已接受的主机密钥', 'Accepted host keys'],
  'hostkey.empty': ['还没有记录任何主机密钥', 'No host keys pinned yet'],
  'hostkey.accepted': ['已接受主机密钥', 'Host key accepted'],
  'hostkey.forgotten': ['已删除主机密钥记录', 'Pin deleted'],

  // --------------------------------------------------------------- settings
  'settings.dialogTitle': ['设置', 'Settings'],
  'settings.tabSsh': ['SSH 设置', 'SSH'],
  'settings.tabTerminal': ['终端', 'Terminal'],
  'settings.tabHostKeys': ['主机密钥', 'Host keys'],
  'settings.closeBehavior': ['关闭响应', 'On disconnect'],
  'settings.closeTabs': ['关闭对应服务器的终端 TAB、打开文件和 SFTP', 'Close that server’s terminals, files and SFTP'],
  'settings.keepTabs': ['保留对应 TAB，稍后重新连接时自动重连', 'Keep the tabs and reconnect them later'],
  'settings.hint1': [
    '选项 1：断开连接时默认关闭对应服务器的终端 TAB、SFTP 和已打开文件。',
    'Option 1: disconnecting closes that server’s terminal tabs, its SFTP pane and its open files.',
  ],
  'settings.hint2': [
    '如果存在未保存文件、运行中的传输任务或活跃终端，会先弹窗确认。',
    'If there are unsaved files, running transfers or live terminals, you are asked first.',
  ],
  'settings.hint3': [
    '选项 2：断开时保留现有 TAB，后续点“重新连接”会自动重连终端和 SFTP。',
    'Option 2: keep the tabs; “Reconnect” brings the terminals and SFTP back.',
  ],
  'settings.fontSize': ['字号', 'Font size'],
  'settings.scrollback': ['回滚行数', 'Scrollback'],
  'settings.cursorBlink': ['光标闪烁', 'Blinking cursor'],
  'settings.copyOnSelect': ['选中即复制', 'Copy on select'],
  'settings.localShell': ['本地 Shell', 'Local shell'],
  'settings.localShellPlaceholder': ['留空使用系统默认', 'blank = system default'],
  'settings.confirmDangerous': ['运行危险命令前确认', 'Confirm dangerous commands'],
  'settings.aiLanguage': ['AI 回答语言', 'AI answer language'],
  'settings.saved': ['设置已保存', 'Settings saved'],
  'settings.ptyOk': ['本地终端：node-pty 可用', 'Local terminal: node-pty available'],
  'settings.ptyMissing': [
    '本地终端：没有 node-pty，处于兼容模式（{reason}）',
    'Local terminal: no node-pty, compatibility mode ({reason})',
  ],

  // ----------------------------------------------------------------- editor
  'editor.save': ['保存', 'Save'],
  'editor.refresh': ['刷新', 'Refresh'],
  'editor.empty': ['请选择一个文件以开始编辑', 'Pick a file to edit'],
  'editor.openFailed': ['打开文件失败: {message}', 'Could not open the file: {message}'],
  'editor.saved': ['文件已保存', 'File saved'],
  'editor.saveFailed': ['保存文件失败: {message}', 'Could not save the file: {message}'],
  'editor.refreshFailed': ['刷新文件失败: {message}', 'Could not refresh the file: {message}'],
  'editor.loading': ['正在读取文件内容...', 'Reading the file…'],
  'editor.binary': ['这是一个二进制文件，编辑器不会打开它，以免保存时损坏内容。', 'This is a binary file; the editor will not open it.'],
  'editor.dirty': ['未保存', 'unsaved'],
  'editor.discardConfirm': ['“{name}” 有未保存的修改，仍要关闭吗？', '“{name}” has unsaved changes. Close anyway?'],
  'editor.lineColumn': ['第 {line} 行，第 {column} 列', 'Line {line}, column {column}'],
  // ------------------------------------------------------------------- sftp
  'sftp.title': ['SFTP', 'SFTP'],
  'sftp.searchPlaceholder': ['搜索服务器上的文件和文件夹', 'Search files and folders on the server'],
  'sftp.searchResults': ['搜索结果', 'Search results'],
  'sftp.searching': ['搜索中...', 'Searching…'],
  'sftp.searchResultTooMany': ['结果过多', 'too many'],
  'sftp.searchResultCount': ['{count} 条', '{count} hits'],
  'sftp.searchingRemote': ['正在搜索远程目录...', 'Searching the remote tree…'],
  'sftp.searchTooMany': [
    '结果超过 200 条，请使用更精确的关键字搜索以提升性能',
    'More than 200 hits — narrow the keyword for a faster search',
  ],
  'sftp.searchEmpty': ['未找到匹配的文件或文件夹', 'Nothing matched'],
  'sftp.searchHint': ['回车开始搜索', 'Press Enter to search'],
  'sftp.favoritesTitle': ['收藏目录', 'Saved directories'],
  'sftp.uploadFile': ['上传文件', 'Upload files'],
  'sftp.uploadFolder': ['上传文件夹', 'Upload a folder'],
  'sftp.refreshDirectory': ['刷新目录', 'Refresh'],
  'sftp.serverFile': ['服务器文件', 'Server file'],
  'sftp.goUp': ['上一级', 'Parent directory'],
  'sftp.goHome': ['回到家目录', 'Home directory'],
  'sftp.dropHere': ['把文件拖到这里上传到 {path}', 'Drop files here to upload into {path}'],
  'sftp.entryCount': ['{count} 项', '{count} items'],
  'sftp.truncated': ['目录条目过多，只显示前 {count} 项', 'Too many entries; showing the first {count}'],
  'sftp.linkTo': ['→ {target}', '→ {target}'],
  'sftp.context.createDirectory': ['新建文件夹', 'New folder'],
  'sftp.context.createFile': ['新建文件', 'New file'],
  'sftp.context.upload': ['上传文件', 'Upload files'],
  'sftp.context.favoriteDirectory': ['收藏此目录', 'Save this directory'],
  'sftp.context.openInTerminal': ['在终端打开', 'Open in terminal'],
  'sftp.context.download': ['下载到本机', 'Download'],
  'sftp.context.downloadToWorkspace': ['下载到工作区…', 'Download into a workspace…'],
  'sftp.context.uploadFromWorkspace': ['从工作区上传…', 'Upload from a workspace…'],
  'sftp.context.copyPath': ['拷贝路径', 'Copy path'],
  'sftp.context.rename': ['重命名', 'Rename'],
  'sftp.context.chmod': ['修改权限', 'Permissions'],
  'sftp.context.delete': ['删除', 'Delete'],
  'sftp.context.refresh': ['刷新', 'Refresh'],
  'sftp.context.edit': ['编辑', 'Edit'],
  'sftp.status.connected': ['已连接', 'Connected'],
  'sftp.status.connecting': ['连接中', 'Connecting'],
  'sftp.status.error': ['异常', 'Error'],
  'sftp.status.disconnected': ['未连接', 'Offline'],
  'sftp.downloadFailed': ['下载失败: {message}', 'Download failed: {message}'],
  'sftp.uploadFailed': ['上传失败: {message}', 'Upload failed: {message}'],
  'sftp.uploadDone': ['已上传 {count} 个文件', 'Uploaded {count} file(s)'],
  'sftp.favoriteAdded': ['已加入收藏', 'Saved'],
  'sftp.pathCopied': ['路径已复制', 'Path copied'],
  'sftp.promptName': ['请输入名称', 'Enter a name'],
  'sftp.promptNameRequired': ['名称不能为空', 'A name is required'],
  'sftp.createDirectory': ['新建文件夹', 'New folder'],
  'sftp.createFile': ['新建文件', 'New file'],
  'sftp.createDirectorySuccess': ['文件夹已创建', 'Folder created'],
  'sftp.createFileSuccess': ['文件已创建', 'File created'],
  'sftp.rename': ['重命名', 'Rename'],
  'sftp.renameSuccess': ['重命名成功', 'Renamed'],
  'sftp.chmodTitle': ['修改权限：{name}', 'Permissions: {name}'],
  'sftp.chmodMode': ['八进制权限', 'Octal mode'],
  'sftp.chmodSuccess': ['权限已修改为 {mode}', 'Mode changed to {mode}'],
  'sftp.deleteConfirmTitle': ['删除确认', 'Delete'],
  'sftp.deleteDirectoryConfirm': [
    '确认删除文件夹“{name}”吗？这会删除该文件夹下所有文件。',
    'Delete the folder “{name}” and everything inside it?',
  ],
  'sftp.deleteFileConfirm': ['确认删除文件“{name}”吗？', 'Delete the file “{name}”?'],
  'sftp.deleteSuccess': ['删除成功', 'Deleted'],
  'sftp.selectWorkspace': ['选择工作区', 'Pick a workspace'],
  'sftp.workspaceEmpty': ['DSH 里还没有打开任何文件夹', 'No folders are open in DSH yet'],
  'sftp.workspaceRelative': ['工作区内的子路径（可留空）', 'Path inside the workspace (optional)'],
  'sftp.transferStarted': ['传输任务已开始', 'Transfer started'],
  // ------------------------------------------------------------ desktop card
  'main.desktop.title': ['远程桌面', 'Remote desktop'],
  'main.desktop.subtitle': [
    '使用当前保存的服务器配置，在本机启动原生远程桌面客户端并发起连接。',
    'Launches a native client on this machine using the stored connection settings.',
  ],
  'main.desktop.launch': ['打开桌面', 'Open desktop'],
  'main.desktop.launching': ['正在启动...', 'Starting…'],
  'main.desktop.protocol': ['协议', 'Protocol'],
  'main.desktop.address': ['地址', 'Address'],
  'main.desktop.auth': ['认证', 'Credentials'],
  'main.desktop.passwordAuth': ['已保存密码', 'password stored'],
  'main.desktop.noPassword': ['启动时输入', 'prompt on launch'],
  'main.desktop.launchSuccess': ['已启动 {client} 客户端：{host}:{port}', 'Launched {client} for {host}:{port}'],
  'main.desktop.launchFailed': ['启动远程桌面失败：{message}', 'Could not start the client: {message}'],
  'main.desktop.clients': ['本机可用客户端', 'Clients found here'],
  'main.desktop.noClients': ['没有检测到可用客户端', 'No client detected'],
  'main.desktop.sendPassword': ['把保存的密码交给客户端', 'Hand the stored password to the client'],
  'main.desktop.sendPasswordHint': [
    '密码会出现在客户端进程的命令行里，本机上的其它程序可以看到；不勾选就由客户端自己弹窗询问。',
    'The password would appear in the client’s command line, where any local process can read it. Leave it off and the client will ask.',
  ],

  // --------------------------------------------------------------- AI bar
  'ai.title': ['AI 助手', 'AI assistant'],
  'ai.askPlaceholder': ['用一句话说要做什么，回车生成命令', 'Describe what you want; Enter writes the command'],
  'ai.generate': ['生成命令', 'Write command'],
  'ai.explain': ['解释这屏输出', 'Explain this screen'],
  'ai.insert': ['插入终端', 'Insert'],
  'ai.run': ['直接运行', 'Run it'],
  'ai.copy': ['复制', 'Copy'],
  'ai.discard': ['丢弃', 'Discard'],
  'ai.cancel': ['停止生成', 'Stop'],
  'ai.thinking': ['正在生成...', 'Thinking…'],
  'ai.model': ['{provider} / {model}', '{provider} / {model}'],
  'ai.unavailable': ['AI 不可用：{reason}', 'AI unavailable: {reason}'],
  'ai.needTerminal': ['请先打开一个终端标签页', 'Open a terminal tab first'],
  'ai.withContext': ['附带最近的终端输出', 'Include recent output'],
  'ai.dangerTitle': ['这条命令可能有破坏性', 'This command looks destructive'],
  'ai.dangerBody': [
    '匹配到：{reasons}。请自己读一遍再决定，AI 不理解你的机器。',
    'Matched: {reasons}. Read it yourself before running — the model does not know your machine.',
  ],
  'ai.dangerConfirm': ['我看过了，运行', 'I read it, run it'],
  'ai.explainTitle': ['AI 解释', 'AI explanation'],
  'ai.failed': ['AI 失败：{message}', 'AI failed: {message}'],

  // -------------------------------------------------------------- terminal
  'term.reconnect': ['重新连接', 'Reconnect'],
  'term.closed': ['会话已结束（退出码 {code}）', 'Session ended (exit code {code})'],
  'term.closedNoCode': ['会话已结束', 'Session ended'],
  'term.error': ['会话异常：{message}', 'Session error: {message}'],
  'term.starting': ['正在启动会话...', 'Starting the session…'],
  'term.overflow': ['输出太快，已丢弃部分内容', 'Output outran the browser; some was dropped'],
  'term.compatibility': ['兼容模式', 'compatibility mode'],
  'term.searchPlaceholder': ['在终端里查找', 'Find in terminal'],
  'term.clear': ['清屏', 'Clear'],
  'term.copySelection': ['复制选中', 'Copy selection'],
  'term.paste': ['粘贴', 'Paste'],
  'term.newTab': ['新建终端', 'New terminal'],
  'term.closeTab': ['关闭标签页', 'Close tab'],
  'term.vendorMissing': [
    'xterm.js 没有安装好，终端无法显示。请在插件目录里执行 npm install。',
    'xterm.js is missing, so the terminal cannot render. Run npm install in the plugin directory.',
  ],
  'term.noSession': ['会话不在了，请重新打开', 'The session is gone; open a new one'],

  // ------------------------------------------------------------- error codes
  'err.unknown': ['未知错误', 'Unknown error'],
  'err.invalid_input': ['请求不合法', 'Invalid request'],
  'err.not_found': ['找不到', 'Not found'],
  'err.no_session': ['会话已结束', 'Session gone'],
  'err.auth_required': ['认证失败', 'Authentication failed'],
  'err.host_key': ['主机密钥需要确认', 'Host key needs review'],
  'err.connect': ['连接失败', 'Connection failed'],
  'err.sftp': ['SFTP 操作失败', 'SFTP failed'],
  'err.transfer': ['传输失败', 'Transfer failed'],
  'err.tunnel': ['转发失败', 'Tunnel failed'],
  'err.port_in_use': ['端口被占用', 'Port already in use'],
  'err.desktop': ['远程桌面失败', 'Remote desktop failed'],
  'err.pty_unavailable': ['本地终端不可用', 'Local terminal unavailable'],
  'err.too_large': ['内容过大', 'Too large'],
  'err.timeout': ['操作超时', 'Timed out'],
  'err.ai_unavailable': ['AI 不可用', 'AI unavailable'],
  'err.internal': ['内部错误', 'Internal error'],
}

// ===== src/client/dom.js =====
/**
 * DOM helpers. Everything the panel builds goes through `el`, and every piece of
 * remote text (a path, a filename, a command, a model answer) reaches the page as a
 * TEXT NODE.
 *
 * Security stance: a filename on someone else's server is attacker-controlled, and
 * so is anything a terminal prints. `innerHTML` is used for exactly one thing in
 * this bundle: this plugin's own static icon markup in icons.js. Nothing that came
 * off a network ever goes near it.
 */

/**
 * Build one element. Attributes go in the second argument (`class`, `style`,
 * `data-*`, aria, and `on<Event>` handlers); children are the rest.
 */
function el(tag, attrs, ...children) {
  const node = document.createElement(tag)
  if (attrs !== null && attrs !== undefined && typeof attrs === 'object' && !(attrs instanceof Node) &&
      !Array.isArray(attrs) && typeof attrs !== 'string') {
    for (const [key, value] of Object.entries(attrs)) {
      if (value === undefined || value === null || value === false) continue
      if (key.startsWith('on') && typeof value === 'function') {
        node.addEventListener(key.slice(2).toLowerCase(), value)
        continue
      }
      if (key === 'style' && typeof value === 'object') {
        for (const [prop, styleValue] of Object.entries(value)) {
          if (styleValue !== undefined && styleValue !== null) node.style.setProperty(prop, String(styleValue))
        }
        continue
      }
      if (value === true) {
        node.setAttribute(key, '')
        continue
      }
      node.setAttribute(key, String(value))
    }
    appendAll(node, children)
    return node
  }
  appendAll(node, [attrs, ...children])
  return node
}

/** Append children, flattening arrays and skipping nullish entries. */
function appendAll(node, children) {
  for (const child of children) {
    if (child === undefined || child === null || child === false) continue
    if (Array.isArray(child)) {
      appendAll(node, child)
      continue
    }
    node.append(child instanceof Node ? child : document.createTextNode(String(child)))
  }
}

/** Replace an element's children in one go. */
function fill(node, ...children) {
  if (node === null || node === undefined) return node
  node.replaceChildren()
  appendAll(node, children)
  return node
}

/** An SVG element (createElement would give an unknown HTML element). */
function svg(tag, attrs, ...children) {
  const node = document.createElementNS('http://www.w3.org/2000/svg', tag)
  if (attrs !== null && attrs !== undefined && typeof attrs === 'object') {
    for (const [key, value] of Object.entries(attrs)) {
      if (value === undefined || value === null || value === false) continue
      if (key.startsWith('on') && typeof value === 'function') {
        node.addEventListener(key.slice(2).toLowerCase(), value)
        continue
      }
      node.setAttribute(key, String(value))
    }
  }
  appendAll(node, children)
  return node
}

/** Toggle a data attribute that CSS keys on. */
function setData(node, key, on) {
  if (node === null || node === undefined) return
  if (on) node.dataset[key] = 'true'
  else delete node.dataset[key]
}

/** Read a persisted preference, tolerating a blocked localStorage. */
function storeGet(key, fallback) {
  try {
    const value = window.localStorage.getItem(key)
    return value === null ? fallback : value
  } catch {
    return fallback
  }
}

/** Write a persisted preference, tolerating a blocked localStorage. */
function storeSet(key, value) {
  try {
    window.localStorage.setItem(key, String(value))
  } catch { /* private mode, quota, or a policy — not worth reporting */ }
}

/** Debounce a function on a trailing edge. */
function debounce(fn, delayMs) {
  let timer
  return (...args) => {
    if (timer !== undefined) clearTimeout(timer)
    timer = setTimeout(() => {
      timer = undefined
      fn(...args)
    }, delayMs)
  }
}

/** Copy text to the clipboard, falling back to a hidden textarea. */
async function copyText(text, notice) {
  try {
    if (navigator.clipboard !== undefined && typeof navigator.clipboard.writeText === 'function') {
      await navigator.clipboard.writeText(String(text))
    } else {
      const area = el('textarea', { style: { position: 'fixed', opacity: '0', top: '0' } })
      area.value = String(text)
      document.body.append(area)
      area.select()
      document.execCommand('copy')
      area.remove()
    }
    if (notice !== undefined) toast(notice, 'success')
    return true
  } catch (error) {
    toast(messageOf(error), 'error')
    return false
  }
}

/**
 * A pointer-driven splitter. Returns the handle element; `onMove` receives the new
 * value in pixels, already clamped.
 *
 * Uses pointer capture so the drag survives the cursor leaving the handle, and sets
 * the body cursor for the duration — without that, a fast drag flickers between the
 * resize cursor and whatever is underneath.
 */
function resizeHandle(options) {
  const axis = options.axis === 'y' ? 'y' : 'x'
  const handle = el('div', {
    class: 'dsh-ot-resizer dsh-ot-resizer-' + axis,
    role: 'separator',
    'aria-orientation': axis === 'x' ? 'vertical' : 'horizontal',
  })
  let start = 0
  let base = 0
  let dragging = false

  const clamp = (value) => Math.max(options.min, Math.min(options.max(), value))

  handle.addEventListener('pointerdown', (event) => {
    if (event.button !== 0) return
    event.preventDefault()
    dragging = true
    start = axis === 'x' ? event.clientX : event.clientY
    base = options.value()
    handle.setPointerCapture(event.pointerId)
    handle.dataset.dragging = 'true'
    document.body.style.cursor = axis === 'x' ? 'col-resize' : 'row-resize'
    document.body.style.userSelect = 'none'
  })
  handle.addEventListener('pointermove', (event) => {
    if (!dragging) return
    const delta = (axis === 'x' ? event.clientX : event.clientY) - start
    options.onMove(clamp(base + (options.invert === true ? -delta : delta)))
  })
  const stop = (event) => {
    if (!dragging) return
    dragging = false
    delete handle.dataset.dragging
    document.body.style.cursor = ''
    document.body.style.userSelect = ''
    try {
      handle.releasePointerCapture(event.pointerId)
    } catch { /* already released */ }
    if (options.onCommit !== undefined) options.onCommit(options.value())
  }
  handle.addEventListener('pointerup', stop)
  handle.addEventListener('pointercancel', stop)
  handle.addEventListener('lostpointercapture', stop)
  if (options.onReset !== undefined) {
    handle.addEventListener('dblclick', () => options.onReset())
  }
  return handle
}

/** Load one same-origin script once, resolving when it has run. */
const loadedScripts = new Map()
function loadScript(url) {
  const existing = loadedScripts.get(url)
  if (existing !== undefined) return existing
  const promise = new Promise((resolvePromise, rejectPromise) => {
    const node = el('script', { src: url, async: false })
    node.addEventListener('load', () => resolvePromise(true))
    node.addEventListener('error', () => {
      loadedScripts.delete(url)
      rejectPromise(new Error('无法加载 ' + url))
    })
    document.head.append(node)
  })
  loadedScripts.set(url, promise)
  return promise
}

/** Add one same-origin stylesheet once. */
function loadStylesheet(url) {
  if (document.querySelector('link[data-dsh-ot-vendor="' + url + '"]') !== null) return
  document.head.append(el('link', { rel: 'stylesheet', href: url, 'data-dsh-ot-vendor': url }))
}

// ===== src/client/styles.js =====
/**
 * The stylesheet, injected once into document.head.
 *
 * Colours, sizes and spacing are lifted from the reference plugin so the port looks
 * the same: the light values come from MenuGit's `src/style.scss`
 * (`--layout-border-color: #eaeaea`, `--toolbar-bg-color: #f7f7f7`, the sidebar
 * hover/active fills) and the dark values from its `html.dark` block. The reference
 * got them from Element Plus and its shell; here they are declared on the panel root,
 * because this bundle ships no component library.
 *
 * Everything is namespaced `dsh-ot-` and scoped under `.dsh-ot-panel` (or the overlay
 * classes) so it cannot leak into the DSH shell around it.
 */
const STYLES = `
.dsh-ot-panel, .dsh-ot-overlay {
  --ot-border: #eaeaea; --ot-border-light: #eff1f6; --ot-toolbar: #f7f7f7; --ot-sidebar: #fafafa;
  --ot-bg: #ffffff; --ot-fill: #f0efef; --ot-fill-hover: #f5f7fa; --ot-active: #ecf3fd;
  --ot-text: #303133; --ot-text-2: #606266; --ot-text-3: #909399;
  --ot-primary: #2370c6; --ot-primary-soft: rgba(35, 112, 198, .1); --ot-primary-line: rgba(35, 112, 198, .28);
  --ot-success: #1f9d57; --ot-warning: #d68a1c; --ot-danger: #d33c4c; --ot-info: #7f8fa4;
  --ot-folder: #ffb713; --ot-file: #409eff;
  --ot-shadow: 0 8px 20px rgba(0, 0, 0, .12); --ot-shadow-lg: 0 12px 34px rgba(15, 23, 42, .2);
  --ot-mono: Menlo, Monaco, Consolas, "Courier New", monospace;
  --ot-term-bg: #1f2430;
}
@media (prefers-color-scheme: dark) {
  .dsh-ot-panel, .dsh-ot-overlay {
    --ot-border: rgba(255, 255, 255, .1); --ot-border-light: rgba(255, 255, 255, .06);
    --ot-toolbar: #232338; --ot-sidebar: #232338; --ot-bg: #1c1c2b;
    --ot-fill: rgba(255, 255, 255, .05); --ot-fill-hover: rgba(255, 255, 255, .08); --ot-active: rgba(79, 148, 232, .18);
    --ot-text: #e8ebf1; --ot-text-2: rgba(232, 235, 241, .72); --ot-text-3: rgba(232, 235, 241, .48);
    --ot-primary: #4f94e8; --ot-primary-soft: rgba(79, 148, 232, .16); --ot-primary-line: rgba(79, 148, 232, .38);
    --ot-success: #3fb06b; --ot-warning: #e0a13c; --ot-danger: #e25563;
    --ot-shadow: 0 8px 20px rgba(0, 0, 0, .5); --ot-shadow-lg: 0 12px 34px rgba(0, 0, 0, .55);
  }
}

/* ----------------------------------------------------- sidebar entry row */
.dsh-ot-entry {
  display: flex; align-items: center; gap: 8px; position: relative;
  width: calc(100% - 8px); margin: 2px 4px; padding: 6px 10px;
  border: none; border-radius: 8px; background: transparent;
  color: var(--dsw-text-secondary, #7f8fa4); font: inherit; font-size: 13px;
  cursor: pointer; text-align: left; box-sizing: border-box;
}
.dsh-ot-entry:hover { background: var(--dsw-hover, rgba(128, 128, 128, .12)); color: var(--dsw-text-primary, inherit); }
.dsh-ot-entry[data-active="true"] { background: var(--dsw-active, rgba(128, 128, 128, .18)); color: var(--dsw-text-primary, inherit); font-weight: 500; }
.dsh-ot-entry-icon { display: inline-flex; flex: none; color: #2f9e7e; }
.dsh-ot-entry-stats { margin-left: auto; font-size: 11px; line-height: 1; color: rgba(127, 143, 164, .9); font-variant-numeric: tabular-nums; white-space: nowrap; }
[data-sidebar-collapsed] [data-dsh-ssh-entry], [class*="_collapsed"] [data-dsh-ssh-entry] {
  width: 36px; height: 36px; min-width: 36px; margin: 0 0 12px; padding: 0; justify-content: center; gap: 0;
}
[data-sidebar-collapsed] [data-dsh-ssh-entry] .dsh-ot-entry-label,
[data-sidebar-collapsed] [data-dsh-ssh-entry] .dsh-ot-entry-stats,
[class*="_collapsed"] [data-dsh-ssh-entry] .dsh-ot-entry-label,
[class*="_collapsed"] [data-dsh-ssh-entry] .dsh-ot-entry-stats { display: none; }
/* ------------------------------------------- center-column takeover + shell */
html[data-dsh-ot-open] [data-pane="conversation"] > *:not([data-dsh-ot-view]),
html[data-dsh-ot-open] [class*="centerCol"] > *:not([data-dsh-ot-view]),
html[data-dsh-ot-open] .dshDesktopConversationSurface > *:not([data-dsh-ot-view]) { display: none !important; }
.dsh-ot-view { display: none; }
html[data-dsh-ot-open] .dsh-ot-view { display: flex; flex-direction: column; height: 100%; min-height: 0; overflow: hidden; }
.dsh-ot-panel {
  display: flex; flex-direction: row; height: 100%; min-height: 0; width: 100%; overflow: hidden;
  box-sizing: border-box; background: var(--ot-bg); color: var(--ot-text); font-size: 13px; line-height: 1.5;
}
.dsh-ot-panel *, .dsh-ot-overlay * { box-sizing: border-box; }

/* --------------------------------------------------------------- sidebar */
.dsh-ot-side { position: relative; flex: none; height: 100%; display: flex; flex-direction: column; background: var(--ot-sidebar); border-right: 1px solid var(--ot-border); overflow: hidden; }
.dsh-ot-side[data-hidden="true"] { display: none; }
.dsh-ot-side-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 10px; padding: 8px; background: var(--ot-toolbar); border-bottom: 1px solid var(--ot-border); }
.dsh-ot-side-copy { min-width: 0; display: flex; flex-direction: column; gap: 2px; }
.dsh-ot-side-title { margin: 0; font-size: 16px; font-weight: 600; line-height: 1.15; }
.dsh-ot-side-sub { font-size: 11px; line-height: 1.35; color: var(--ot-text-3); }
.dsh-ot-side-actions { display: flex; align-items: center; gap: 6px; flex: none; }
.dsh-ot-side-body { flex: 1; min-height: 0; display: flex; flex-direction: column; overflow: hidden; }
.dsh-ot-servers { min-height: 0; display: flex; flex-direction: column; overflow: auto; }
.dsh-ot-server {
  display: flex; align-items: center; justify-content: space-between; gap: 10px; padding: 10px 8px;
  background: var(--ot-bg); border-bottom: 1px solid var(--ot-border); cursor: pointer;
  transition: background-color .18s ease, box-shadow .18s ease;
}
.dsh-ot-server:hover { background: var(--ot-fill-hover); }
.dsh-ot-server[data-active="true"] { background: var(--ot-active); box-shadow: inset 3px 0 0 var(--ot-primary); }
.dsh-ot-server-main { flex: 1; min-width: 0; }
.dsh-ot-server-row { display: flex; align-items: center; gap: 8px; min-width: 0; }
.dsh-ot-server-name { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-weight: 600; }
.dsh-ot-server[data-active="true"] .dsh-ot-server-name { color: var(--ot-primary); }
.dsh-ot-dot { width: 8px; height: 8px; flex: none; border-radius: 999px; background: var(--ot-text-3); }
.dsh-ot-dot[data-state="connected"] { background: var(--ot-success); }
.dsh-ot-dot[data-state="connecting"] { background: var(--ot-warning); }
.dsh-ot-dot[data-state="error"] { background: var(--ot-danger); }
.dsh-ot-dot[data-state="local"] { background: var(--ot-file); }
.dsh-ot-server-meta { margin-top: 4px; font-size: 12px; color: var(--ot-text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-server-badges { display: flex; gap: 4px; flex: none; }
.dsh-ot-menu-btn { width: 26px; height: 26px; display: inline-flex; align-items: center; justify-content: center; border: none; border-radius: 8px; background: transparent; color: var(--ot-text-3); cursor: pointer; }
.dsh-ot-menu-btn:hover { background: var(--ot-fill); color: var(--ot-text); }
.dsh-ot-side-empty { padding: 18px 12px; text-align: center; color: var(--ot-text-3); font-size: 12px; }

/* ------------------------------------------------------------- resizers */
.dsh-ot-resizer { position: relative; flex: none; touch-action: none; z-index: 5; }
.dsh-ot-resizer-x { width: 8px; margin: 0 -3px; cursor: col-resize; align-self: stretch; }
.dsh-ot-resizer-y { height: 8px; margin: -3px 0; cursor: row-resize; }
.dsh-ot-resizer::before { content: ''; position: absolute; background: var(--ot-border); transition: background-color .15s ease; }
.dsh-ot-resizer-x::before { left: 50%; top: 0; bottom: 0; width: 1px; transform: translateX(-50%); }
.dsh-ot-resizer-y::before { top: 50%; left: 0; right: 0; height: 1px; transform: translateY(-50%); }
.dsh-ot-resizer:hover::before, .dsh-ot-resizer[data-dragging="true"]::before { background: var(--ot-primary); box-shadow: 0 0 0 1px var(--ot-primary-soft); }
/* ------------------------------------------------------------------ main */
.dsh-ot-main { flex: 1; min-width: 0; min-height: 0; display: flex; flex-direction: column; overflow: hidden; }
.dsh-ot-toolbar { display: flex; align-items: center; gap: 12px; padding: 8px 12px; background: var(--ot-toolbar); border-bottom: 1px solid var(--ot-border); }
.dsh-ot-toolbar-left { flex: 1; min-width: 0; display: flex; align-items: center; gap: 10px; }
.dsh-ot-toolbar-title { font-size: 16px; font-weight: 600; line-height: 1.15; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-toolbar-sub { font-size: 11px; line-height: 1.35; color: var(--ot-text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-toolbar-group { display: flex; align-items: center; gap: 8px; flex: none; }
.dsh-ot-btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 6px; height: 30px; padding: 0 10px;
  border: 1px solid var(--ot-border); border-radius: 8px; background: var(--ot-bg); color: var(--ot-text);
  font: inherit; font-size: 12px; cursor: pointer; white-space: nowrap;
  transition: background-color .15s ease, border-color .15s ease, color .15s ease;
}
.dsh-ot-btn:hover:not(:disabled) { background: var(--ot-fill-hover); border-color: var(--ot-primary-line); }
.dsh-ot-btn:disabled { opacity: .5; cursor: not-allowed; }
.dsh-ot-btn[data-icon="true"] { width: 30px; padding: 0; }
.dsh-ot-btn[data-variant="primary"] { background: var(--ot-primary); border-color: var(--ot-primary); color: #fff; }
.dsh-ot-btn[data-variant="primary"]:hover:not(:disabled) { filter: brightness(1.08); background: var(--ot-primary); }
.dsh-ot-btn[data-variant="danger"] { color: var(--ot-danger); border-color: var(--ot-danger); background: transparent; }
.dsh-ot-btn[data-variant="ghost"] { border-color: transparent; background: transparent; }
.dsh-ot-btn[data-active="true"] { background: var(--ot-primary-soft); border-color: var(--ot-primary-line); color: var(--ot-primary); }
.dsh-ot-badge { min-width: 18px; height: 18px; padding: 0 5px; display: inline-flex; align-items: center; justify-content: center; border-radius: 999px; background: var(--ot-primary); color: #fff; font-size: 11px; font-variant-numeric: tabular-nums; }
.dsh-ot-tag { display: inline-flex; align-items: center; gap: 4px; padding: 1px 7px; border-radius: 6px; font-size: 11px; background: var(--ot-fill); color: var(--ot-text-2); white-space: nowrap; }
.dsh-ot-tag[data-tone="success"] { background: rgba(31, 157, 87, .14); color: var(--ot-success); }
.dsh-ot-tag[data-tone="danger"] { background: rgba(211, 60, 76, .14); color: var(--ot-danger); }
.dsh-ot-tag[data-tone="warning"] { background: rgba(214, 138, 28, .16); color: var(--ot-warning); }
.dsh-ot-tag[data-tone="primary"] { background: var(--ot-primary-soft); color: var(--ot-primary); }
.dsh-ot-select { height: 30px; padding: 0 8px; border: 1px solid var(--ot-border); border-radius: 8px; background: var(--ot-bg); color: var(--ot-text); font: inherit; font-size: 12px; }
.dsh-ot-input, .dsh-ot-textarea {
  width: 100%; padding: 6px 8px; border: 1px solid var(--ot-border); border-radius: 8px;
  background: var(--ot-bg); color: var(--ot-text); font: inherit; font-size: 13px;
}
.dsh-ot-input:focus, .dsh-ot-textarea:focus { outline: none; border-color: var(--ot-primary); box-shadow: 0 0 0 2px var(--ot-primary-soft); }
.dsh-ot-textarea { resize: vertical; min-height: 72px; font-family: var(--ot-mono); }

/* ------------------------------------------------------------------ tabs */
.dsh-ot-tabs { display: flex; align-items: stretch; flex: none; overflow-x: auto; background: var(--ot-toolbar); border-bottom: 1px solid var(--ot-border); scrollbar-width: thin; }
.dsh-ot-tab { display: inline-flex; align-items: center; gap: 8px; padding: 6px 12px; background: var(--ot-fill); color: var(--ot-text-2); border: none; border-right: 1px solid var(--ot-border); font: inherit; font-size: 12px; cursor: pointer; white-space: nowrap; max-width: 240px; }
.dsh-ot-tab[data-active="true"] { background: var(--ot-bg); color: var(--ot-primary); }
.dsh-ot-tab-label { overflow: hidden; text-overflow: ellipsis; }
.dsh-ot-tab-dot { width: 6px; height: 6px; border-radius: 999px; flex: none; background: var(--ot-text-3); }
.dsh-ot-tab-dot[data-state="running"] { background: var(--ot-success); }
.dsh-ot-tab-dot[data-state="closed"] { background: var(--ot-text-3); }
.dsh-ot-tab-dot[data-state="error"] { background: var(--ot-danger); }
.dsh-ot-tab-close { display: inline-flex; width: 16px; height: 16px; align-items: center; justify-content: center; border: none; border-radius: 4px; background: transparent; color: var(--ot-text-3); cursor: pointer; font-size: 14px; line-height: 1; }
.dsh-ot-tab-close:hover { background: var(--ot-fill-hover); color: var(--ot-danger); }
.dsh-ot-tab-add { padding: 6px 12px; font-size: 16px; line-height: 1; }

/* ------------------------------------------------------------------ body */
.dsh-ot-body { flex: 1; min-height: 0; position: relative; display: flex; flex-direction: column; overflow: hidden; background: var(--ot-bg); }
.dsh-ot-panes { flex: 1; min-height: 0; position: relative; }
.dsh-ot-pane { position: absolute; inset: 0; display: none; flex-direction: column; min-height: 0; overflow: hidden; }
.dsh-ot-pane[data-active="true"] { display: flex; }
.dsh-ot-empty { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 6px; color: var(--ot-text-3); text-align: center; padding: 20px; }
/* -------------------------------------------------------------- terminal */
.dsh-ot-term { flex: 1; min-height: 0; position: relative; padding: 4px 0 0 6px; background: var(--ot-term-bg); overflow: hidden; }
.dsh-ot-term .xterm { height: 100%; }
.dsh-ot-term-overlay {
  position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 10px; background: rgba(0, 0, 0, .55); color: #fff; font-size: 13px; text-align: center; padding: 20px; z-index: 4;
}
.dsh-ot-term-overlay[data-hidden="true"] { display: none; }

/* ---------------------------------------------------------------- editor */
.dsh-ot-editor { flex: 1; min-height: 0; display: flex; flex-direction: column; padding: 10px 12px 12px; gap: 8px; }
.dsh-ot-editor-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.dsh-ot-editor-path { min-width: 0; font-size: 12px; color: var(--ot-text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-family: var(--ot-mono); }
.dsh-ot-editor-body { flex: 1; min-height: 0; display: flex; border: 1px solid var(--ot-border); border-radius: 10px; overflow: hidden; background: var(--ot-bg); }
.dsh-ot-editor-gutter { flex: none; padding: 8px 6px 8px 10px; text-align: right; color: var(--ot-text-3); background: var(--ot-fill); font-family: var(--ot-mono); font-size: 12.5px; line-height: 1.5; user-select: none; overflow: hidden; min-width: 44px; }
.dsh-ot-editor-area { flex: 1; min-width: 0; border: none; padding: 8px 10px; resize: none; background: transparent; color: var(--ot-text); font-family: var(--ot-mono); font-size: 12.5px; line-height: 1.5; tab-size: 4; white-space: pre; overflow: auto; }
.dsh-ot-editor-area:focus { outline: none; }
.dsh-ot-editor-foot { display: flex; gap: 12px; font-size: 11px; color: var(--ot-text-3); font-variant-numeric: tabular-nums; }

/* ------------------------------------------------------------------ sftp */
.dsh-ot-sftp { flex: 1; min-height: 0; display: flex; flex-direction: column; border-top: 1px solid var(--ot-border); overflow: hidden; }
.dsh-ot-sftp-head { display: flex; align-items: center; gap: 6px; padding: 6px 8px; border-bottom: 1px solid var(--ot-border); background: var(--ot-toolbar); }
.dsh-ot-sftp-title { font-size: 12px; font-weight: 600; color: var(--ot-text-2); }
.dsh-ot-sftp-path { padding: 4px 8px; font-size: 11px; color: var(--ot-text-3); font-family: var(--ot-mono); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; direction: rtl; text-align: left; }
.dsh-ot-sftp-search { padding: 4px 8px 6px; }
.dsh-ot-sftp-tree { flex: 1; min-height: 0; overflow: auto; padding: 2px 0 10px; }
.dsh-ot-sftp[data-drop="true"] { outline: 2px dashed var(--ot-primary); outline-offset: -4px; }
.dsh-ot-node { display: flex; align-items: center; gap: 6px; padding: 3px 8px; cursor: pointer; user-select: none; font-size: 12.5px; }
.dsh-ot-node:hover { background: var(--ot-fill-hover); }
.dsh-ot-node[data-active="true"] { background: var(--ot-active); color: var(--ot-primary); }
.dsh-ot-node-twist { width: 14px; flex: none; text-align: center; color: var(--ot-text-3); font-size: 10px; }
.dsh-ot-node-icon { flex: none; display: inline-flex; color: var(--ot-file); }
.dsh-ot-node-icon[data-kind="dir"] { color: var(--ot-folder); }
.dsh-ot-node-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-node-size { flex: none; font-size: 11px; color: var(--ot-text-3); font-variant-numeric: tabular-nums; }
.dsh-ot-node-link { flex: none; font-size: 11px; color: var(--ot-text-3); max-width: 120px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-node-actions { flex: none; display: none; gap: 2px; }
.dsh-ot-node:hover .dsh-ot-node-actions { display: flex; }
.dsh-ot-sftp-hint, .dsh-ot-sftp-error { padding: 10px 12px; font-size: 12px; color: var(--ot-text-3); }
.dsh-ot-sftp-error { color: var(--ot-danger); }
.dsh-ot-search-row { display: flex; flex-direction: column; gap: 1px; padding: 5px 10px; cursor: pointer; }
.dsh-ot-search-row:hover { background: var(--ot-fill-hover); }
.dsh-ot-search-name { display: flex; align-items: center; gap: 6px; font-size: 12.5px; }
.dsh-ot-search-path { font-size: 11px; color: var(--ot-text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
/* --------------------------------------------------------------- AI bar */
.dsh-ot-ai { flex: none; display: flex; flex-direction: column; gap: 6px; padding: 8px 12px; border-top: 1px solid var(--ot-border); background: var(--ot-toolbar); }
.dsh-ot-ai[data-hidden="true"] { display: none; }
.dsh-ot-ai-row { display: flex; align-items: center; gap: 8px; }
.dsh-ot-ai-row input { flex: 1; }
.dsh-ot-ai-meta { display: flex; align-items: center; gap: 10px; font-size: 11px; color: var(--ot-text-3); }
.dsh-ot-ai-out { padding: 8px 10px; border: 1px solid var(--ot-primary-line); border-radius: 8px; background: var(--ot-primary-soft); font-family: var(--ot-mono); font-size: 12.5px; white-space: pre-wrap; word-break: break-all; max-height: 30vh; overflow: auto; }
.dsh-ot-ai-out[data-kind="explain"] { font-family: inherit; white-space: pre-wrap; word-break: normal; }
.dsh-ot-ai-danger { display: flex; align-items: flex-start; gap: 8px; padding: 6px 10px; border-radius: 8px; background: rgba(211, 60, 76, .12); color: var(--ot-danger); font-size: 12px; }

/* --------------------------------------------------------- transfer drawer */
.dsh-ot-drawer { position: absolute; left: 0; right: 0; bottom: 0; max-height: 46%; display: flex; flex-direction: column; background: var(--ot-bg); border-top: 1px solid var(--ot-border); box-shadow: var(--ot-shadow); z-index: 8; }
.dsh-ot-drawer[data-hidden="true"] { display: none; }
.dsh-ot-drawer-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 10px 12px; border-bottom: 1px solid var(--ot-border); }
.dsh-ot-drawer-title { font-size: 15px; font-weight: 600; }
.dsh-ot-drawer-body { flex: 1; min-height: 0; overflow: auto; padding: 10px 12px; display: flex; flex-direction: column; gap: 10px; }
.dsh-ot-task { padding: 10px; border: 1px solid var(--ot-border-light); border-radius: 8px; background: var(--ot-bg); }
.dsh-ot-task-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.dsh-ot-task-name { font-weight: 600; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-task-line { margin-top: 5px; font-size: 11.5px; color: var(--ot-text-3); word-break: break-all; }
.dsh-ot-task-meta { margin-top: 5px; display: flex; gap: 14px; flex-wrap: wrap; font-size: 11.5px; color: var(--ot-text-3); font-variant-numeric: tabular-nums; }
.dsh-ot-progress { margin-top: 7px; height: 8px; border-radius: 999px; background: var(--ot-fill); overflow: hidden; }
.dsh-ot-progress > span { display: block; height: 100%; background: var(--ot-primary); transition: width .2s ease; }
.dsh-ot-progress[data-state="completed"] > span { background: var(--ot-success); }
.dsh-ot-progress[data-state="failed"] > span { background: var(--ot-danger); }
.dsh-ot-task-error { margin-top: 6px; font-size: 11.5px; color: var(--ot-danger); }

/* --------------------------------------------------- menus, dialogs, toasts */
.dsh-ot-menu { position: fixed; z-index: 2147483000; min-width: 180px; max-width: 280px; padding: 4px; border: 1px solid var(--ot-border); border-radius: 10px; background: var(--ot-bg); color: var(--ot-text); box-shadow: var(--ot-shadow-lg); font-size: 13px; }
.dsh-ot-menu-title { padding: 4px 8px 6px; font-size: 11px; color: var(--ot-text-3); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dsh-ot-menu-item { display: flex; align-items: center; gap: 8px; width: 100%; padding: 6px 8px; border: none; border-radius: 7px; background: transparent; color: inherit; font: inherit; text-align: left; cursor: pointer; }
.dsh-ot-menu-item:hover:not(:disabled) { background: var(--ot-fill-hover); }
.dsh-ot-menu-item:disabled { opacity: .45; cursor: not-allowed; }
.dsh-ot-menu-item[data-tone="danger"] { color: var(--ot-danger); }
.dsh-ot-menu-sep { height: 1px; margin: 4px 2px; background: var(--ot-border); }
.dsh-ot-overlay { position: fixed; inset: 0; z-index: 2147483100; display: flex; align-items: center; justify-content: center; padding: 24px; background: rgba(15, 23, 42, .45); }
.dsh-ot-dialog { width: min(680px, 100%); max-height: calc(100vh - 48px); display: flex; flex-direction: column; border-radius: 14px; background: var(--ot-bg); color: var(--ot-text); box-shadow: var(--ot-shadow-lg); overflow: hidden; }
.dsh-ot-dialog[data-size="small"] { width: min(440px, 100%); }
.dsh-ot-dialog[data-size="large"] { width: min(820px, 100%); }
.dsh-ot-dialog-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 14px 16px; border-bottom: 1px solid var(--ot-border); font-size: 15px; font-weight: 600; }
.dsh-ot-dialog-body { flex: 1; min-height: 0; overflow: auto; padding: 14px 16px; display: flex; flex-direction: column; gap: 12px; }
.dsh-ot-dialog-foot { display: flex; justify-content: flex-end; gap: 8px; padding: 12px 16px; border-top: 1px solid var(--ot-border); }
.dsh-ot-field { display: flex; flex-direction: column; gap: 4px; }
.dsh-ot-field > label { font-size: 12px; color: var(--ot-text-2); }
.dsh-ot-field-hint { font-size: 11px; color: var(--ot-text-3); }
.dsh-ot-row { display: flex; gap: 10px; }
.dsh-ot-row > * { flex: 1; min-width: 0; }
.dsh-ot-check { display: flex; align-items: center; gap: 6px; font-size: 12.5px; }
.dsh-ot-section { border: 1px solid var(--ot-border-light); border-radius: 10px; padding: 12px; display: flex; flex-direction: column; gap: 10px; }
.dsh-ot-section-head { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.dsh-ot-section-title { font-size: 13px; font-weight: 600; }
.dsh-ot-mono { font-family: var(--ot-mono); font-size: 12px; word-break: break-all; }
.dsh-ot-toasts { position: fixed; top: 16px; left: 50%; transform: translateX(-50%); z-index: 2147483200; display: flex; flex-direction: column; gap: 8px; align-items: center; pointer-events: none; }
.dsh-ot-toast { padding: 8px 14px; border-radius: 10px; background: var(--ot-bg, #fff); color: var(--ot-text, #303133); border: 1px solid var(--ot-border, #eaeaea); box-shadow: var(--ot-shadow); font-size: 13px; max-width: 60vw; }
.dsh-ot-toast[data-tone="success"] { border-color: var(--ot-success); color: var(--ot-success); }
.dsh-ot-toast[data-tone="error"] { border-color: var(--ot-danger); color: var(--ot-danger); }
.dsh-ot-toast[data-tone="warning"] { border-color: var(--ot-warning); color: var(--ot-warning); }
@media (max-width: 860px) { .dsh-ot-toolbar { flex-wrap: wrap; } .dsh-ot-side { position: absolute; z-index: 9; height: 100%; box-shadow: var(--ot-shadow); } }
`

/** Inject the stylesheet once. */
function injectStyles() {
  if (document.getElementById(STYLE_ID) !== null) return
  document.head.append(el('style', { id: STYLE_ID, type: 'text/css' }, STYLES))
}

// ===== src/client/model.js =====
/**
 * The model: one plain object, plus a subscriber list.
 *
 * The panel repaints from this on every change, EXCEPT for the two things that
 * cannot survive a repaint — a live xterm instance and an editor textarea's caret.
 * Those live in per-tab pane elements that `shell.js` reconciles instead of
 * rebuilding (see `syncPanes`), which is the one structural difference from the
 * sibling plugins' "repaint everything" approach.
 */

const model = {
  // shell
  open: false,
  connected: false,
  booted: false,
  busy: 0,
  revision: 0,
  language: 'zh',
  // ledger
  servers: [],
  favoriteCommands: [],
  favoriteDirs: {},
  prefs: {},
  workspace: null,
  knownHosts: [],
  workspaces: [],
  // runtime
  sessions: [],
  tasks: [],
  tunnels: { portForwards: [], socks5: [] },
  connections: {},
  jobs: [],
  ai: { available: false, reason: '' },
  local: { pty: false, reason: '', shell: '', platform: '' },
  desktop: { platform: '', rdp: [], vnc: [] },
  vendor: { ready: true, missing: [] },
  // selection
  selectedServerId: '',
  tabs: [],
  activeTabId: null,
  // panes
  sftp: {
    serverId: '',
    path: '',
    loading: false,
    error: '',
    children: {},
    expanded: {},
    search: { keyword: '', loading: false, items: [], truncated: false, active: false },
  },
  drawerOpen: false,
  aiOpen: false,
  aiJobId: null,
  hostKey: null,
  serverFilter: '',
}

/** The local-shell row, which is not a stored server. */
function localServer() {
  return {
    id: LOCAL_SERVER_ID,
    name: t('main.localTerminal'),
    protocol: 'ssh',
    host: '',
    port: 0,
    username: '',
    local: true,
  }
}

/** Every row the sidebar shows, local shell first (the reference's order). */
function allServers() {
  const filter = model.serverFilter.trim().toLowerCase()
  const rows = [localServer(), ...model.servers]
  if (filter.length === 0) return rows
  return rows.filter((row) => (
    row.name.toLowerCase().includes(filter) ||
    String(row.host).toLowerCase().includes(filter) ||
    String(row.group ?? '').toLowerCase().includes(filter)
  ))
}

/** One server row by id (including the local one). */
function serverById(id) {
  if (id === LOCAL_SERVER_ID) return localServer()
  return model.servers.find((row) => row.id === id)
}

/** The selected row, or undefined. */
function selectedServer() {
  return serverById(model.selectedServerId)
}

/** Whether a row can open a terminal. */
function canOpenTerminal(server) {
  return server !== undefined && (server.id === LOCAL_SERVER_ID || server.protocol === 'ssh')
}

/** Whether a row can browse files. */
function canUseSftp(server) {
  return server !== undefined && server.id !== LOCAL_SERVER_ID && server.protocol === 'ssh'
}

/** Whether a row launches a desktop client. */
function canLaunchDesktop(server) {
  return server !== undefined && server.id !== LOCAL_SERVER_ID && (server.protocol === 'rdp' || server.protocol === 'vnc')
}

/** The connection state one row shows as a dot. */
function connectionState(serverId) {
  if (serverId === LOCAL_SERVER_ID) return 'local'
  const row = model.connections[serverId]
  if (row === undefined) return 'disconnected'
  return row.status ?? 'disconnected'
}

/** One preference, with its default. */
function pref(key, fallback) {
  const value = model.prefs[key]
  return value === undefined ? fallback : value
}

/** Tabs, in creation order. */
function tabsOf(serverId) {
  return model.tabs.filter((tab) => tab.serverId === serverId)
}

/** One tab by id. */
function tabById(id) {
  return model.tabs.find((tab) => tab.id === id)
}

/** The active tab, or undefined. */
function activeTab() {
  return tabById(model.activeTabId)
}

/** One session record by id. */
function sessionById(sessionId) {
  return model.sessions.find((row) => row.sessionId === sessionId)
}

/** How many transfers are still moving. */
function runningTaskCount() {
  return model.tasks.filter((task) => task.status === 'pending' || task.status === 'transferring').length
}

/** Whether one forwarding rule is up. */
function isForwardRunning(serverId, ruleId) {
  return model.tunnels.portForwards.some((row) => row.serverId === serverId && row.ruleId === ruleId)
}

/** Whether one server's SOCKS5 proxy is up. */
function isSocksRunning(serverId) {
  return model.tunnels.socks5.some((row) => row.serverId === serverId)
}

/** Whether a server has any tunnel up (the sidebar shows a badge). */
function hasTunnels(serverId) {
  return model.tunnels.portForwards.some((row) => row.serverId === serverId) || isSocksRunning(serverId)
}

/** The favourite directories of one server. */
function favoriteDirsOf(serverId) {
  const rows = model.favoriteDirs[serverId]
  return Array.isArray(rows) ? rows : []
}

// ------------------------------------------------------------------ events
const listeners = new Set()

/** Subscribe to model changes; returns the unsubscriber. */
function onModel(listener) {
  listeners.add(listener)
  return () => listeners.delete(listener)
}

/** Announce a change. A throwing listener must not stop the others. */
function emit() {
  for (const listener of [...listeners]) {
    try {
      listener()
    } catch (error) {
      console.error(LOG + ' listener failed:', messageOf(error))
    }
  }
}

/** Merge a `/state` (or `hello`) payload into the model. */
function applyState(value) {
  if (value === null || typeof value !== 'object') return
  if (typeof value.revision === 'number') model.revision = value.revision
  if (typeof value.language === 'string') lang.current = value.language === 'en' ? 'en' : 'zh'
  if (Array.isArray(value.servers)) model.servers = value.servers
  if (Array.isArray(value.favoriteCommands)) model.favoriteCommands = value.favoriteCommands
  if (value.favoriteDirs !== null && typeof value.favoriteDirs === 'object') model.favoriteDirs = value.favoriteDirs
  if (value.prefs !== null && typeof value.prefs === 'object') model.prefs = value.prefs
  if (Object.hasOwn(value, 'workspace')) model.workspace = value.workspace
  if (Array.isArray(value.sessions)) model.sessions = value.sessions
  if (Array.isArray(value.tasks)) model.tasks = value.tasks
  if (value.tunnels !== null && typeof value.tunnels === 'object') model.tunnels = value.tunnels
  if (value.connections !== null && typeof value.connections === 'object') model.connections = value.connections
  if (Array.isArray(value.jobs)) model.jobs = value.jobs
  if (Array.isArray(value.knownHosts)) model.knownHosts = value.knownHosts
  if (Array.isArray(value.workspaces)) model.workspaces = value.workspaces
  if (value.ai !== null && typeof value.ai === 'object') model.ai = value.ai
  if (value.local !== null && typeof value.local === 'object') model.local = value.local
  if (value.desktop !== null && typeof value.desktop === 'object') model.desktop = value.desktop
  if (value.vendor !== null && typeof value.vendor === 'object') model.vendor = value.vendor
}

/** Merge one session record. */
function mergeSession(record) {
  const index = model.sessions.findIndex((row) => row.sessionId === record.sessionId)
  if (index === -1) model.sessions = [...model.sessions, record]
  else {
    const next = [...model.sessions]
    next[index] = record
    model.sessions = next
  }
}

/** Merge one transfer task, newest first. */
function mergeTask(record) {
  const index = model.tasks.findIndex((row) => row.id === record.id)
  if (index === -1) model.tasks = [record, ...model.tasks].slice(0, 200)
  else {
    const next = [...model.tasks]
    next[index] = record
    model.tasks = next
  }
}

/** Merge one AI job. */
function mergeJob(record) {
  const index = model.jobs.findIndex((row) => row.id === record.id)
  if (index === -1) model.jobs = [record, ...model.jobs].slice(0, 20)
  else {
    const next = [...model.jobs]
    next[index] = record
    model.jobs = next
  }
}

/** The AI job the bar is showing. */
function activeJob() {
  return model.jobs.find((row) => row.id === model.aiJobId)
}

// ===== src/client/api.js =====
/**
 * The transport: the `{ok, value}` / `{ok, error}` envelope the host speaks, the SSE
 * stream the panel listens on, the terminal WebSocket, and the batched HTTP fallback
 * for input.
 *
 * Terminal bytes prefer the WebSocket — DSH's webserver has an upgrade hook, so a
 * keystroke can be a frame instead of an HTTP request. Everything else (ledger
 * changes, transfer progress, tunnel state, AI deltas) stays on SSE.
 *
 * The HTTP path is kept as a fallback rather than deleted, because a build without the
 * upgrade hook, or a proxy that eats upgrades, must still give a working terminal:
 * keystrokes are then coalesced into one in-flight POST (`INPUT_FLUSH_MS`), so holding
 * a key is one request per frame rather than one per character, and output arrives on
 * the SSE stream instead.
 */

/** A rejected envelope, carrying the host's stable code. */
class ApiError extends Error {
  constructor(code, message, extra) {
    super(message)
    this.name = 'ApiError'
    this.code = code
    if (extra !== undefined && extra !== null) Object.assign(this, extra)
  }
}

/** This panel's identity on the event stream (stable across reloads). */
function clientId() {
  let id = storeGet(STORE_KEYS.clientId, '')
  if (typeof id !== 'string' || id.length === 0) {
    id = newId('panel')
    storeSet(STORE_KEYS.clientId, id)
  }
  return id
}

/** GET one route. */
async function apiGet(path, params) {
  const query = new URLSearchParams()
  if (params !== undefined && params !== null) {
    for (const [key, value] of Object.entries(params)) {
      if (value === undefined || value === null || value === '') continue
      query.set(key, String(value))
    }
  }
  const suffix = query.toString()
  return unwrap(await fetch(ROUTE_PREFIX + path + (suffix.length > 0 ? '?' + suffix : ''), {
    headers: { accept: 'application/json' },
  }))
}

/** POST one route with a JSON body. */
async function apiPost(path, body) {
  return unwrap(await fetch(ROUTE_PREFIX + path, {
    method: 'POST',
    headers: { 'content-type': 'application/json', accept: 'application/json' },
    body: JSON.stringify(body ?? {}),
  }))
}

/** Unwrap one envelope, throwing an ApiError on failure. */
async function unwrap(response) {
  let payload
  try {
    payload = await response.json()
  } catch {
    throw new ApiError('internal', 'HTTP ' + response.status + '：返回的不是 JSON')
  }
  if (payload !== null && typeof payload === 'object' && payload.ok === true) return payload.value
  const error = payload !== null && typeof payload === 'object' ? payload.error : undefined
  throw new ApiError(
    error !== undefined && typeof error.code === 'string' ? error.code : 'internal',
    error !== undefined && typeof error.message === 'string' ? error.message : 'HTTP ' + response.status,
    error,
  )
}

/** Run one request with the busy counter held. */
async function withBusy(run) {
  model.busy += 1
  emit()
  try {
    return await run()
  } finally {
    model.busy -= 1
    emit()
  }
}

// --------------------------------------------------------------------- SSE
let sse = null

/**
 * Subscribe to the host's event stream.
 *
 * Frame kinds: `hello` (a whole `/state`), `state` (the ledger changed — refetch),
 * `session`, `session-removed`, `connection`, `output`, `overflow`, `task`, `tasks`,
 * `tunnels`, `job`, `job-delta`.
 */
function startSse(handlers) {
  stopSse()
  if (typeof window.EventSource !== 'function') return
  let source
  try {
    source = new window.EventSource(SSE_PATH + '?clientId=' + encodeURIComponent(clientId()))
  } catch (error) {
    console.warn(LOG + ' event stream unavailable:', messageOf(error))
    return
  }
  sse = source

  source.addEventListener('open', () => {
    model.connected = true
    emit()
    // A reconnected stream has forgotten which sessions this panel shows.
    void pushSubscriptions()
  })
  source.addEventListener('error', () => {
    // EventSource reconnects on its own; the panel only reflects the state.
    model.connected = false
    emit()
  })
  source.addEventListener('hello', (event) => {
    model.connected = true
    const data = parseEvent(event)
    if (data !== undefined) applyState(data)
    emit()
    void pushSubscriptions()
    if (handlers.onHello !== undefined) handlers.onHello(data)
  })
  source.addEventListener('state', (event) => {
    const data = parseEvent(event)
    if (data !== undefined && typeof data.revision === 'number' && data.revision === model.revision) return
    void loadState()
  })
  source.addEventListener('session', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.sessionId !== 'string') return
    mergeSession(data)
    if (handlers.onSession !== undefined) handlers.onSession(data)
    emit()
  })
  source.addEventListener('session-removed', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.sessionId !== 'string') return
    model.sessions = model.sessions.filter((row) => row.sessionId !== data.sessionId)
    if (handlers.onSessionRemoved !== undefined) handlers.onSessionRemoved(data.sessionId)
    emit()
  })
  source.addEventListener('connection', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.serverId !== 'string') return
    const next = { ...model.connections }
    next[data.serverId] = { ...(next[data.serverId] ?? {}), status: data.status, error: data.error }
    model.connections = next
    emit()
  })
  source.addEventListener('output', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.sessionId !== 'string' || typeof data.data !== 'string') return
    // The whole frame is handed over, not just the bytes: it carries the byte offset
    // the replay splice needs, and dropping it here would make a re-attach over the
    // SSE fallback print the last screenful twice.
    if (handlers.onOutput !== undefined) handlers.onOutput(data.sessionId, data.data, data)
  })
  source.addEventListener('overflow', (event) => {
    const data = parseEvent(event)
    if (data !== undefined && handlers.onOverflow !== undefined) handlers.onOverflow(data.sessionId)
  })
  source.addEventListener('task', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.id !== 'string') return
    mergeTask(data)
    emit()
  })
  source.addEventListener('tasks', (event) => {
    const data = parseEvent(event)
    if (data !== undefined && Array.isArray(data.tasks)) model.tasks = data.tasks
    emit()
  })
  source.addEventListener('tunnels', (event) => {
    const data = parseEvent(event)
    if (data !== undefined && data.tunnels !== undefined) model.tunnels = data.tunnels
    emit()
  })
  source.addEventListener('job', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.id !== 'string') return
    mergeJob(data)
    emit()
  })
  source.addEventListener('job-delta', (event) => {
    const data = parseEvent(event)
    if (data === undefined || typeof data.id !== 'string' || typeof data.delta !== 'string') return
    const job = model.jobs.find((row) => row.id === data.id)
    if (job === undefined) return
    job.text = (job.text ?? '') + data.delta
    emit()
  })
}

/** Parse one SSE payload, tolerating a truncated frame. */
function parseEvent(event) {
  try {
    const data = JSON.parse(event.data)
    return data === null || typeof data !== 'object' ? undefined : data
  } catch {
    return undefined
  }
}

/** Close the stream. */
function stopSse() {
  if (sse !== null) {
    try {
      sse.close()
    } catch { /* already closed */ }
    sse = null
  }
}

/** Tell the host which sessions this panel is showing. */
async function pushSubscriptions() {
  const sessionIds = model.tabs.filter((tab) => tab.kind === 'terminal' && typeof tab.sessionId === 'string')
    .map((tab) => tab.sessionId)
  if (socketSend({ kind: 'subscribe', sessionIds })) return
  try {
    await apiPost('/terminal/subscribe', { clientId: clientId(), sessionIds })
  } catch (error) {
    console.warn(LOG + ' subscribe failed:', messageOf(error))
  }
}

// ------------------------------------------------------------ terminal socket
const socketState = { socket: null, ready: false, attempts: 0, timer: null, handlers: null }

/** Whether the terminal socket is usable right now. */
function socketOpen() {
  return socketState.ready && socketState.socket !== null && socketState.socket.readyState === 1
}

/** Send one frame over the socket; false when there is no socket to send it on. */
function socketSend(message) {
  if (!socketOpen()) return false
  try {
    socketState.socket.send(JSON.stringify(message))
    return true
  } catch {
    return false
  }
}

/**
 * Open the terminal socket, retrying a few times before settling for HTTP.
 *
 * A missing `WebSocket` (an old browser, or the test's synthetic DOM) and a refused
 * upgrade land in the same place: `socketOpen()` stays false and every caller takes
 * the HTTP path.
 */
function startSocket(handlers) {
  socketState.handlers = handlers
  if (typeof window.WebSocket !== 'function') return
  stopSocket()
  const origin = String(window.location?.origin ?? '')
  const base = origin.startsWith('https') ? 'wss' + origin.slice(5) : (origin.startsWith('http') ? 'ws' + origin.slice(4) : '')
  if (base.length === 0) return
  let socket
  try {
    socket = new window.WebSocket(base + SOCKET_PATH + '?clientId=' + encodeURIComponent(clientId()))
  } catch (error) {
    console.warn(LOG + ' terminal socket unavailable:', messageOf(error))
    return
  }
  socketState.socket = socket
  socket.addEventListener('open', () => {
    socketState.ready = true
    socketState.attempts = 0
    void pushSubscriptions()
  })
  socket.addEventListener('message', (event) => {
    let frame
    try {
      frame = JSON.parse(String(event.data))
    } catch {
      return
    }
    if (frame === null || typeof frame !== 'object') return
    const data = frame.data
    if (frame.event === 'output' && data !== null && typeof data === 'object') {
      if (handlers.onOutput !== undefined) handlers.onOutput(data.sessionId, data.data, data)
      return
    }
    if (frame.event === 'overflow' && data !== null && typeof data === 'object') {
      if (handlers.onOverflow !== undefined) handlers.onOverflow(data.sessionId)
      return
    }
    if (frame.event === 'socket-error' && data !== null && typeof data === 'object') {
      console.warn(LOG + ' socket rejected a frame:', data.message)
    }
  })
  const closed = () => {
    socketState.ready = false
    socketState.socket = null
    // Three tries, then the HTTP path stands: a panel that cannot upgrade should stop
    // hammering the server about it.
    if (socketState.attempts >= 3 || socketState.timer !== null) return
    socketState.attempts += 1
    socketState.timer = setTimeout(() => {
      socketState.timer = null
      if (socketState.handlers !== null) startSocket(socketState.handlers)
    }, 1000 * socketState.attempts)
  }
  socket.addEventListener('close', closed)
  socket.addEventListener('error', closed)
}

/** Close the terminal socket. */
function stopSocket() {
  if (socketState.timer !== null) {
    clearTimeout(socketState.timer)
    socketState.timer = null
  }
  const socket = socketState.socket
  socketState.socket = null
  socketState.ready = false
  if (socket !== null) {
    try {
      socket.close()
    } catch { /* already closed */ }
  }
}

// ----------------------------------------------------------------- loaders
/** The whole state, in one call. */
async function loadState() {
  try {
    applyState(await apiGet('/state'))
  } catch (error) {
    console.warn(LOG + ' state unavailable:', messageOf(error))
  }
  emit()
}

/** Persist a preference patch, optimistically applied first. */
async function savePrefs(patch) {
  model.prefs = { ...model.prefs, ...patch }
  emit()
  try {
    const value = await apiPost('/prefs', { prefs: patch })
    model.prefs = value.prefs
  } catch (error) {
    console.warn(LOG + ' prefs not saved:', messageOf(error))
  }
  emit()
}

// ------------------------------------------------------------- terminal io
/** Pending bytes per session, and the in-flight promise that will send them. */
const inputQueues = new Map()

/**
 * Queue bytes for one session.
 *
 * With the socket up this is one frame per call — a terminal wants the keystroke gone
 * now. Without it, one POST at a time per session: while a request is in flight new
 * keystrokes accumulate and go out together when it settles, which bounds the request
 * rate without ever reordering input.
 */
function sendInput(sessionId, text) {
  if (socketSend({ kind: 'input', sessionId, data: base64OfText(text) })) return
  const queue = inputQueues.get(sessionId) ?? { pending: '', sending: false, timer: null }
  queue.pending += text
  inputQueues.set(sessionId, queue)
  if (queue.sending || queue.timer !== null) return
  queue.timer = setTimeout(() => {
    queue.timer = null
    void flushInput(sessionId)
  }, INPUT_FLUSH_MS)
}

/** Report a new terminal size, over whichever channel is up. */
function sendResize(sessionId, cols, rows) {
  if (socketSend({ kind: 'resize', sessionId, cols, rows })) return
  void apiPost('/terminal/resize', { sessionId, cols, rows }).catch(() => {})
}

/** Send whatever is queued for one session. */
async function flushInput(sessionId) {
  const queue = inputQueues.get(sessionId)
  if (queue === undefined || queue.sending || queue.pending.length === 0) return
  const payload = queue.pending
  queue.pending = ''
  queue.sending = true
  try {
    await apiPost('/terminal/input', { sessionId, data: base64OfText(payload) })
  } catch (error) {
    if (codeOf(error) === 'no_session') {
      const session = sessionById(sessionId)
      if (session !== undefined) mergeSession({ ...session, status: 'closed' })
      emit()
    } else {
      console.warn(LOG + ' input dropped:', messageOf(error))
    }
  } finally {
    queue.sending = false
    if (queue.pending.length > 0) void flushInput(sessionId)
  }
}

/** UTF-8 text → base64, without assuming a Buffer. */
function base64OfText(text) {
  const bytes = new TextEncoder().encode(text)
  let binary = ''
  for (const byte of bytes) binary += String.fromCharCode(byte)
  return window.btoa(binary)
}

/** base64 → a Uint8Array (terminal output arrives this way). */
function bytesOfBase64(value) {
  const binary = window.atob(value)
  const bytes = new Uint8Array(binary.length)
  for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index)
  return bytes
}

/** Forget one session's queue. */
function dropInputQueue(sessionId) {
  const queue = inputQueues.get(sessionId)
  if (queue !== undefined && queue.timer !== null) clearTimeout(queue.timer)
  inputQueues.delete(sessionId)
}

// ------------------------------------------------------------- file bytes
/**
 * Upload one File to a remote directory.
 *
 * `XMLHttpRequest` rather than fetch: it is the only way to get upload progress in a
 * browser, and a 2 GB file with no progress bar is indistinguishable from a hang.
 */
function uploadFile(serverId, dir, file, name, onProgress) {
  return new Promise((resolvePromise, rejectPromise) => {
    const query = new URLSearchParams({ serverId, dir, name: name ?? file.name })
    const request = new XMLHttpRequest()
    request.open('POST', ROUTE_PREFIX + '/sftp/upload?' + query.toString())
    request.responseType = 'json'
    if (onProgress !== undefined && request.upload !== null) {
      request.upload.addEventListener('progress', (event) => {
        if (event.lengthComputable) onProgress(event.loaded, event.total)
      })
    }
    request.addEventListener('load', () => {
      const payload = request.response
      if (payload !== null && typeof payload === 'object' && payload.ok === true) {
        resolvePromise(payload.value)
        return
      }
      const error = payload !== null && typeof payload === 'object' ? payload.error : undefined
      rejectPromise(new ApiError(error?.code ?? 'internal', error?.message ?? 'HTTP ' + request.status))
    })
    request.addEventListener('error', () => rejectPromise(new ApiError('internal', '上传请求失败')))
    request.addEventListener('abort', () => rejectPromise(new ApiError('internal', '上传已取消')))
    request.send(file)
  })
}

/** Start a browser download of one remote path (a file, or a folder as tar). */
function downloadPath(serverId, path, kind) {
  const query = new URLSearchParams({ serverId, path })
  if (kind !== undefined) query.set('kind', kind)
  // A hidden anchor rather than window.open: no popup blocker, and the
  // Content-Disposition filename is honoured.
  const link = el('a', { href: ROUTE_PREFIX + '/sftp/download?' + query.toString(), download: '' })
  document.body.append(link)
  link.click()
  link.remove()
}

// ===== src/client/icons.js =====
/**
 * The icon set: 20×20 stroked glyphs, built with `svg()` rather than `innerHTML`.
 *
 * The reference used Element Plus's icon components plus a few hand-drawn SVGs in
 * its toolbar; those hand-drawn ones (the sidebar toggle, the command list, the
 * starred folder) are reproduced here path for path, and the rest are drawn in the
 * same 1.8-stroke style so the set looks like one family.
 */

/** Path data per icon. A string is one path; an array is several. */
const ICON_PATHS = {
  terminal: ['M2.5 4.5h15v11h-15z', 'm6 8 2 2-2 2', 'M10.5 12.5h3.5'],
  'sidebar-collapse': ['M2.75 3.25h14.5v13.5H2.75z', 'M7.5 3.75v12.5', 'M12.75 10H17', 'm10.75 8 2 2-2 2'],
  'sidebar-expand': ['M2.75 3.25h14.5v13.5H2.75z', 'M7.5 3.75v12.5', 'M10.75 10H15', 'm12.75 8-2 2 2 2'],
  commands: ['M2.5 3.5h15v13h-15z', 'M5.75 7.25h5.5', 'm5.75 10 1.8-1.8', 'm5.75 10 1.8 1.8', 'M10 12.75h4.25'],
  'folder-star': [
    'M2.75 6.25a2 2 0 0 1 2-2H8l1.45 1.6a1.4 1.4 0 0 0 1.03.45h4.77a2 2 0 0 1 2 2v5.45a2 2 0 0 1-2 2H4.75a2 2 0 0 1-2-2z',
    'm13.15 9.15.48 1.02 1.12.18-.8.78.18 1.1-.98-.54-.98.54.18-1.1-.8-.78 1.12-.18z',
  ],
  folder: ['M2.75 6.25a2 2 0 0 1 2-2H8l1.45 1.6a1.4 1.4 0 0 0 1.03.45h4.77a2 2 0 0 1 2 2v5.45a2 2 0 0 1-2 2H4.75a2 2 0 0 1-2-2z'],
  file: ['M5 2.75h6l4 4v10.5H5z', 'M11 2.75V7h4'],
  upload: ['M10 14.5V5.5', 'm6.5 9 3.5-3.5L13.5 9', 'M3.5 16.5h13'],
  download: ['M10 5.5v9', 'm6.5 11 3.5 3.5L13.5 11', 'M3.5 16.5h13'],
  refresh: ['M16.5 10a6.5 6.5 0 1 1-2.2-4.87', 'M16.5 3.5V7h-3.5'],
  plus: ['M10 4.5v11', 'M4.5 10h11'],
  close: ['m5 5 10 10', 'm15 5-10 10'],
  more: ['M5 10h.01', 'M10 10h.01', 'M15 10h.01'],
  settings: ['M10 12.75a2.75 2.75 0 1 0 0-5.5 2.75 2.75 0 0 0 0 5.5z', 'M10 2.5v2M10 15.5v2M2.5 10h2M15.5 10h2M4.7 4.7l1.4 1.4M13.9 13.9l1.4 1.4M15.3 4.7l-1.4 1.4M6.1 13.9l-1.4 1.4'],
  search: ['M9 15a6 6 0 1 0 0-12 6 6 0 0 0 0 12z', 'm13.5 13.5 3.5 3.5'],
  monitor: ['M3 4.5h14v9H3z', 'M7.5 17h5', 'M10 13.5V17'],
  server: ['M3.5 4h13v4h-13z', 'M3.5 12h13v4h-13z', 'M6 6h.01M6 14h.01'],
  key: ['M12.5 3.5a4 4 0 1 0-3.2 6.4L4 15.2V17h1.8l1-1h1.5v-1.5h1.5l1.4-1.4a4 4 0 0 0 5.3-5.3', 'M13.8 6.2h.01'],
  trash: ['M4.5 6.5h11', 'M8 6.5V4.5h4v2', 'M6 6.5 6.7 17h6.6L14 6.5', 'M8.7 9.5v5M11.3 9.5v5'],
  edit: ['m4 16 1-3.5 8-8 2.5 2.5-8 8z', 'M12 5.5 14.5 8'],
  link: ['M8.5 11.5 6 14a2.5 2.5 0 0 1-3.5-3.5l2.5-2.5', 'M11.5 8.5 14 6a2.5 2.5 0 0 1 3.5 3.5L15 12', 'm7.5 12.5 5-5'],
  tunnel: ['M3 10h5', 'M12 10h5', 'm8 6.5 4 3.5-4 3.5'],
  sparkles: ['m10 3 1.4 3.2L14.6 7.6l-3.2 1.4L10 12.2 8.6 9 5.4 7.6 8.6 6.2z', 'M15 13l.7 1.6 1.6.7-1.6.7-.7 1.6-.7-1.6-1.6-.7 1.6-.7z'],
  play: ['m6.5 4.5 9 5.5-9 5.5z'],
  chevron: ['m8 6 4 4-4 4'],
  check: ['m4.5 10.5 3.5 3.5 7.5-8'],
  warning: ['M10 3.5 17.5 16.5H2.5z', 'M10 8v3.5M10 14h.01'],
  eye: ['M2.5 10s3-5 7.5-5 7.5 5 7.5 5-3 5-7.5 5S2.5 10 2.5 10z', 'M10 12a2 2 0 1 0 0-4 2 2 0 0 0 0 4z'],
  copy: ['M7 7h9v9H7z', 'M4.5 12.5V4h8'],
  home: ['m3.5 9.5 6.5-6 6.5 6', 'M5.5 8.5V16h9V8.5'],
  arrowUp: ['M10 15.5V5', 'm5.5 9.5 4.5-4.5 4.5 4.5'],
  lock: ['M5.5 9h9v7.5h-9z', 'M7.5 9V6.75a2.5 2.5 0 0 1 5 0V9'],
}

/**
 * One icon element. `size` is the rendered box; the artwork is a 20-unit grid.
 */
function icon(name, size) {
  const paths = ICON_PATHS[name] ?? ICON_PATHS.file
  const list = Array.isArray(paths) ? paths : [paths]
  return svg('svg', {
    viewBox: '0 0 20 20',
    width: size ?? 16,
    height: size ?? 16,
    fill: 'none',
    stroke: 'currentColor',
    'stroke-width': 1.8,
    'stroke-linecap': 'round',
    'stroke-linejoin': 'round',
    'aria-hidden': 'true',
    focusable: 'false',
  }, ...list.map((d) => svg('path', { d })))
}

// ===== src/client/widgets.js =====
/**
 * Widgets: the buttons, tags, context menus, dialogs and toasts the panel is built
 * from.
 *
 * The reference had Element Plus for all of this (`el-button`, `el-dropdown`,
 * `el-dialog`, `ElMessage`, `ElMessageBox`). A dsh client bundle ships no component
 * library, so each one is re-implemented here at the size the panel actually uses —
 * which is why the dialogs are promises rather than a component tree: every call
 * site in the reference was `await ElMessageBox.confirm(...)`.
 */

/** One button. */
function button(options) {
  const node = el('button', {
    type: 'button',
    class: 'dsh-ot-btn',
    'data-variant': options.variant,
    'data-icon': options.iconOnly === true ? 'true' : undefined,
    'data-active': options.active === true ? 'true' : undefined,
    title: options.title ?? (options.iconOnly === true ? options.label : undefined),
    'aria-label': options.iconOnly === true ? (options.title ?? options.label) : undefined,
    disabled: options.disabled === true,
    onClick: options.onClick,
  })
  if (options.icon !== undefined) node.append(icon(options.icon, options.iconSize ?? 15))
  if (options.iconOnly !== true && options.label !== undefined) node.append(el('span', {}, options.label))
  if (options.badge !== undefined && options.badge !== null && options.badge !== 0) {
    node.append(el('span', { class: 'dsh-ot-badge' }, String(options.badge)))
  }
  return node
}

/** A square icon-only button. */
function iconButton(name, options) {
  return button({ ...options, icon: name, iconOnly: true })
}

/** A coloured pill. */
function tag(label, tone, options) {
  return el('span', {
    class: 'dsh-ot-tag',
    'data-tone': tone,
    title: options?.title,
    onClick: options?.onClick,
    style: options?.onClick === undefined ? undefined : { cursor: 'pointer' },
  }, label)
}

/** A labelled form row. */
function field(label, control, hint) {
  return el('div', { class: 'dsh-ot-field' },
    label === undefined ? undefined : el('label', {}, label),
    control,
    hint === undefined ? undefined : el('div', { class: 'dsh-ot-field-hint' }, hint))
}

/** A text input. */
function input(options) {
  return el('input', {
    class: 'dsh-ot-input',
    type: options.type ?? 'text',
    value: options.value ?? '',
    placeholder: options.placeholder,
    autocomplete: options.type === 'password' ? 'new-password' : 'off',
    spellcheck: 'false',
    min: options.min,
    max: options.max,
    disabled: options.disabled === true,
    onInput: options.onInput,
    onKeydown: options.onKeydown,
    onChange: options.onChange,
  })
}

/** A textarea. */
function textarea(options) {
  const node = el('textarea', {
    class: 'dsh-ot-textarea',
    placeholder: options.placeholder,
    rows: options.rows ?? 4,
    spellcheck: 'false',
    onInput: options.onInput,
  })
  node.value = options.value ?? ''
  return node
}

/** A checkbox row. */
function checkbox(label, checked, onChange) {
  const box = el('input', { type: 'checkbox', onChange: (event) => onChange(event.target.checked) })
  box.checked = checked === true
  return el('label', { class: 'dsh-ot-check' }, box, el('span', {}, label))
}

/** A radio group rendered as a row of labelled radios. */
function radioGroup(name, options, value, onChange) {
  return el('div', { class: 'dsh-ot-row', style: { gap: '14px' } }, ...options.map((option) => {
    const dot = el('input', { type: 'radio', name, value: option.id, onChange: () => onChange(option.id) })
    dot.checked = option.id === value
    return el('label', { class: 'dsh-ot-check' }, dot, el('span', {}, option.label))
  }))
}

/** A select. */
function select(options, value, onChange) {
  const node = el('select', { class: 'dsh-ot-select', onChange: (event) => onChange(event.target.value) })
  for (const option of options) {
    const item = el('option', { value: option.id }, option.label)
    if (option.id === value) item.selected = true
    node.append(item)
  }
  return node
}

/** A determinate progress bar. */
function progressBar(percent, state) {
  const value = Math.max(0, Math.min(100, Number(percent) || 0))
  return el('div', { class: 'dsh-ot-progress', 'data-state': state }, el('span', { style: { width: value + '%' } }))
}

// ------------------------------------------------------------------ toasts
let toastHost = null

/** Show a transient message. */
function toast(message, tone, ms) {
  if (typeof document === 'undefined') return
  if (toastHost === null || !toastHost.isConnected) {
    toastHost = el('div', { class: 'dsh-ot-toasts dsh-ot-overlay-tokens' })
    document.body.append(toastHost)
  }
  const node = el('div', { class: 'dsh-ot-toast', 'data-tone': tone }, String(message))
  toastHost.append(node)
  const timer = setTimeout(() => node.remove(), ms ?? (tone === 'error' ? 6000 : 2600))
  node.addEventListener('click', () => {
    clearTimeout(timer)
    node.remove()
  })
}

/** Report a rejected request. */
function toastError(error) {
  toast(friendlyError(error), 'error')
}

// ------------------------------------------------------------------- menus
let openMenu = null

/** Close the context menu. */
function closeMenu() {
  if (openMenu === null) return
  openMenu.remove()
  openMenu = null
}

/**
 * Open a context menu at a point. `items` are `{label, icon, tone, disabled, onClick}`
 * or `{separator: true}`.
 *
 * Positioning clamps to the viewport, the way the reference's hand-rolled menu did:
 * a right-click near the bottom edge must not open a menu that runs off screen.
 */
function openContextMenu(x, y, items, title) {
  closeMenu()
  const menu = el('div', { class: 'dsh-ot-menu dsh-ot-overlay', role: 'menu' })
  if (title !== undefined) menu.append(el('div', { class: 'dsh-ot-menu-title' }, title))
  for (const item of items) {
    if (item === undefined || item === null || item === false) continue
    if (item.separator === true) {
      menu.append(el('div', { class: 'dsh-ot-menu-sep' }))
      continue
    }
    menu.append(el('button', {
      type: 'button',
      class: 'dsh-ot-menu-item',
      'data-tone': item.tone,
      disabled: item.disabled === true,
      onClick: () => {
        closeMenu()
        try {
          item.onClick()
        } catch (error) {
          toastError(error)
        }
      },
    }, item.icon === undefined ? undefined : icon(item.icon, 14), el('span', {}, item.label)))
  }
  // Measured off-screen first, then clamped: the height depends on the item count.
  menu.style.left = '-9999px'
  menu.style.top = '-9999px'
  document.body.append(menu)
  const box = menu.getBoundingClientRect()
  const width = box.width > 0 ? box.width : 200
  const height = box.height > 0 ? box.height : 240
  const maxX = Math.max(8, (window.innerWidth ?? 1024) - width - 8)
  const maxY = Math.max(8, (window.innerHeight ?? 768) - height - 8)
  menu.style.left = Math.min(Math.max(8, x), maxX) + 'px'
  menu.style.top = Math.min(Math.max(8, y), maxY) + 'px'
  openMenu = menu
  return menu
}

// ----------------------------------------------------------------- dialogs
const overlays = []

/**
 * Open a modal. `build(close)` returns the body; `footer(close)` the buttons.
 * Returns `{close}`.
 */
function openDialog(options) {
  const overlay = el('div', { class: 'dsh-ot-overlay' })
  const record = { overlay, close: null }
  const close = (result) => {
    const index = overlays.indexOf(record)
    if (index !== -1) overlays.splice(index, 1)
    overlay.remove()
    if (options.onClose !== undefined) options.onClose(result)
  }
  record.close = close
  const body = el('div', { class: 'dsh-ot-dialog-body' })
  appendAll(body, [options.build(close)])
  const dialog = el('div', { class: 'dsh-ot-dialog', 'data-size': options.size, role: 'dialog', 'aria-modal': 'true' },
    el('div', { class: 'dsh-ot-dialog-head' },
      el('span', {}, options.title),
      iconButton('close', { variant: 'ghost', title: t('main.close'), onClick: () => close(undefined) })),
    body,
    options.footer === undefined ? undefined : el('div', { class: 'dsh-ot-dialog-foot' }, options.footer(close)))
  overlay.append(dialog)
  overlay.addEventListener('mousedown', (event) => {
    // A click on the backdrop closes; a drag that started inside must not.
    if (event.target === overlay && options.dismissable !== false) close(undefined)
  })
  document.body.append(overlay)
  overlays.push(record)
  const focusTarget = dialog.querySelector('input, textarea, select, button')
  if (focusTarget !== null) {
    try {
      focusTarget.focus()
    } catch { /* not focusable yet */ }
  }
  return record
}

/** Close every open dialog (panel teardown). */
function closeAllOverlays() {
  for (const record of [...overlays]) record.close(undefined)
}

/** A yes/no question. Resolves true when confirmed. */
function confirmDialog(options) {
  return new Promise((resolvePromise) => {
    let settled = false
    const finish = (value) => {
      if (settled) return
      settled = true
      resolvePromise(value)
    }
    openDialog({
      title: options.title,
      size: 'small',
      build: () => [
        el('div', {}, options.message),
        options.detail === undefined ? undefined : el('div', { class: 'dsh-ot-field-hint' }, options.detail),
      ],
      footer: (close) => [
        button({ label: options.cancelLabel ?? t('main.cancel'), onClick: () => close(false) }),
        button({
          label: options.confirmLabel ?? t('main.confirm'),
          variant: options.danger === true ? 'danger' : 'primary',
          onClick: () => close(true),
        }),
      ],
      onClose: (value) => finish(value === true),
    })
  })
}

/** A one-line text prompt. Resolves the trimmed value, or undefined when cancelled. */
function promptDialog(options) {
  return new Promise((resolvePromise) => {
    let settled = false
    const finish = (value) => {
      if (settled) return
      settled = true
      resolvePromise(value)
    }
    let box
    const submit = (close) => {
      const value = String(box.value ?? '').trim()
      if (value.length === 0) {
        toast(options.required ?? t('sftp.promptNameRequired'), 'warning')
        return
      }
      close(value)
    }
    openDialog({
      title: options.title,
      size: 'small',
      build: (close) => {
        box = input({
          value: options.value ?? '',
          placeholder: options.placeholder,
          onKeydown: (event) => {
            if (event.key === 'Enter') {
              event.preventDefault()
              submit(close)
            }
          },
        })
        return [field(options.label, box, options.hint)]
      },
      footer: (close) => [
        button({ label: t('main.cancel'), onClick: () => close(undefined) }),
        button({ label: options.confirmLabel ?? t('main.confirm'), variant: 'primary', onClick: () => submit(close) }),
      ],
      onClose: (value) => finish(typeof value === 'string' ? value : undefined),
    })
  })
}

/** A section box with a heading and actions. */
function section(title, actions, ...children) {
  return el('div', { class: 'dsh-ot-section' },
    el('div', { class: 'dsh-ot-section-head' },
      el('span', { class: 'dsh-ot-section-title' }, title),
      el('div', { class: 'dsh-ot-toolbar-group' }, actions)),
    ...children)
}

// ===== src/client/themes.js =====
/**
 * The terminal colour schemes, ported verbatim from the reference plugin's
 * `src/themes.js` — same twelve names in the same order, same hex values, so a user
 * who picked "gruvbox" over there gets the same terminal here.
 *
 * Two changes were unavoidable:
 *
 *  - xterm 4 called the selection colour `selection`; xterm 5 calls it
 *    `selectionBackground`. The reference kept the old key, which means its
 *    selection colour silently stopped working when it moved to xterm 5. Both keys
 *    are set here.
 *  - `accent-light` followed the host shell's Element Plus custom properties
 *    (`--el-color-primary` and friends). DSH's shell exposes `--dsw-alias-*`
 *    instead, so those are read; the fallbacks are the reference's one-light values,
 *    which is what it looked like before any accent was applied.
 */

const ACCENT_LIGHT_THEME = 'accent-light'

/** Build one xterm theme from the reference's field order. */
function makeTheme(background, foreground, cursor, selection, normal, bright) {
  return {
    background,
    foreground,
    cursor,
    cursorAccent: background,
    selection,
    selectionBackground: selection,
    black: normal[0],
    red: normal[1],
    green: normal[2],
    yellow: normal[3],
    blue: normal[4],
    magenta: normal[5],
    cyan: normal[6],
    white: normal[7],
    brightBlack: bright[0],
    brightRed: bright[1],
    brightGreen: bright[2],
    brightYellow: bright[3],
    brightBlue: bright[4],
    brightMagenta: bright[5],
    brightCyan: bright[6],
    brightWhite: bright[7],
  }
}

/** The one-light palette `accent-light` is built on. */
const ACCENT_LIGHT_BASE = makeTheme('#ffffff', '#383a42', '#526fff', 'rgba(82, 111, 255, 0.2)',
  ['#383a42', '#e45649', '#50a14f', '#c18401', '#4078f2', '#a626a4', '#0184bc', '#a0a1a7'],
  ['#696c77', '#d33f32', '#22863a', '#b07000', '#315fdb', '#8e1f8c', '#006f9a', '#ffffff'])

/** Every theme, in the reference's declaration order. */
const THEMES = {
  midnight: makeTheme('#0b0f14', '#d6dde6', '#8fb3ff', 'rgba(143, 179, 255, 0.35)',
    ['#0b0f14', '#ff6b6b', '#46d39a', '#f8d26a', '#5aa9ff', '#c58bff', '#4fd1c5', '#d6dde6'],
    ['#556070', '#ff8787', '#6ee7b7', '#fde68a', '#7ab8ff', '#d7a7ff', '#7ee6e0', '#f2f5f9']),
  nord: makeTheme('#2e3440', '#d8dee9', '#88c0d0', 'rgba(136, 192, 208, 0.35)',
    ['#3b4252', '#bf616a', '#a3be8c', '#ebcb8b', '#81a1c1', '#b48ead', '#88c0d0', '#e5e9f0'],
    ['#4c566a', '#bf616a', '#a3be8c', '#ebcb8b', '#81a1c1', '#b48ead', '#8fbcbb', '#eceff4']),
  gruvbox: makeTheme('#282828', '#ebdbb2', '#fabd2f', 'rgba(250, 189, 47, 0.3)',
    ['#282828', '#cc241d', '#98971a', '#d79921', '#458588', '#b16286', '#689d6a', '#a89984'],
    ['#928374', '#fb4934', '#b8bb26', '#fabd2f', '#83a598', '#d3869b', '#8ec07c', '#ebdbb2']),
  oneDark: makeTheme('#282c34', '#abb2bf', '#528bff', 'rgba(82, 139, 255, 0.3)',
    ['#282c34', '#e06c75', '#98c379', '#e5c07b', '#61afef', '#c678dd', '#56b6c2', '#abb2bf'],
    ['#5c6370', '#e06c75', '#98c379', '#e5c07b', '#61afef', '#c678dd', '#56b6c2', '#ffffff']),
  catppuccin: makeTheme('#1e1e2e', '#cdd6f4', '#f5e0dc', 'rgba(245, 224, 220, 0.28)',
    ['#1e1e2e', '#f38ba8', '#a6e3a1', '#f9e2af', '#89b4fa', '#cba6f7', '#94e2d5', '#bac2de'],
    ['#45475a', '#f38ba8', '#a6e3a1', '#f9e2af', '#89b4fa', '#cba6f7', '#94e2d5', '#f5e0dc']),
  solarized: makeTheme('#002b36', '#839496', '#93a1a1', 'rgba(147, 161, 161, 0.3)',
    ['#073642', '#dc322f', '#859900', '#b58900', '#268bd2', '#d33682', '#2aa198', '#eee8d5'],
    ['#002b36', '#cb4b16', '#586e75', '#657b83', '#839496', '#6c71c4', '#93a1a1', '#fdf6e3']),
  'solarized-light': makeTheme('#fdf6e3', '#657b83', '#586e75', 'rgba(101, 123, 131, 0.25)',
    ['#073642', '#dc322f', '#859900', '#b58900', '#268bd2', '#d33682', '#2aa198', '#eee8d5'],
    ['#002b36', '#cb4b16', '#586e75', '#657b83', '#839496', '#6c71c4', '#93a1a1', '#fdf6e3']),
  'one-light': makeTheme('#fafafa', '#383a42', '#526fff', 'rgba(82, 111, 255, 0.2)',
    ['#383a42', '#e45649', '#50a14f', '#c18401', '#4078f2', '#a626a4', '#0184bc', '#a0a1a7'],
    ['#696c77', '#d33f32', '#22863a', '#b07000', '#315fdb', '#8e1f8c', '#006f9a', '#ffffff']),
  'github-light': makeTheme('#ffffff', '#24292f', '#0969da', 'rgba(9, 105, 218, 0.2)',
    ['#24292f', '#cf222e', '#1a7f37', '#9a6700', '#0969da', '#8250df', '#1b7c83', '#57606a'],
    ['#6e7781', '#a40e26', '#116329', '#7d4e00', '#0550ae', '#6639ba', '#11606a', '#f6f8fa']),
  'gruvbox-light': makeTheme('#fbf1c7', '#3c3836', '#b57614', 'rgba(181, 118, 20, 0.2)',
    ['#282828', '#cc241d', '#98971a', '#d79921', '#458588', '#b16286', '#689d6a', '#7c6f64'],
    ['#928374', '#9d0006', '#79740e', '#b57614', '#076678', '#8f3f71', '#427b58', '#f9f5d7']),
  dracula: makeTheme('#282a36', '#f8f8f2', '#f8f8f2', 'rgba(189, 147, 249, 0.35)',
    ['#21222c', '#ff5555', '#50fa7b', '#f1fa8c', '#bd93f9', '#ff79c6', '#8be9fd', '#f8f8f2'],
    ['#6272a4', '#ff6e6e', '#69ff94', '#ffffa5', '#d6acff', '#ff92df', '#a4ffff', '#ffffff']),
  [ACCENT_LIGHT_THEME]: ACCENT_LIGHT_BASE,
}

/** The synthetic option that follows the shell's light/dark state. */
const THEME_DEFAULT_OPTION = 'default'
const DARK_MODE_DEFAULT_THEME = 'nord'
const LIGHT_MODE_DEFAULT_THEME = ACCENT_LIGHT_THEME

/** Every option the picker offers, in the reference's order. */
function themeOptions() {
  return [THEME_DEFAULT_OPTION, ...Object.keys(THEMES)]
}

/** Whether the DSH shell is in dark mode right now. */
function isDarkShell() {
  if (typeof document === 'undefined') return false
  const root = document.documentElement
  if (root.classList !== undefined && typeof root.classList.contains === 'function' && root.classList.contains('dark')) return true
  const attr = typeof root.getAttribute === 'function' ? root.getAttribute('data-theme') : null
  if (attr === 'dark') return true
  if (typeof window !== 'undefined' && typeof window.matchMedia === 'function') {
    try {
      return window.matchMedia('(prefers-color-scheme: dark)').matches
    } catch {
      return false
    }
  }
  return false
}

/** One CSS custom property off the document root, or a fallback. */
function readThemeVar(name, fallback) {
  if (typeof window === 'undefined' || typeof document === 'undefined') return fallback
  try {
    const value = window.getComputedStyle(document.documentElement).getPropertyValue(name)
    const text = typeof value === 'string' ? value.trim() : ''
    return text.length > 0 ? text : fallback
  } catch {
    return fallback
  }
}

/** `accent-light`, resolved against the DSH shell's own design tokens. */
function resolveAccentLight() {
  return {
    ...ACCENT_LIGHT_BASE,
    background: readThemeVar('--dsw-alias-bg-base', ACCENT_LIGHT_BASE.background),
    foreground: readThemeVar('--dsw-alias-label-primary', ACCENT_LIGHT_BASE.foreground),
    cursor: readThemeVar('--dsw-alias-label-primary', ACCENT_LIGHT_BASE.cursor),
    black: readThemeVar('--dsw-alias-label-primary', ACCENT_LIGHT_BASE.black),
  }
}

/** The theme object for one name, falling back the way the reference did. */
function getTheme(name) {
  const wanted = name === THEME_DEFAULT_OPTION || name === undefined || name === null || name === ''
    ? (isDarkShell() ? DARK_MODE_DEFAULT_THEME : LIGHT_MODE_DEFAULT_THEME)
    : name
  if (wanted === ACCENT_LIGHT_THEME) return resolveAccentLight()
  return THEMES[wanted] ?? THEMES.midnight
}

/** The name a `default` choice resolves to right now. */
function effectiveThemeName(name) {
  if (name === undefined || name === null || name === '' || name === THEME_DEFAULT_OPTION) {
    return isDarkShell() ? DARK_MODE_DEFAULT_THEME : LIGHT_MODE_DEFAULT_THEME
  }
  return THEMES[name] === undefined ? 'midnight' : name
}

// ===== src/client/vendor.js =====
/**
 * Loading xterm.js at runtime.
 *
 * The bundle cannot `import` anything, so the terminal emulator arrives as three
 * script tags pointed at this plugin's own `/vendor` route (host/vendor.js serves the
 * exact files npm installed). That keeps a 290 KB minified library out of a generated
 * source file while still pinning its version, and it never touches the network.
 *
 * The UMD wrappers differ by package: `xterm.js` copies its exports onto the global,
 * so `window.Terminal` is the class; the addons assign their whole namespace, so
 * `window.FitAddon.FitAddon` is the class. Both shapes are accepted below because a
 * future xterm release could switch either way.
 */

let vendorPromise = null

/** Load xterm and its addons once. Resolves with the constructors. */
function ensureXterm() {
  if (vendorPromise !== null) return vendorPromise
  vendorPromise = (async () => {
    loadStylesheet(VENDOR_PREFIX + '/xterm.css')
    await loadScript(VENDOR_PREFIX + '/xterm.js')
    // The addons are optional extras: a missing search addon must not cost the user
    // a terminal, so each one is loaded on its own and its absence tolerated.
    const optional = async (file) => {
      try {
        await loadScript(VENDOR_PREFIX + file)
        return true
      } catch (error) {
        console.warn(LOG + ' optional addon missing:', messageOf(error))
        return false
      }
    }
    await optional('/addon-fit.js')
    await optional('/addon-search.js')
    await optional('/addon-web-links.js')

    const Terminal = window.Terminal
    if (typeof Terminal !== 'function') throw new Error(t('term.vendorMissing'))
    return {
      Terminal,
      FitAddon: pickClass(window.FitAddon, 'FitAddon'),
      SearchAddon: pickClass(window.SearchAddon, 'SearchAddon'),
      WebLinksAddon: pickClass(window.WebLinksAddon, 'WebLinksAddon'),
    }
  })()
  vendorPromise.catch(() => {
    // A failed load must not be cached as a permanent failure: the user may run
    // `npm install` and reopen the panel.
    vendorPromise = null
  })
  return vendorPromise
}

/** Unwrap `X` or `{X}` into the constructor. */
function pickClass(value, name) {
  if (typeof value === 'function') return value
  if (value !== null && value !== undefined && typeof value[name] === 'function') return value[name]
  return undefined
}

// ===== src/client/terminal.js =====
/**
 * One xterm instance per terminal tab, and the splice that makes a re-attach exact.
 *
 * The host owns the session; this file owns the widget. Because a session outlives
 * the page, attaching is not "start a shell" but "catch up with one":
 *
 *   1. open (or re-open) the session — the host returns the existing record when the
 *      id is already live, so a reload costs nothing;
 *   2. subscribe, and QUEUE live frames instead of writing them;
 *   3. read the replay, which says at which byte offset it ends;
 *   4. write the replay, then write the queued frames with the overlap trimmed.
 *
 * Step 4 is why every frame carries an offset. Without it a re-attach either
 * double-prints the last screenful (write both) or loses whatever arrived during the
 * round trip (write only the replay).
 */

/** tabId → the live widget. */
const terminals = new Map()

/** The font stack, verbatim from the reference's xterm options. */
const TERM_FONT = "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

/** Build the xterm options from the current preferences. */
function termOptions() {
  return {
    allowTransparency: true,
    cursorBlink: pref('cursorBlink', true) === true,
    fontFamily: TERM_FONT,
    fontSize: pref('fontSize', 13),
    scrollback: pref('scrollback', 2000),
    theme: getTheme(pref('themeName', 'default')),
    // A terminal that reports its own size wrongly wraps every line; xterm measures
    // the cell itself, so the only thing to get right is telling the host.
    convertEol: false,
    macOptionIsMeta: true,
    rightClickSelectsWord: false,
  }
}

/** The widget for one tab, or undefined. */
function terminalOf(tabId) {
  return terminals.get(tabId)
}

/** The widget showing one session, or undefined. */
function terminalOfSession(sessionId) {
  for (const entry of terminals.values()) {
    if (entry.sessionId === sessionId) return entry
  }
  return undefined
}

/**
 * Create the DOM for one terminal pane. Called once per tab; the returned element is
 * kept across repaints.
 */
function buildTerminalPane(tab) {
  const host = el('div', { class: 'dsh-ot-term' })
  const overlay = el('div', { class: 'dsh-ot-term-overlay' }, el('div', {}, t('term.starting')))
  const pane = el('div', { class: 'dsh-ot-pane', 'data-tab': tab.id }, host, overlay)
  const entry = {
    tabId: tab.id,
    sessionId: tab.sessionId,
    pane,
    host,
    overlay,
    term: undefined,
    fit: undefined,
    search: undefined,
    observer: undefined,
    queue: [],
    attaching: true,
    nextOffset: 0,
    text: '',
    disposed: false,
  }
  terminals.set(tab.id, entry)
  void attachTerminal(entry, tab)
  return pane
}

/** Show or hide the pane overlay. */
function setOverlay(entry, ...children) {
  if (children.length === 0 || children[0] === undefined) {
    setData(entry.overlay, 'hidden', true)
    return
  }
  delete entry.overlay.dataset.hidden
  fill(entry.overlay, ...children)
}

/** Bring one terminal up: mount xterm, open the session, splice the replay. */
async function attachTerminal(entry, tab) {
  let vendor
  try {
    vendor = await ensureXterm()
  } catch (error) {
    setOverlay(entry, el('div', {}, t('term.vendorMissing')), el('div', { class: 'dsh-ot-mono' }, messageOf(error)))
    return
  }
  if (entry.disposed) return

  const term = new vendor.Terminal(termOptions())
  entry.term = term
  if (vendor.FitAddon !== undefined) {
    entry.fit = new vendor.FitAddon()
    term.loadAddon(entry.fit)
  }
  if (vendor.SearchAddon !== undefined) {
    entry.search = new vendor.SearchAddon()
    term.loadAddon(entry.search)
  }
  if (vendor.WebLinksAddon !== undefined) {
    term.loadAddon(new vendor.WebLinksAddon((event, uri) => {
      // Open in a new tab, and never in this one: navigating the DSH page away would
      // kill every terminal in it.
      window.open(uri, '_blank', 'noopener,noreferrer')
    }))
  }
  term.open(entry.host)
  fitTerminal(entry)

  term.onData((data) => {
    if (entry.sessionId === undefined) return
    sendInput(entry.sessionId, data)
  })
  term.onBinary((data) => {
    if (entry.sessionId === undefined) return
    sendInput(entry.sessionId, data)
  })
  term.onSelectionChange(() => {
    if (pref('copyOnSelect', false) !== true) return
    const selection = term.getSelection()
    if (typeof selection === 'string' && selection.length > 0) void copyText(selection)
  })
  // The right-click menu is the panel's own, so the browser's must not also appear.
  entry.host.addEventListener('contextmenu', (event) => {
    event.preventDefault()
    openTerminalMenu(entry, event.clientX, event.clientY)
  })

  if (typeof ResizeObserver === 'function') {
    entry.observer = new ResizeObserver(() => fitTerminal(entry))
    entry.observer.observe(entry.host)
  }

  await openSessionFor(entry, tab)
}

/**
 * Open the host session for one tab and splice its replay.
 *
 * Also used by the reconnect button, so it tolerates being called again on a tab
 * whose session has ended.
 */
async function openSessionFor(entry, tab) {
  const term = entry.term
  if (term === undefined) return
  entry.attaching = true
  entry.queue = []
  setOverlay(entry, el('div', {}, t('term.starting')))
  try {
    const record = await apiPost('/terminal/open', {
      sessionId: entry.sessionId,
      serverId: tab.serverId,
      cols: term.cols,
      rows: term.rows,
      cwd: tab.cwd,
      initialCommand: tab.initialCommand,
    })
    mergeSession(record)
    // Subscribe before the replay so nothing printed in between is lost; the frames
    // land in `entry.queue` while `attaching` is true.
    await pushSubscriptions()
    const replay = await apiGet('/session/replay', { sessionId: entry.sessionId })
    if (entry.disposed) return
    entry.nextOffset = typeof replay.offset === 'number' ? replay.offset : 0
    if (typeof replay.data === 'string' && replay.data.length > 0) {
      term.write(bytesOfBase64(replay.data))
    }
    entry.attaching = false
    const queued = entry.queue
    entry.queue = []
    for (const frame of queued) writeFrame(entry, frame)
    setOverlay(entry)
    term.focus()
    fitTerminal(entry)
    // A tab restored from the workspace snapshot has no initial command to run a
    // second time.
    if (tab.initialCommand !== undefined) delete tab.initialCommand
  } catch (error) {
    entry.attaching = false
    setOverlay(entry,
      el('div', {}, friendlyError(error)),
      hostKeyPromptFor(error, tab.serverId) ??
        button({ label: t('term.reconnect'), variant: 'primary', onClick: () => void openSessionFor(entry, tab) }))
  }
}

/** The "review the fingerprint" button an unknown host key deserves. */
function hostKeyPromptFor(error, serverId) {
  if (codeOf(error) !== 'host_key') return undefined
  const detail = { ...error, serverId }
  return button({
    label: t('hostkey.title'),
    variant: 'primary',
    onClick: () => openHostKeyDialog(detail),
  })
}

/** Write one output frame, trimming whatever the replay already showed. */
function writeFrame(entry, frame) {
  const term = entry.term
  if (term === undefined) return
  const bytes = bytesOfBase64(frame.data)
  const offset = typeof frame.offset === 'number' ? frame.offset : entry.nextOffset
  const end = offset + bytes.length
  if (end <= entry.nextOffset) return
  const slice = offset < entry.nextOffset ? bytes.subarray(entry.nextOffset - offset) : bytes
  entry.nextOffset = end
  term.write(slice)
  rememberText(entry, slice)
}

/**
 * Keep a plain-text tail of the session for the AI bar.
 *
 * The host has its own copy and the AI routes use THAT; this one exists so the bar
 * can show what it is about to send without a round trip.
 */
function rememberText(entry, bytes) {
  try {
    entry.text += new TextDecoder('utf-8', { fatal: false }).decode(bytes)
  } catch {
    return
  }
  if (entry.text.length > CLIENT_SCROLLBACK_CHARS) {
    entry.text = entry.text.slice(-CLIENT_SCROLLBACK_CHARS)
  }
}

/** Deliver one live output frame to whichever terminal owns it. */
function handleOutput(sessionId, data, frame) {
  const entry = terminalOfSession(sessionId)
  if (entry === undefined) return
  const record = frame ?? { data, offset: undefined }
  if (entry.attaching) {
    // Bounded: a session that floods while a panel is attaching must not grow the
    // queue without limit. Past the cap the oldest frames go, which the offset
    // splice then reports as a gap rather than silently mis-rendering.
    if (entry.queue.length > 400) entry.queue.shift()
    entry.queue.push(record)
    return
  }
  writeFrame(entry, record)
}

/** Re-measure and tell the host the new size. */
function fitTerminal(entry) {
  const term = entry.term
  if (term === undefined || entry.disposed) return
  if (entry.host.clientWidth <= 0 || entry.host.clientHeight <= 0) return
  if (entry.fit !== undefined) {
    try {
      entry.fit.fit()
    } catch { /* a hidden pane */ }
  }
  const cols = Math.max(2, term.cols || 80)
  const rows = Math.max(1, term.rows || 24)
  if (entry.lastCols === cols && entry.lastRows === rows) return
  entry.lastCols = cols
  entry.lastRows = rows
  if (entry.sessionId === undefined) return
  // The reference never reported a resize at all — its PTY stayed at the 80×24 it was
  // created with, so every resized window wrapped at the wrong column.
  sendResize(entry.sessionId, cols, rows)
}

/** Apply changed preferences (font, theme, scrollback) to every live terminal. */
function restyleTerminals() {
  const options = termOptions()
  for (const entry of terminals.values()) {
    const term = entry.term
    if (term === undefined) continue
    term.options.theme = options.theme
    term.options.fontSize = options.fontSize
    term.options.cursorBlink = options.cursorBlink
    term.options.scrollback = options.scrollback
    entry.host.style.background = options.theme.background ?? ''
    fitTerminal(entry)
  }
}

/** Focus the terminal of the active tab, if it has one. */
function focusTerminal(tabId) {
  const entry = terminals.get(tabId)
  if (entry === undefined || entry.term === undefined) return
  fitTerminal(entry)
  try {
    entry.term.focus()
  } catch { /* not attached yet */ }
}

/** Paste text into a session as if typed (used by the command list and the AI bar). */
function typeIntoTerminal(tabId, text) {
  const entry = terminals.get(tabId)
  if (entry === undefined || entry.sessionId === undefined) return false
  sendInput(entry.sessionId, text)
  if (entry.term !== undefined) {
    try {
      entry.term.focus()
    } catch { /* not attached */ }
  }
  return true
}

/** The plain-text tail one tab has on screen. */
function terminalText(tabId) {
  const entry = terminals.get(tabId)
  return entry === undefined ? '' : entry.text
}

/**
 * Find text in the scrollback.
 *
 * A prompt rather than an always-visible find bar: `Ctrl+F` belongs to the shell
 * running inside the terminal (it is forward-char in emacs mode, and a search in
 * less), so the panel must not take it. Enter in the prompt jumps to the first hit and
 * the menu item can be used again for the next one.
 */
async function findInTerminal(entry) {
  if (entry.search === undefined) {
    toast(t('term.searchPlaceholder'), 'warning')
    return
  }
  const term = await promptDialog({
    title: t('term.searchPlaceholder'),
    label: t('term.searchPlaceholder'),
    value: entry.lastSearch ?? '',
  })
  if (term === undefined) return
  entry.lastSearch = term
  try {
    const found = entry.search.findNext(term, { incremental: false, caseSensitive: false })
    if (found === false) toast(t('sftp.searchEmpty'), 'warning')
  } catch (error) {
    toastError(error)
  }
}

/** Tear one terminal down (its tab was closed). */
function disposeTerminal(tabId) {
  const entry = terminals.get(tabId)
  if (entry === undefined) return
  entry.disposed = true
  terminals.delete(tabId)
  if (entry.observer !== undefined) {
    try {
      entry.observer.disconnect()
    } catch { /* already gone */ }
  }
  if (entry.term !== undefined) {
    try {
      entry.term.dispose()
    } catch { /* already disposed */ }
  }
  if (entry.sessionId !== undefined) dropInputQueue(entry.sessionId)
  try {
    entry.pane.remove()
  } catch { /* already detached */ }
}

/** Note a dropped-output episode on the screen. */
function noteOverflow(sessionId) {
  const entry = terminalOfSession(sessionId)
  if (entry === undefined || entry.term === undefined) return
  entry.term.write('\r\n\u001b[33m' + t('term.overflow') + '\u001b[0m\r\n')
}

/** Reflect a session record change in the pane (exit code, error, reconnect). */
function refreshTerminalStatus(record) {
  const entry = terminalOfSession(record.sessionId)
  if (entry === undefined) return
  const tab = model.tabs.find((row) => row.sessionId === record.sessionId)
  if (record.status === 'running') {
    if (!entry.attaching) setOverlay(entry)
    return
  }
  if (record.status === 'closed') {
    setOverlay(entry,
      el('div', {}, record.exitCode === null || record.exitCode === undefined
        ? t('term.closedNoCode')
        : t('term.closed', { code: record.exitCode })),
      button({
        label: t('term.reconnect'),
        variant: 'primary',
        onClick: () => {
          if (tab === undefined) return
          // A finished session id cannot be revived, so the tab gets a new one.
          const next = newId('sess')
          tab.sessionId = next
          entry.sessionId = next
          entry.nextOffset = 0
          if (entry.term !== undefined) entry.term.reset()
          void openSessionFor(entry, tab)
        },
      }))
    return
  }
  if (record.status === 'error') {
    setOverlay(entry, el('div', {}, t('term.error', { message: record.error ?? '' })))
  }
}

// ===== src/client/editor.js =====
/**
 * The remote file editor: one pane per editor tab, opened from the SFTP tree.
 *
 * The reference embedded CodeMirror 6 with six language modes. That is a second
 * vendored library and a per-language module graph for a feature whose job is
 * "change a line in nginx.conf and save it", so this is a textarea with a line
 * gutter, tab-inserts-a-tab, and Ctrl/Cmd+S — the same interaction contract
 * (`save-requested`, dirty tracking, a size cap) without the payload. Syntax
 * highlighting is the one thing knowingly given up, and the README says so.
 *
 * Like the terminal panes, an editor pane is built once and kept across repaints: a
 * textarea rebuilt on every render would lose the caret on every keystroke.
 */

/** tabId → the live editor. */
const editors = new Map()

/** Build the DOM for one editor tab. */
function buildEditorPane(tab) {
  const gutter = el('div', { class: 'dsh-ot-editor-gutter' }, '1')
  const area = el('textarea', {
    class: 'dsh-ot-editor-area',
    spellcheck: 'false',
    wrap: 'off',
    placeholder: t('editor.loading'),
  })
  const status = el('div', { class: 'dsh-ot-editor-foot' })
  const head = el('div', { class: 'dsh-ot-editor-head' })
  const pane = el('div', { class: 'dsh-ot-pane', 'data-tab': tab.id },
    el('div', { class: 'dsh-ot-editor' },
      head,
      el('div', { class: 'dsh-ot-editor-body' }, gutter, area),
      status))

  const entry = { tabId: tab.id, pane, head, area, gutter, status, disposed: false }
  editors.set(tab.id, entry)

  area.addEventListener('input', () => {
    tab.content = area.value
    tab.dirty = tab.content !== tab.original
    renderEditorChrome(tab)
    emit()
  })
  area.addEventListener('keydown', (event) => {
    if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === 's') {
      event.preventDefault()
      void saveEditor(tab)
      return
    }
    if (event.key === 'Tab') {
      // A code editor inserts a tab; letting the browser move focus out of a file
      // you are editing is never what was meant.
      event.preventDefault()
      insertAtCaret(area, '\t')
      tab.content = area.value
      tab.dirty = tab.content !== tab.original
      renderEditorChrome(tab)
    }
  })
  area.addEventListener('scroll', () => {
    entry.gutter.scrollTop = area.scrollTop
  })
  area.addEventListener('click', () => renderEditorChrome(tab))
  area.addEventListener('keyup', () => renderEditorChrome(tab))

  void loadEditor(tab)
  return pane
}

/** Insert text at the caret, keeping the caret after it. */
function insertAtCaret(area, text) {
  const start = area.selectionStart
  const end = area.selectionEnd
  area.value = area.value.slice(0, start) + text + area.value.slice(end)
  area.selectionStart = start + text.length
  area.selectionEnd = area.selectionStart
}

/** Read one remote file into its tab. */
async function loadEditor(tab) {
  const entry = editors.get(tab.id)
  if (entry === undefined) return
  tab.loading = true
  renderEditorChrome(tab)
  try {
    const value = await apiGet('/sftp/read', { serverId: tab.serverId, path: tab.filePath })
    tab.binary = value.binary === true
    tab.content = value.binary === true ? '' : value.content
    tab.original = tab.content
    tab.dirty = false
    tab.mode = value.mode
    tab.permissions = value.permissions
    tab.size = value.size
    entry.area.value = tab.content
    entry.area.readOnly = tab.binary === true
    entry.area.placeholder = tab.binary === true ? t('editor.binary') : ''
  } catch (error) {
    toast(t('editor.openFailed', { message: friendlyError(error) }), 'error')
    entry.area.value = ''
    entry.area.placeholder = friendlyError(error)
  } finally {
    tab.loading = false
    renderEditorChrome(tab)
    emit()
  }
}

/** Write one tab back to the server. */
async function saveEditor(tab) {
  const entry = editors.get(tab.id)
  if (entry === undefined || tab.loading === true || tab.saving === true) return
  if (tab.binary === true) return
  tab.saving = true
  renderEditorChrome(tab)
  try {
    const value = await apiPost('/sftp/write', { serverId: tab.serverId, path: tab.filePath, content: tab.content })
    tab.original = tab.content
    tab.dirty = false
    tab.size = value.size
    toast(t('editor.saved'), 'success')
  } catch (error) {
    toast(t('editor.saveFailed', { message: friendlyError(error) }), 'error')
  } finally {
    tab.saving = false
    renderEditorChrome(tab)
    emit()
  }
}

/** Re-read one tab, warning when that would drop edits. */
async function refreshEditor(tab) {
  if (tab.dirty === true) {
    const go = await confirmDialog({
      title: t('editor.refresh'),
      message: t('editor.discardConfirm', { name: tab.name }),
      danger: true,
    })
    if (!go) return
  }
  await loadEditor(tab)
  toast(t('editor.refresh'), 'success')
}

/** Repaint the head, gutter and status line of one editor. */
function renderEditorChrome(tab) {
  const entry = editors.get(tab.id)
  if (entry === undefined) return
  fill(entry.head,
    el('div', { class: 'dsh-ot-editor-path', title: tab.filePath }, tab.filePath),
    el('div', { class: 'dsh-ot-toolbar-group' },
      tab.dirty === true ? tag(t('editor.dirty'), 'warning') : undefined,
      tab.permissions === undefined ? undefined : tag(tab.permissions, undefined, { title: t('sftp.context.chmod') }),
      button({ label: t('editor.refresh'), disabled: tab.loading === true || tab.saving === true, onClick: () => void refreshEditor(tab) }),
      button({
        label: t('editor.save'),
        variant: 'primary',
        disabled: tab.loading === true || tab.saving === true || tab.dirty !== true || tab.binary === true,
        onClick: () => void saveEditor(tab),
      })))

  const lines = entry.area.value.split('\n')
  // The gutter is a text column, not one element per line: a 20 000-line file would
  // otherwise be 20 000 nodes to repaint on every keystroke.
  const numbers = []
  for (let index = 1; index <= Math.max(1, lines.length); index += 1) numbers.push(index)
  fill(entry.gutter, numbers.join('\n'))
  entry.gutter.scrollTop = entry.area.scrollTop

  const caret = entry.area.selectionStart ?? 0
  const before = entry.area.value.slice(0, caret).split('\n')
  fill(entry.status,
    el('span', {}, t('editor.lineColumn', { line: before.length, column: (before[before.length - 1] ?? '').length + 1 })),
    el('span', {}, formatBytes(new TextEncoder().encode(entry.area.value).length)),
    tab.loading === true ? el('span', {}, t('editor.loading')) : undefined)
}

/** Tear one editor down. */
function disposeEditor(tabId) {
  const entry = editors.get(tabId)
  if (entry === undefined) return
  entry.disposed = true
  editors.delete(tabId)
  try {
    entry.pane.remove()
  } catch { /* already detached */ }
}

/** Whether any editor tab of one server has unsaved work. */
function hasDirtyEditors(serverId) {
  return model.tabs.some((tab) => tab.kind === 'editor' && tab.serverId === serverId && tab.dirty === true)
}

// ===== src/client/sftp.js =====
/**
 * The SFTP pane: a lazy tree in the sidebar, exactly the shape the reference had.
 *
 * Worth knowing what the reference's SFTP browser was NOT, because this is a faithful
 * port of it rather than of some ideal file manager: it has no breadcrumb, no
 * detail-view columns, no multi-select, no sort control and no hidden-file toggle.
 * One `el-tree`, one search box, one context menu. The additions here are the ones a
 * browser forces or clearly deserves: drag-and-drop upload from the desktop, a
 * folder download (as a tar, since a page cannot write a tree), chmod (the reference
 * fetched the mode and never showed it), and the workspace-scoped recursive
 * transfers that replace its native file dialogs.
 */

/** The tree's per-path children cache and expansion set live in `model.sftp`. */
function sftpState() {
  return model.sftp
}

/** Reset the pane onto one server. */
function openSftpFor(serverId, path) {
  const state = sftpState()
  if (state.serverId !== serverId) {
    state.serverId = serverId
    state.children = {}
    state.expanded = {}
    state.error = ''
    state.search = { keyword: '', loading: false, items: [], truncated: false, active: false }
    state.path = typeof path === 'string' ? path : ''
  }
  void loadSftpRoot(path)
}

/** Close the pane. */
function closeSftp() {
  const state = sftpState()
  state.serverId = ''
  state.children = {}
  state.expanded = {}
  state.path = ''
  emit()
}

/** Load the starting directory (the login directory unless one was asked for). */
async function loadSftpRoot(path) {
  const state = sftpState()
  if (state.serverId.length === 0) return
  state.loading = true
  state.error = ''
  emit()
  try {
    let target = path
    if (typeof target !== 'string' || target.length === 0) target = state.path
    if (typeof target !== 'string' || target.length === 0) {
      const home = await apiGet('/sftp/home', { serverId: state.serverId })
      target = home.path
    }
    await expandTo(target)
    state.path = target
  } catch (error) {
    state.error = friendlyError(error)
  } finally {
    state.loading = false
    emit()
  }
}

/** Read one directory into the cache. */
async function loadDirectory(path, force) {
  const state = sftpState()
  if (state.children[path] !== undefined && force !== true) return state.children[path]
  const value = await apiGet('/sftp/list', { serverId: state.serverId, path })
  state.children[value.path] = { entries: value.entries, truncated: value.truncated, parent: value.parent }
  return state.children[value.path]
}

/** Expand every ancestor of a path and select it. */
async function expandTo(path) {
  const state = sftpState()
  const clean = String(path ?? '/').replace(/\\/g, '/')
  const parts = clean.split('/').filter((part) => part.length > 0)
  const chain = ['/']
  let current = ''
  for (const part of parts) {
    current += '/' + part
    chain.push(current)
  }
  for (const dir of chain) {
    try {
      await loadDirectory(dir)
      state.expanded[dir] = true
    } catch (error) {
      // A path that no longer exists must not wedge the tree at a blank pane.
      state.error = friendlyError(error)
      break
    }
  }
  state.path = clean.length === 0 ? '/' : clean
}

/** Toggle one directory. */
async function toggleDirectory(entry) {
  const state = sftpState()
  if (state.expanded[entry.path] === true) {
    delete state.expanded[entry.path]
    emit()
    return
  }
  state.expanded[entry.path] = true
  state.path = entry.path
  emit()
  try {
    await loadDirectory(entry.path)
  } catch (error) {
    toastError(error)
  }
  emit()
}

/** Re-read one directory (and the tree below it stays collapsed-as-is). */
async function refreshDirectory(path) {
  try {
    await loadDirectory(path, true)
  } catch (error) {
    toastError(error)
  }
  emit()
}

/** The directory a context action applies to. */
function contextDirectory(entry) {
  if (entry === undefined) return sftpState().path
  return entry.isDirectory ? entry.path : parentPath(entry.path)
}

// -------------------------------------------------------------------- search
/** Run the remote search (Enter, like the reference). */
async function runSearch(keyword) {
  const state = sftpState()
  const term = String(keyword ?? '').trim()
  state.search.keyword = term
  if (term.length === 0) {
    state.search = { keyword: '', loading: false, items: [], truncated: false, active: false }
    emit()
    return
  }
  state.search.active = true
  state.search.loading = true
  emit()
  try {
    const value = await apiGet('/sftp/search', { serverId: state.serverId, path: '/', keyword: term })
    state.search.items = value.items
    state.search.truncated = value.truncated === true
  } catch (error) {
    state.search.items = []
    state.search.truncated = false
    toastError(error)
  } finally {
    state.search.loading = false
    emit()
  }
}

/** Jump to one search hit. */
async function openSearchResult(item) {
  const state = sftpState()
  state.search = { keyword: '', loading: false, items: [], truncated: false, active: false }
  if (item.isDirectory) {
    await expandTo(item.path)
    emit()
    return
  }
  await expandTo(parentPath(item.path))
  emit()
  openRemoteFile(state.serverId, item.path)
}

// ------------------------------------------------------------------ actions
/** Create a directory under `dir`. */
async function createDirectory(dir) {
  const name = await promptDialog({ title: t('sftp.createDirectory'), label: t('sftp.promptName') })
  if (name === undefined) return
  try {
    await apiPost('/sftp/mkdir', { serverId: sftpState().serverId, path: joinPath(dir, name) })
    await refreshDirectory(dir)
    toast(t('sftp.createDirectorySuccess'), 'success')
  } catch (error) {
    toastError(error)
  }
}

/** Create an empty file under `dir`, then open it in the editor. */
async function createFile(dir) {
  const name = await promptDialog({ title: t('sftp.createFile'), label: t('sftp.promptName') })
  if (name === undefined) return
  const path = joinPath(dir, name)
  try {
    await apiPost('/sftp/create-file', { serverId: sftpState().serverId, path })
    await refreshDirectory(dir)
    toast(t('sftp.createFileSuccess'), 'success')
    openRemoteFile(sftpState().serverId, path)
  } catch (error) {
    toastError(error)
  }
}

/** Rename one entry. */
async function renameEntry(entry) {
  const name = await promptDialog({ title: t('sftp.rename'), label: t('sftp.promptName'), value: entry.name })
  if (name === undefined || name === entry.name) return
  const dir = parentPath(entry.path)
  try {
    const value = await apiPost('/sftp/rename', { serverId: sftpState().serverId, from: entry.path, to: joinPath(dir, name) })
    renamePathInTabs(value.from, value.to, entry.isDirectory)
    await refreshDirectory(dir)
    toast(t('sftp.renameSuccess'), 'success')
  } catch (error) {
    toastError(error)
  }
}

/** Delete one entry, with the reference's two confirmation texts. */
async function deleteEntry(entry) {
  const go = await confirmDialog({
    title: t('sftp.deleteConfirmTitle'),
    message: entry.isDirectory
      ? t('sftp.deleteDirectoryConfirm', { name: entry.name })
      : t('sftp.deleteFileConfirm', { name: entry.name }),
    danger: true,
    confirmLabel: t('main.delete'),
  })
  if (!go) return
  const dir = parentPath(entry.path)
  try {
    await apiPost('/sftp/delete', { serverId: sftpState().serverId, path: entry.path })
    closeTabsUnder(entry.path, entry.isDirectory)
    await refreshDirectory(dir)
    toast(t('sftp.deleteSuccess'), 'success')
  } catch (error) {
    toastError(error)
  }
}

/** Change one entry's mode. */
async function chmodEntry(entry) {
  const value = await promptDialog({
    title: t('sftp.chmodTitle', { name: entry.name }),
    label: t('sftp.chmodMode'),
    value: (entry.mode ?? 0o644).toString(8).padStart(3, '0'),
    hint: entry.permissions,
  })
  if (value === undefined) return
  try {
    const result = await apiPost('/sftp/chmod', { serverId: sftpState().serverId, path: entry.path, mode: value })
    await refreshDirectory(parentPath(entry.path))
    toast(t('sftp.chmodSuccess', { mode: result.permissions }), 'success')
  } catch (error) {
    toastError(error)
  }
}

/** Add one directory to the favourites. */
async function favoriteDirectory(path) {
  const state = sftpState()
  const current = favoriteDirsOf(state.serverId)
  if (current.includes(path)) {
    toast(t('main.directoryAlreadyFavorited'), 'warning')
    return
  }
  try {
    await apiPost('/favorites', { serverId: state.serverId, paths: [...current, path] })
    await loadState()
    toast(t('sftp.favoriteAdded'), 'success')
  } catch (error) {
    toastError(error)
  }
}

/** Drop one favourite. */
async function unfavoriteDirectory(serverId, path) {
  try {
    await apiPost('/favorites', { serverId, paths: favoriteDirsOf(serverId).filter((row) => row !== path) })
    await loadState()
  } catch (error) {
    toastError(error)
  }
}

/** Upload a FileList into one remote directory, one request per file. */
async function uploadFiles(dir, files) {
  const state = sftpState()
  if (files.length === 0) return
  let done = 0
  for (const file of files) {
    try {
      // `webkitRelativePath` is set when a whole folder was picked or dropped, and it
      // is what lets the host recreate the tree instead of flattening it.
      const name = typeof file.webkitRelativePath === 'string' && file.webkitRelativePath.length > 0
        ? file.webkitRelativePath
        : file.name
      await uploadFile(state.serverId, dir, file, name)
      done += 1
    } catch (error) {
      toast(t('sftp.uploadFailed', { message: friendlyError(error) }), 'error')
    }
  }
  await refreshDirectory(dir)
  if (done > 0) toast(t('sftp.uploadDone', { count: done }), 'success')
}

/** Open the browser's file picker and upload into `dir`. */
function pickAndUpload(dir, directory) {
  const picker = el('input', { type: 'file', multiple: true, style: { display: 'none' } })
  if (directory === true) {
    // Chromium-only; on a browser without it the picker simply stays file-wise.
    picker.setAttribute('webkitdirectory', '')
    picker.setAttribute('directory', '')
  }
  picker.addEventListener('change', () => {
    const files = [...(picker.files ?? [])]
    picker.remove()
    void uploadFiles(dir, files)
  })
  document.body.append(picker)
  picker.click()
}

/** Start a workspace-scoped transfer (the replacement for the native dialogs). */
function openWorkspaceTransfer(kind, remotePath) {
  const state = sftpState()
  if (model.workspaces.length === 0) {
    toast(t('sftp.workspaceEmpty'), 'warning')
    return
  }
  let workspaceId = model.workspaces[0].id
  let relative = ''
  openDialog({
    title: kind === 'upload' ? t('sftp.context.uploadFromWorkspace') : t('sftp.context.downloadToWorkspace'),
    size: 'small',
    build: () => [
      el('div', { class: 'dsh-ot-field-hint' }, remotePath),
      field(t('sftp.selectWorkspace'),
        select(model.workspaces.map((row) => ({ id: row.id, label: row.title + ' — ' + row.path })), workspaceId, (value) => {
          workspaceId = value
        })),
      field(t('sftp.workspaceRelative'), input({ value: '', onInput: (event) => {
        relative = event.target.value
      } })),
    ],
    footer: (close) => [
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      button({
        label: t('main.confirm'),
        variant: 'primary',
        onClick: async () => {
          close(undefined)
          try {
            if (kind === 'upload') {
              await apiPost('/transfer/upload-workspace', {
                serverId: state.serverId,
                workspaceId,
                relative,
                remoteDir: remotePath,
              })
            } else {
              await apiPost('/transfer/download-workspace', {
                serverId: state.serverId,
                path: remotePath,
                workspaceId,
                relative,
              })
            }
            model.drawerOpen = true
            toast(t('sftp.transferStarted'), 'success')
            emit()
          } catch (error) {
            toastError(error)
          }
        },
      }),
    ],
  })
}

// ------------------------------------------------------------------ render
/**
 * The search box, built once and reused.
 *
 * A repaint is triggered by anything that changes the model — a transfer frame every
 * 250 ms, a session status — and rebuilding this input would throw away whatever the
 * user was halfway through typing. So the element is kept and only re-parented.
 */
let searchInputEl = null

/** The persistent search input. */
function sftpSearchInput() {
  if (searchInputEl !== null) return searchInputEl
  searchInputEl = input({
    placeholder: t('sftp.searchPlaceholder'),
    onInput: (event) => {
      // Remembered without a repaint: Enter is what starts the search, exactly as the
      // reference did it.
      sftpState().search.keyword = event.target.value
    },
    onKeydown: (event) => {
      if (event.key === 'Enter') void runSearch(event.target.value)
      if (event.key === 'Escape') {
        event.target.value = ''
        void runSearch('')
      }
    },
  })
  return searchInputEl
}

/** The whole pane: head, path, search, then the tree or the search results. */
function renderSftpPane(host) {
  const state = sftpState()
  const server = serverById(state.serverId)
  if (server === undefined) {
    fill(host)
    return
  }
  const status = connectionState(state.serverId)
  const head = el('div', { class: 'dsh-ot-sftp-head' },
    el('span', { class: 'dsh-ot-sftp-title' }, t('sftp.title')),
    tag(t('sftp.status.' + (status === 'connected' ? 'connected' : status === 'connecting' ? 'connecting' : status === 'error' ? 'error' : 'disconnected')),
      status === 'connected' ? 'success' : status === 'error' ? 'danger' : undefined),
    el('span', { style: { flex: '1' } }),
    iconButton('arrowUp', { variant: 'ghost', title: t('sftp.goUp'), onClick: () => void expandThenEmit(parentPath(state.path)) }),
    iconButton('home', { variant: 'ghost', title: t('sftp.goHome'), onClick: () => void loadSftpRoot('') }),
    iconButton('upload', { variant: 'ghost', title: t('sftp.uploadFile'), onClick: () => pickAndUpload(state.path, false) }),
    iconButton('refresh', { variant: 'ghost', title: t('sftp.refreshDirectory'), onClick: () => void refreshDirectory(state.path) }))

  const body = state.search.active ? renderSearchResults() : renderTree()
  fill(host,
    head,
    el('div', { class: 'dsh-ot-sftp-path', title: state.path }, state.path),
    el('div', { class: 'dsh-ot-sftp-search' }, sftpSearchInput()),
    body)
  wireDrop(host, () => state.path)
}

/** Expand to a path and repaint. */
async function expandThenEmit(path) {
  try {
    await expandTo(path)
  } catch (error) {
    toastError(error)
  }
  emit()
}

/** The lazy tree, rooted at '/' and pre-expanded down to the current path. */
function renderTree() {
  const state = sftpState()
  const wrap = el('div', { class: 'dsh-ot-sftp-tree' })
  if (state.error.length > 0) wrap.append(el('div', { class: 'dsh-ot-sftp-error' }, state.error))
  const root = { name: '/', path: '/', isDirectory: true }
  wrap.append(...renderNode(root, 0))
  if (state.loading) wrap.append(el('div', { class: 'dsh-ot-sftp-hint' }, t('main.connecting')))
  return wrap
}

/** One row, plus its children when expanded. */
function renderNode(entry, depth) {
  const state = sftpState()
  const expanded = entry.isDirectory && state.expanded[entry.path] === true
  const row = el('div', {
    class: 'dsh-ot-node',
    'data-active': state.path === entry.path ? 'true' : undefined,
    style: { 'padding-left': (6 + depth * 12) + 'px' },
    onClick: () => {
      if (entry.isDirectory) void toggleDirectory(entry)
      else openRemoteFile(state.serverId, entry.path)
    },
    onContextmenu: (event) => {
      event.preventDefault()
      event.stopPropagation()
      openSftpMenu(entry, event.clientX, event.clientY)
    },
  },
  el('span', { class: 'dsh-ot-node-twist' }, entry.isDirectory ? (expanded ? '▾' : '▸') : ''),
  el('span', { class: 'dsh-ot-node-icon', 'data-kind': entry.isDirectory ? 'dir' : 'file' },
    icon(entry.isDirectory ? 'folder' : (entry.isSymlink === true ? 'link' : 'file'), 14)),
  el('span', { class: 'dsh-ot-node-name', title: entry.path }, entry.name),
  entry.isSymlink === true && typeof entry.linkTarget === 'string'
    ? el('span', { class: 'dsh-ot-node-link', title: entry.linkTarget }, t('sftp.linkTo', { target: entry.linkTarget }))
    : undefined,
  entry.isDirectory !== true && typeof entry.size === 'number'
    ? el('span', { class: 'dsh-ot-node-size' }, formatBytes(entry.size))
    : undefined)

  const rows = [row]
  if (!expanded) return rows
  const cached = state.children[entry.path]
  if (cached === undefined) {
    rows.push(el('div', { class: 'dsh-ot-sftp-hint', style: { 'padding-left': (18 + depth * 12) + 'px' } }, t('main.connecting')))
    return rows
  }
  if (cached.truncated === true) {
    rows.push(el('div', { class: 'dsh-ot-sftp-hint' }, t('sftp.truncated', { count: cached.entries.length })))
  }
  for (const child of cached.entries) rows.push(...renderNode(child, depth + 1))
  if (cached.entries.length === 0) {
    rows.push(el('div', { class: 'dsh-ot-sftp-hint', style: { 'padding-left': (18 + depth * 12) + 'px' } }, t('sftp.entryCount', { count: 0 })))
  }
  return rows
}
/** The search-results panel, with the reference's four states. */
function renderSearchResults() {
  const state = sftpState()
  const wrap = el('div', { class: 'dsh-ot-sftp-tree' })
  const meta = state.search.loading
    ? t('sftp.searching')
    : state.search.truncated
      ? t('sftp.searchResultTooMany')
      : t('sftp.searchResultCount', { count: state.search.items.length })
  wrap.append(el('div', { class: 'dsh-ot-sftp-head' },
    el('span', { class: 'dsh-ot-sftp-title' }, t('sftp.searchResults')),
    el('span', { style: { flex: '1' } }),
    el('span', { class: 'dsh-ot-field-hint' }, meta),
    iconButton('close', { variant: 'ghost', title: t('main.close'), onClick: () => void runSearch('') })))
  if (state.search.loading) {
    wrap.append(el('div', { class: 'dsh-ot-sftp-hint' }, t('sftp.searchingRemote')))
    return wrap
  }
  if (state.search.truncated) wrap.append(el('div', { class: 'dsh-ot-sftp-hint' }, t('sftp.searchTooMany')))
  if (state.search.items.length === 0) {
    wrap.append(el('div', { class: 'dsh-ot-sftp-hint' }, t('sftp.searchEmpty')))
    return wrap
  }
  for (const item of state.search.items) {
    wrap.append(el('div', {
      class: 'dsh-ot-search-row',
      onClick: () => void openSearchResult(item),
      onContextmenu: (event) => {
        event.preventDefault()
        openSftpMenu({ ...item, mode: undefined, permissions: undefined }, event.clientX, event.clientY)
      },
    },
    el('div', { class: 'dsh-ot-search-name' },
      el('span', { class: 'dsh-ot-node-icon', 'data-kind': item.isDirectory ? 'dir' : 'file' },
        icon(item.isDirectory ? 'folder' : 'file', 14)),
      el('span', {}, item.name)),
    el('div', { class: 'dsh-ot-search-path', title: item.path }, item.path)))
  }
  return wrap
}

/**
 * The context menu: the reference's ten items in the reference's order, plus the four
 * this port adds (edit, chmod, and the two workspace transfers).
 */
function openSftpMenu(entry, x, y) {
  const state = sftpState()
  const dir = contextDirectory(entry)
  const isRoot = entry.path === '/'
  const items = []
  if (entry.isDirectory) {
    items.push({ label: t('sftp.context.createDirectory'), icon: 'folder', onClick: () => void createDirectory(entry.path) })
    items.push({ label: t('sftp.context.createFile'), icon: 'file', onClick: () => void createFile(entry.path) })
    items.push({ label: t('sftp.context.upload'), icon: 'upload', onClick: () => pickAndUpload(entry.path, false) })
    items.push({ label: t('sftp.uploadFolder'), icon: 'upload', onClick: () => pickAndUpload(entry.path, true) })
    items.push({ label: t('sftp.context.favoriteDirectory'), icon: 'folder-star', onClick: () => void favoriteDirectory(entry.path) })
    items.push({ label: t('sftp.context.openInTerminal'), icon: 'terminal', onClick: () => openTerminalAt(state.serverId, entry.path) })
    items.push({ separator: true })
    items.push({ label: t('sftp.context.download'), icon: 'download', onClick: () => downloadPath(state.serverId, entry.path, 'tar') })
    items.push({ label: t('sftp.context.uploadFromWorkspace'), icon: 'upload', onClick: () => openWorkspaceTransfer('upload', entry.path) })
    items.push({ label: t('sftp.context.downloadToWorkspace'), icon: 'download', onClick: () => openWorkspaceTransfer('download', entry.path) })
  } else {
    items.push({ label: t('sftp.context.edit'), icon: 'edit', onClick: () => openRemoteFile(state.serverId, entry.path) })
    items.push({ label: t('sftp.context.download'), icon: 'download', onClick: () => downloadPath(state.serverId, entry.path, 'file') })
    items.push({ label: t('sftp.context.downloadToWorkspace'), icon: 'download', onClick: () => openWorkspaceTransfer('download', entry.path) })
  }
  items.push({ separator: true })
  items.push({ label: t('sftp.context.copyPath'), icon: 'copy', onClick: () => void copyText(entry.path, t('sftp.pathCopied')) })
  items.push({ label: t('sftp.context.rename'), icon: 'edit', disabled: isRoot, onClick: () => void renameEntry(entry) })
  items.push({ label: t('sftp.context.chmod'), icon: 'lock', disabled: isRoot, onClick: () => void chmodEntry(entry) })
  items.push({ label: t('sftp.context.delete'), icon: 'trash', tone: 'danger', disabled: isRoot, onClick: () => void deleteEntry(entry) })
  items.push({ separator: true })
  items.push({ label: t('sftp.context.refresh'), icon: 'refresh', onClick: () => void refreshDirectory(dir) })
  openContextMenu(x, y, items, entry.name === '/' ? t('sftp.serverFile') : entry.name)
}

/**
 * Drag-and-drop upload.
 *
 * `webkitGetAsEntry` walks a dropped FOLDER, which is the only way a page can upload
 * a tree; browsers without it still get the flat file list.
 */
function wireDrop(host, dirOf) {
  host.addEventListener('dragover', (event) => {
    event.preventDefault()
    setData(host, 'drop', true)
  })
  host.addEventListener('dragleave', (event) => {
    if (event.target !== host) return
    delete host.dataset.drop
  })
  host.addEventListener('drop', (event) => {
    event.preventDefault()
    delete host.dataset.drop
    const dir = dirOf()
    void collectDropped(event.dataTransfer).then((files) => uploadFiles(dir, files))
  })
}

/** Flatten a DataTransfer into a list of files with relative paths. */
async function collectDropped(transfer) {
  if (transfer === null || transfer === undefined) return []
  const items = [...(transfer.items ?? [])]
  const entries = items.map((item) => (typeof item.webkitGetAsEntry === 'function' ? item.webkitGetAsEntry() : null))
    .filter((row) => row !== null)
  if (entries.length === 0) return [...(transfer.files ?? [])]
  const files = []
  const walkEntry = async (fsEntry, prefix) => {
    if (fsEntry.isFile) {
      const file = await new Promise((resolvePromise) => fsEntry.file(resolvePromise, () => resolvePromise(null)))
      if (file !== null) {
        // The relative path is carried on the File the same way the folder picker
        // does it, so the host takes one code path for both.
        try {
          Object.defineProperty(file, 'webkitRelativePath', { value: prefix + fsEntry.name, configurable: true })
        } catch { /* a File that refuses the property still uploads flat */ }
        files.push(file)
      }
      return
    }
    if (!fsEntry.isDirectory) return
    const reader = fsEntry.createReader()
    // readEntries returns at most 100 per call, so it has to be drained.
    for (;;) {
      const batch = await new Promise((resolvePromise) => reader.readEntries(resolvePromise, () => resolvePromise([])))
      if (batch.length === 0) break
      for (const child of batch) await walkEntry(child, prefix + fsEntry.name + '/')
    }
  }
  for (const fsEntry of entries) await walkEntry(fsEntry, '')
  return files
}

// ===== src/client/transfers.js =====
/**
 * The transfer drawer: the reference's bottom sheet, with a row per task.
 *
 * Same fields the reference showed (kind · basename, status tag, source → target,
 * current item, file count, bytes, a progress bar, the error). Two additions: a
 * cancel button, because a recursive transfer of the wrong folder used to be
 * unstoppable, and honest progress on a single large file — the reference emitted one
 * event per file, so a 4 GB copy sat at 0% and then jumped to 100%.
 */

/** Localised status text. */
function taskStatusText(status) {
  const known = { pending: 'main.taskStatus.pending', transferring: 'main.taskStatus.transferring', completed: 'main.taskStatus.completed', failed: 'main.taskStatus.failed' }
  return known[status] === undefined ? status : t(known[status])
}

/** Tag tone per status. */
function taskTone(status) {
  if (status === 'completed') return 'success'
  if (status === 'failed') return 'danger'
  if (status === 'transferring') return 'primary'
  return undefined
}

/** Render the drawer into its host element. */
function renderDrawer(host) {
  setData(host, 'hidden', !model.drawerOpen)
  if (!model.drawerOpen) {
    fill(host)
    return
  }
  const body = el('div', { class: 'dsh-ot-drawer-body' })
  if (model.tasks.length === 0) {
    body.append(el('div', { class: 'dsh-ot-empty' }, t('main.noTransferTasks')))
  }
  for (const task of model.tasks) body.append(renderTask(task))
  fill(host,
    el('div', { class: 'dsh-ot-drawer-head' },
      el('span', { class: 'dsh-ot-drawer-title' }, t('main.transferTasks')),
      el('div', { class: 'dsh-ot-toolbar-group' },
        button({ label: t('main.clearFinished'), onClick: () => void clearFinishedTasks() }),
        iconButton('close', {
          variant: 'ghost',
          title: t('main.close'),
          onClick: () => {
            model.drawerOpen = false
            emit()
          },
        }))),
    body)
}

/** One task card. */
function renderTask(task) {
  const running = task.status === 'pending' || task.status === 'transferring'
  return el('div', { class: 'dsh-ot-task' },
    el('div', { class: 'dsh-ot-task-head' },
      el('span', { class: 'dsh-ot-task-name', title: task.source },
        (task.kind === 'upload' ? t('main.taskKindUpload') : t('main.taskKindDownload')) + ' · ' + baseName(task.source)),
      el('div', { class: 'dsh-ot-toolbar-group' },
        tag(taskStatusText(task.status), taskTone(task.status)),
        running
          ? iconButton('close', { variant: 'ghost', title: t('main.cancelTask'), onClick: () => void cancelTask(task.id) })
          : undefined)),
    el('div', { class: 'dsh-ot-task-line' }, task.source + ' → ' + task.target),
    el('div', { class: 'dsh-ot-task-meta' },
      el('span', {}, task.currentItem.length > 0 ? task.currentItem : '-'),
      el('span', {}, t('main.fileCount', { done: task.completedFiles, total: task.totalFiles })),
      el('span', {}, formatBytes(task.bytesTransferred) + ' / ' + formatBytes(task.bytesTotal))),
    progressBar(task.progress, task.status),
    task.error.length > 0 ? el('div', { class: 'dsh-ot-task-error' }, task.error) : undefined)
}

/** Ask the host to stop one transfer. */
async function cancelTask(taskId) {
  try {
    await apiPost('/transfer/cancel', { taskId })
  } catch (error) {
    toastError(error)
  }
}

/** Drop the finished rows. */
async function clearFinishedTasks() {
  try {
    const value = await apiPost('/transfer/clear', {})
    model.tasks = value.tasks
    emit()
  } catch (error) {
    toastError(error)
  }
}

// ===== src/client/servers.js =====
/**
 * The server list and its dialogs: add / edit, the per-row context menu, the delete
 * confirmation, the host-key review, and the ~/.ssh/config import.
 *
 * The dialog is the reference's field for field (name, protocol radio, host, port,
 * user, auth radio, password / key path + passphrase), plus what a browser needs
 * instead of a native file dialog: a "paste the key" textarea, an SSH-agent checkbox,
 * and the import.
 */

/** Blank draft, with the reference's defaults. */
function emptyServerDraft() {
  return {
    id: '',
    name: '',
    protocol: 'ssh',
    host: '',
    port: DEFAULT_PORTS.ssh,
    username: '',
    authType: 'password',
    privateKeyPath: '',
    useAgent: false,
    keepaliveSeconds: 30,
    group: '',
    note: '',
    secrets: {},
  }
}

/** Open the add/edit dialog. */
function openServerDialog(existing) {
  const draft = existing === undefined
    ? emptyServerDraft()
    : { ...emptyServerDraft(), ...existing, secrets: {} }
  const body = el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '12px' } })
  const record = openDialog({
    title: existing === undefined ? t('main.serverDialog.addTitle') : t('main.serverDialog.editTitle'),
    build: () => body,
    footer: (close) => [
      existing === undefined
        ? button({ label: t('main.serverDialog.importSshConfig'), onClick: () => {
          close(undefined)
          void openImportDialog()
        } })
        : undefined,
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      button({ label: t('main.save'), variant: 'primary', onClick: () => void submitServer(draft, existing, close) }),
    ],
  })
  const repaint = () => fill(body, ...serverFields(draft, existing, repaint))
  repaint()
  return record
}

/** The dialog's fields, rebuilt when the protocol or auth type changes. */
function serverFields(draft, existing, repaint) {
  const rows = []
  rows.push(field(t('main.serverDialog.name'), input({
    value: draft.name,
    placeholder: t('main.serverDialog.namePlaceholder'),
    onInput: (event) => {
      draft.name = event.target.value
    },
  })))
  rows.push(field(t('main.serverDialog.protocol'), radioGroup('dsh-ot-protocol', [
    { id: 'ssh', label: t('main.serverDialog.protocolSsh') },
    { id: 'rdp', label: t('main.serverDialog.protocolRdp') },
    { id: 'vnc', label: t('main.serverDialog.protocolVnc') },
  ], draft.protocol, (value) => {
    // Moving the protocol moves the port with it, unless the user typed one.
    if (draft.port === DEFAULT_PORTS[draft.protocol]) draft.port = DEFAULT_PORTS[value]
    draft.protocol = value
    repaint()
  })))
  rows.push(el('div', { class: 'dsh-ot-row' },
    field(t('main.serverDialog.host'), input({
      value: draft.host,
      placeholder: t('main.serverDialog.hostPlaceholder'),
      onInput: (event) => {
        draft.host = event.target.value
      },
    })),
    field(t('main.serverDialog.port'), input({
      type: 'number',
      value: draft.port,
      min: 1,
      max: 65535,
      onInput: (event) => {
        draft.port = Number(event.target.value)
      },
    }))))
  rows.push(el('div', { class: 'dsh-ot-row' },
    field(t('main.serverDialog.username'), input({
      value: draft.username,
      placeholder: t('main.serverDialog.usernamePlaceholder'),
      onInput: (event) => {
        draft.username = event.target.value
      },
    })),
    field(t('main.serverDialog.group'), input({
      value: draft.group,
      onInput: (event) => {
        draft.group = event.target.value
      },
    }))))

  if (draft.protocol === 'ssh') {
    rows.push(field(t('main.serverDialog.authType'), radioGroup('dsh-ot-auth', [
      { id: 'password', label: t('main.serverDialog.authPassword') },
      { id: 'private_key', label: t('main.serverDialog.authPrivateKey') },
    ], draft.authType, (value) => {
      draft.authType = value
      repaint()
    })))
  }

  if (draft.protocol !== 'ssh' || draft.authType === 'password') {
    rows.push(field(t('main.serverDialog.password'), input({
      type: 'password',
      placeholder: t('main.serverDialog.passwordPlaceholder'),
      onInput: (event) => {
        draft.secrets.password = event.target.value
      },
    }), existing !== undefined && existing.hasPassword === true ? t('main.serverDialog.passwordKept') : undefined))
  } else {
    rows.push(field(t('main.serverDialog.privateKeyPath'), input({
      value: draft.privateKeyPath,
      placeholder: t('main.serverDialog.privateKeyPathPlaceholder'),
      onInput: (event) => {
        draft.privateKeyPath = event.target.value
      },
    })))
    rows.push(field(t('main.serverDialog.privateKeyBody'), textarea({
      placeholder: t('main.serverDialog.privateKeyBodyPlaceholder'),
      rows: 4,
      onInput: (event) => {
        draft.secrets.privateKeyBody = event.target.value
      },
    }), existing !== undefined && existing.hasPrivateKeyBody === true ? t('main.serverDialog.privateKeyStored') : undefined))
    rows.push(field(t('main.serverDialog.passphrase'), input({
      type: 'password',
      placeholder: t('main.serverDialog.passphrasePlaceholder'),
      onInput: (event) => {
        draft.secrets.passphrase = event.target.value
      },
    })))
  }

  if (draft.protocol === 'ssh') {
    rows.push(el('div', { class: 'dsh-ot-row' },
      el('div', {}, checkbox(t('main.serverDialog.useAgent'), draft.useAgent === true, (value) => {
        draft.useAgent = value
      })),
      field(t('main.serverDialog.keepalive'), input({
        type: 'number',
        value: draft.keepaliveSeconds,
        min: 0,
        max: 600,
        onInput: (event) => {
          draft.keepaliveSeconds = Number(event.target.value)
        },
      }))))
  }
  rows.push(field(t('main.serverDialog.note'), input({
    value: draft.note,
    onInput: (event) => {
      draft.note = event.target.value
    },
  })))
  return rows
}

/** Validate and save one draft. */
async function submitServer(draft, existing, close) {
  if (draft.name.trim().length === 0 || draft.host.trim().length === 0) {
    toast(t('main.serverDialog.requiredFields'), 'warning')
    return
  }
  if (draft.protocol !== 'vnc' && draft.username.trim().length === 0) {
    toast(t('main.serverDialog.requiredFields'), 'warning')
    return
  }
  const hasStoredPassword = existing !== undefined && existing.hasPassword === true
  const hasStoredKey = existing !== undefined && (existing.hasPrivateKeyBody === true || (existing.privateKeyPath ?? '').length > 0)
  if (draft.protocol === 'ssh' && draft.authType === 'private_key') {
    const keyGiven = (draft.privateKeyPath ?? '').trim().length > 0 ||
      (draft.secrets.privateKeyBody ?? '').trim().length > 0 || hasStoredKey || draft.useAgent === true
    if (!keyGiven) {
      toast(t('main.serverDialog.privateKeyRequired'), 'warning')
      return
    }
  } else if ((draft.secrets.password ?? '').length === 0 && !hasStoredPassword && draft.useAgent !== true) {
    toast(t('main.serverDialog.passwordRequired'), 'warning')
    return
  }
  const server = { ...draft }
  delete server.secrets
  // Only the secrets the user actually typed are sent: an untouched password field
  // must not clear the stored one.
  const secrets = {}
  for (const key of ['password', 'passphrase', 'privateKeyBody']) {
    const value = draft.secrets[key]
    if (typeof value === 'string' && value.length > 0) secrets[key] = value
  }
  try {
    await withBusy(() => apiPost('/servers/save', {
      server,
      secrets: Object.keys(secrets).length > 0 ? secrets : undefined,
      reconnect: existing !== undefined,
    }))
    await loadState()
    close(undefined)
  } catch (error) {
    toastError(error)
  }
}

/** The ~/.ssh/config import dialog: candidates with checkboxes. */
async function openImportDialog() {
  let value
  try {
    value = await withBusy(() => apiPost('/servers/import-ssh-config', {}))
  } catch (error) {
    toastError(error)
    return
  }
  const chosen = new Set(value.hosts.map((row) => row.alias))
  openDialog({
    title: t('main.serverDialog.importTitle'),
    build: () => {
      if (value.hosts.length === 0) return el('div', { class: 'dsh-ot-empty' }, t('main.serverDialog.importEmpty'))
      return [
        el('div', { class: 'dsh-ot-field-hint' }, value.file),
        ...value.hosts.map((row) => el('div', { class: 'dsh-ot-section' },
          checkbox(row.alias, true, (on) => {
            if (on) chosen.add(row.alias)
            else chosen.delete(row.alias)
          }),
          el('div', { class: 'dsh-ot-field-hint' },
            (row.username.length > 0 ? row.username + '@' : '') + row.host + ':' + row.port +
            (row.privateKeyPath.length > 0 ? ' · ' + row.privateKeyPath : '') +
            (row.unsupported.length > 0 ? ' · ' + t('main.serverDialog.importUnsupported', { list: row.unsupported.join(', ') }) : '')))),
      ]
    },
    footer: (close) => [
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      button({
        label: t('main.serverDialog.importSelected', { count: chosen.size }),
        variant: 'primary',
        disabled: value.hosts.length === 0,
        onClick: async () => {
          let count = 0
          for (const row of value.hosts) {
            if (!chosen.has(row.alias)) continue
            try {
              await apiPost('/servers/save', {
                server: {
                  name: row.name,
                  protocol: 'ssh',
                  host: row.host,
                  port: row.port,
                  username: row.username,
                  authType: row.authType,
                  privateKeyPath: row.privateKeyPath,
                  useAgent: row.authType === 'private_key' && row.privateKeyPath.length === 0,
                },
              })
              count += 1
            } catch (error) {
              toastError(error)
            }
          }
          await loadState()
          close(undefined)
          toast(t('main.serverDialog.imported', { count }), 'success')
        },
      }),
    ],
  })
}

/** The host-key review dialog: first contact, or a mismatch. */
function openHostKeyDialog(detail) {
  const mismatch = detail.mismatch === true
  openDialog({
    title: t('hostkey.title'),
    size: 'small',
    build: () => [
      el('div', {}, mismatch
        ? t('hostkey.mismatch', { host: detail.host, port: detail.port })
        : t('hostkey.firstTime', { host: detail.host, port: detail.port })),
      mismatch
        ? el('div', { class: 'dsh-ot-field-hint' }, t('hostkey.pinned') + ': ' + (detail.pinnedFingerprint ?? ''))
        : undefined,
      el('div', { class: 'dsh-ot-mono' }, t('hostkey.incoming') + ': ' + (detail.keyType ?? '') + ' ' + (detail.fingerprint ?? '')),
      mismatch ? el('div', { class: 'dsh-ot-field-hint' }, t('hostkey.forgetHint')) : undefined,
    ],
    footer: (close) => [
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      mismatch
        ? button({
          label: t('hostkey.forget'),
          variant: 'danger',
          onClick: async () => {
            try {
              await apiPost('/connection/host-key/forget', { host: detail.host, port: detail.port })
              await loadState()
              close(undefined)
              toast(t('hostkey.forgotten'), 'success')
            } catch (error) {
              toastError(error)
            }
          },
        })
        : button({
          label: t('hostkey.accept'),
          variant: 'primary',
          onClick: async () => {
            try {
              await apiPost('/connection/host-key/accept', {
                serverId: detail.serverId,
                fingerprint: detail.fingerprint,
                keyType: detail.keyType,
              })
              close(undefined)
              toast(t('hostkey.accepted'), 'success')
              await connectServer(detail.serverId)
            } catch (error) {
              toastError(error)
            }
          },
        }),
    ],
  })
}

// ===== src/client/forward.js =====
/**
 * The forwarding dialog: local port forwards and the SOCKS5 proxy for one server.
 *
 * Same two sections the reference had, same buttons (add rule / save / start / stop /
 * delete, and start / stop for the proxy). Two things are different because they were
 * wrong rather than merely different:
 *
 *  - a listener binds to 127.0.0.1 unless "allow access from other machines" is
 *    ticked. The reference took whatever was in the field, so a `0.0.0.0` left over
 *    from a previous rule silently published a hole into the remote network;
 *  - a running rule shows its live byte counters, which is how you tell a tunnel that
 *    is up from one that is up AND being used.
 */

/** Open the dialog for one server. */
function openForwardingDialog(serverId) {
  const server = serverById(serverId)
  if (server === undefined) return
  // A working copy: nothing is written until Save, exactly like the reference's draft.
  const draft = {
    portForwards: (server.portForwards ?? []).map((row) => ({ ...row })),
    socks5Proxy: { ...(server.socks5Proxy ?? { listenHost: '127.0.0.1', listenPort: 1080, allowPublic: false }) },
  }
  const body = el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '14px' } })
  let unbind = null
  const record = openDialog({
    title: t('main.forwarding.title'),
    size: 'large',
    build: () => body,
    footer: (close) => [
      button({ label: t('main.close'), onClick: () => close(undefined) }),
      button({ label: t('main.forwarding.saveConfig'), variant: 'primary', onClick: () => void saveForwarding(serverId, draft) }),
    ],
    onClose: () => {
      if (unbind !== null) unbind()
    },
  })
  const repaint = () => {
    fill(body, renderForwardingBody(serverId, draft, repaint))
  }
  // The dialog follows the live tunnel state, so a rule started here flips to
  // "running" without a manual refresh.
  unbind = onModel(() => {
    if (record.overlay.isConnected) repaint()
  })
  repaint()
}

/** The two sections. */
function renderForwardingBody(serverId, draft, repaint) {
  const server = serverById(serverId)
  const rows = []
  rows.push(el('div', { class: 'dsh-ot-field-hint' },
    (server?.name ?? '') + ' · ' + (server?.username ?? '') + '@' + (server?.host ?? '') + ':' + (server?.port ?? '')))

  const ruleCards = draft.portForwards.length === 0
    ? [el('div', { class: 'dsh-ot-empty' }, t('main.forwarding.emptyRules'))]
    : draft.portForwards.map((rule) => renderRuleCard(serverId, draft, rule, repaint))
  rows.push(section(t('main.forwarding.portForwardTitle'), [
    button({ label: t('main.forwarding.addRule'), icon: 'plus', onClick: () => {
      draft.portForwards.push({
        id: newId('rule'),
        name: '',
        listenHost: '127.0.0.1',
        listenPort: 3307,
        remoteHost: '127.0.0.1',
        remotePort: 3306,
        allowPublic: false,
        enabled: false,
      })
      repaint()
    } }),
    button({ label: t('main.forwarding.saveRules'), onClick: () => void saveForwarding(serverId, draft) }),
  ], ...ruleCards))

  rows.push(renderSocksSection(serverId, draft, repaint))
  return rows
}

/** One forwarding rule. */
function renderRuleCard(serverId, draft, rule, repaint) {
  const running = isForwardRunning(serverId, rule.id)
  const runtime = model.tunnels.portForwards.find((row) => row.serverId === serverId && row.ruleId === rule.id)
  return el('div', { class: 'dsh-ot-section' },
    el('div', { class: 'dsh-ot-section-head' },
      el('span', { class: 'dsh-ot-section-title' },
        rule.name.length > 0 ? rule.name : t('main.forwarding.portRuleName', { port: rule.listenPort })),
      el('div', { class: 'dsh-ot-toolbar-group' },
        runtime === undefined ? undefined : el('span', { class: 'dsh-ot-field-hint' }, t('main.forwarding.traffic', {
          out: formatBytes(runtime.bytesOut),
          in: formatBytes(runtime.bytesIn),
          connections: runtime.connections,
        })),
        tag(running ? t('main.forwarding.running') : t('main.forwarding.stopped'), running ? 'success' : undefined))),
    field(t('main.forwarding.ruleName'), input({
      value: rule.name,
      placeholder: t('main.forwarding.ruleNamePlaceholder'),
      onInput: (event) => {
        rule.name = event.target.value
      },
    })),
    el('div', { class: 'dsh-ot-row' },
      field(t('main.forwarding.localHost'), input({
        value: rule.listenHost,
        placeholder: '127.0.0.1',
        onInput: (event) => {
          rule.listenHost = event.target.value
        },
      })),
      field(t('main.forwarding.localPort'), input({
        type: 'number',
        value: rule.listenPort,
        min: 1,
        max: 65535,
        onInput: (event) => {
          rule.listenPort = Number(event.target.value)
        },
      }))),
    el('div', { class: 'dsh-ot-row' },
      field(t('main.forwarding.remoteHost'), input({
        value: rule.remoteHost,
        placeholder: '127.0.0.1',
        onInput: (event) => {
          rule.remoteHost = event.target.value
        },
      })),
      field(t('main.forwarding.remotePort'), input({
        type: 'number',
        value: rule.remotePort,
        min: 1,
        max: 65535,
        onInput: (event) => {
          rule.remotePort = Number(event.target.value)
        },
      }))),
    checkbox(t('main.forwarding.allowPublic'), rule.allowPublic === true, (value) => {
      rule.allowPublic = value
      repaint()
    }),
    rule.allowPublic === true ? el('div', { class: 'dsh-ot-field-hint' }, t('main.forwarding.allowPublicHint')) : undefined,
    el('div', { class: 'dsh-ot-toolbar-group', style: { 'justify-content': 'flex-end' } },
      button({
        label: t('main.forwarding.start'),
        variant: 'primary',
        disabled: running,
        onClick: () => void startForward(serverId, rule),
      }),
      button({ label: t('main.forwarding.stop'), disabled: !running, onClick: () => void stopForward(serverId, rule.id) }),
      button({
        label: t('main.forwarding.delete'),
        variant: 'danger',
        onClick: async () => {
          try {
            await apiPost('/tunnel/forward/delete', { serverId, ruleId: rule.id })
            draft.portForwards = draft.portForwards.filter((row) => row.id !== rule.id)
            await loadState()
            repaint()
          } catch (error) {
            toastError(error)
          }
        },
      })))
}

/** The SOCKS5 section. */
function renderSocksSection(serverId, draft, repaint) {
  const running = isSocksRunning(serverId)
  const runtime = model.tunnels.socks5.find((row) => row.serverId === serverId)
  const proxy = draft.socks5Proxy
  return section(t('main.forwarding.socks5Title'), [
    button({ label: t('main.forwarding.saveConfig'), onClick: () => void saveForwarding(serverId, draft) }),
  ],
  el('div', { class: 'dsh-ot-section-head' },
    el('span', { class: 'dsh-ot-section-title' }, t('main.forwarding.socks5Listen')),
    el('div', { class: 'dsh-ot-toolbar-group' },
      runtime === undefined ? undefined : el('span', { class: 'dsh-ot-field-hint' }, t('main.forwarding.traffic', {
        out: formatBytes(runtime.bytesOut),
        in: formatBytes(runtime.bytesIn),
        connections: runtime.connections,
      })),
      tag(running ? t('main.forwarding.running') : t('main.forwarding.stopped'), running ? 'success' : undefined))),
  el('div', { class: 'dsh-ot-row' },
    field(t('main.forwarding.listenHost'), input({
      value: proxy.listenHost,
      placeholder: '127.0.0.1',
      onInput: (event) => {
        proxy.listenHost = event.target.value
      },
    })),
    field(t('main.forwarding.listenPort'), input({
      type: 'number',
      value: proxy.listenPort,
      min: 1,
      max: 65535,
      onInput: (event) => {
        proxy.listenPort = Number(event.target.value)
      },
    }))),
  checkbox(t('main.forwarding.allowPublic'), proxy.allowPublic === true, (value) => {
    proxy.allowPublic = value
    repaint()
  }),
  proxy.allowPublic === true ? el('div', { class: 'dsh-ot-field-hint' }, t('main.forwarding.allowPublicHint')) : undefined,
  el('div', { class: 'dsh-ot-toolbar-group', style: { 'justify-content': 'flex-end' } },
    button({
      label: t('main.forwarding.start'),
      variant: 'primary',
      disabled: running,
      onClick: async () => {
        try {
          const runtimeInfo = await withBusy(() => apiPost('/tunnel/socks/start', { serverId, proxy }))
          toast(t('main.forwarding.socks5Started', { host: runtimeInfo.listenHost, port: runtimeInfo.listenPort }), 'success')
          await loadState()
        } catch (error) {
          toastError(error)
        }
      },
    }),
    button({
      label: t('main.forwarding.stop'),
      disabled: !running,
      onClick: async () => {
        try {
          await apiPost('/tunnel/socks/stop', { serverId })
          toast(t('main.forwarding.socks5Stopped'), 'success')
          await loadState()
        } catch (error) {
          toastError(error)
        }
      },
    })))
}

/** Start one rule. */
async function startForward(serverId, rule) {
  try {
    const runtime = await withBusy(() => apiPost('/tunnel/forward/start', { serverId, rule }))
    toast(t('main.forwarding.started', { host: runtime.listenHost, port: runtime.listenPort }), 'success')
    await loadState()
  } catch (error) {
    toastError(error)
  }
}

/** Stop one rule. */
async function stopForward(serverId, ruleId) {
  try {
    await apiPost('/tunnel/forward/stop', { serverId, ruleId })
    toast(t('main.forwarding.stoppedMessage'), 'success')
    await loadState()
  } catch (error) {
    toastError(error)
  }
}

/** Persist the draft rules and proxy config. */
async function saveForwarding(serverId, draft) {
  try {
    await apiPost('/tunnel/forward/save', { serverId, rules: draft.portForwards })
    await loadState()
    toast(t('main.forwarding.saved'), 'success')
  } catch (error) {
    toastError(error)
  }
}

// ===== src/client/desktop.js =====
/**
 * The remote-desktop card: the reference's hero panel, field for field (protocol,
 * address, user, credential state) plus a launch button.
 *
 * Two additions, both because the reference put a password on a command line: the
 * card lists which native clients this machine actually has, and handing the stored
 * password to the client is a checkbox that says what it costs.
 */

/** Build the desktop pane for one server tab. */
function buildDesktopPane(tab) {
  const pane = el('div', { class: 'dsh-ot-pane', 'data-tab': tab.id })
  renderDesktopPane(pane, tab)
  return pane
}

/** Repaint the card. */
function renderDesktopPane(pane, tab) {
  const server = serverById(tab.serverId)
  if (server === undefined) {
    fill(pane, el('div', { class: 'dsh-ot-empty' }, t('main.emptySelectServer')))
    return
  }
  const clients = server.protocol === 'rdp' ? model.desktop.rdp : model.desktop.vnc
  const available = Array.isArray(clients) ? clients : []
  const sendPassword = tab.sendPassword === true

  fill(pane, el('div', { class: 'dsh-ot-editor', style: { gap: '14px' } },
    el('div', { class: 'dsh-ot-section' },
      el('div', { class: 'dsh-ot-section-head' },
        el('div', {},
          el('div', { class: 'dsh-ot-field-hint' }, t('main.desktop.title')),
          el('div', { style: { 'font-size': '18px', 'font-weight': '600' } }, server.name)),
        button({
          label: tab.launching === true ? t('main.desktop.launching') : t('main.desktop.launch'),
          variant: 'primary',
          icon: 'monitor',
          disabled: tab.launching === true || available.length === 0,
          onClick: () => void launchDesktop(tab, server),
        })),
      el('div', { class: 'dsh-ot-field-hint' }, t('main.desktop.subtitle')),
      el('div', { class: 'dsh-ot-row', style: { 'flex-wrap': 'wrap' } },
        infoCard(t('main.desktop.protocol'), String(server.protocol).toUpperCase()),
        infoCard(t('main.desktop.address'), server.host + ':' + server.port),
        infoCard(t('main.serverDialog.username'), server.username.length > 0 ? server.username : '-'),
        infoCard(t('main.desktop.auth'), server.hasPassword === true ? t('main.desktop.passwordAuth') : t('main.desktop.noPassword'))),
      el('div', { class: 'dsh-ot-field-hint' },
        t('main.desktop.clients') + '：' + (available.length > 0 ? available.join('、') : t('main.desktop.noClients'))),
      server.hasPassword === true
        ? el('div', {},
          checkbox(t('main.desktop.sendPassword'), sendPassword, (value) => {
            tab.sendPassword = value
            renderDesktopPane(pane, tab)
          }),
          sendPassword ? el('div', { class: 'dsh-ot-field-hint' }, t('main.desktop.sendPasswordHint')) : undefined)
        : undefined)))
}

/** One labelled value box. */
function infoCard(label, value) {
  return el('div', { class: 'dsh-ot-section', style: { gap: '4px', 'min-width': '160px' } },
    el('div', { class: 'dsh-ot-field-hint' }, label),
    el('div', { style: { 'font-size': '14px', 'font-weight': '600' } }, value))
}

/** Ask the host to spawn a client. */
async function launchDesktop(tab, server) {
  tab.launching = true
  emit()
  try {
    const value = await apiPost('/desktop/launch', { serverId: server.id, sendPassword: tab.sendPassword === true })
    toast(t('main.desktop.launchSuccess', { client: value.client, host: value.host, port: value.port }), 'success')
  } catch (error) {
    toast(t('main.desktop.launchFailed', { message: friendlyError(error) }), 'error')
  } finally {
    tab.launching = false
    emit()
  }
}

// ===== src/client/aibar.js =====
/**
 * The AI bar: DSH's own model, one row under the terminal.
 *
 * Two jobs, and a deliberate asymmetry between them:
 *
 *   一句话 → 命令   the answer is INSERTED into the terminal without a trailing
 *                   newline, so the user reads it and presses Enter. "Run it" exists,
 *                   but a command matching the danger list has to be confirmed first.
 *   解释这屏输出     the answer is prose in the same bar; nothing is ever sent to the
 *                   shell.
 *
 * Nothing here talks to a model directly: `/ai/start` runs it host-side through
 * `ctx.llm.stream` and the deltas arrive on the panel's event stream, which means
 * closing the bar (or the whole panel) does not cancel a generation, and reopening
 * shows what has arrived so far.
 */

/** Render the bar into its host element. */
function renderAiBar(host) {
  const tab = activeTab()
  const isTerminal = tab !== undefined && tab.kind === 'terminal'
  setData(host, 'hidden', !model.aiOpen)
  if (!model.aiOpen) {
    fill(host)
    return
  }
  if (model.ai.available !== true) {
    fill(host, el('div', { class: 'dsh-ot-ai-row' },
      el('span', { class: 'dsh-ot-field-hint' }, t('ai.unavailable', { reason: model.ai.reason ?? '' })),
      el('span', { style: { flex: '1' } }),
      iconButton('close', { variant: 'ghost', title: t('main.close'), onClick: () => toggleAiBar(false) })))
    return
  }

  const job = activeJob()
  const box = input({
    value: model.aiAsk ?? '',
    placeholder: t('ai.askPlaceholder'),
    onInput: (event) => {
      model.aiAsk = event.target.value
    },
    onKeydown: (event) => {
      if (event.key === 'Enter') {
        event.preventDefault()
        void startAiJob('command')
      }
    },
  })
  const rows = [
    el('div', { class: 'dsh-ot-ai-row' },
      el('span', { class: 'dsh-ot-entry-icon' }, icon('sparkles', 16)),
      box,
      button({ label: t('ai.generate'), variant: 'primary', disabled: !isTerminal, onClick: () => void startAiJob('command') }),
      button({ label: t('ai.explain'), disabled: !isTerminal, onClick: () => void startAiJob('explain') }),
      iconButton('close', { variant: 'ghost', title: t('main.close'), onClick: () => toggleAiBar(false) })),
    el('div', { class: 'dsh-ot-ai-meta' },
      el('span', {}, t('ai.model', { provider: model.ai.provider ?? '', model: model.ai.model ?? '' })),
      isTerminal ? undefined : el('span', {}, t('ai.needTerminal'))),
  ]

  if (job !== undefined) rows.push(...renderAiJob(job, tab))
  fill(host, ...rows)
}

/** The answer block for one job. */
function renderAiJob(job, tab) {
  const rows = []
  const running = job.status === 'running'
  rows.push(el('div', { class: 'dsh-ot-ai-out', 'data-kind': job.kind },
    job.text.length > 0 ? job.text : (running ? t('ai.thinking') : (job.error.length > 0 ? job.error : ''))))
  if (job.kind === 'command' && job.risk !== undefined && job.risk.dangerous === true) {
    rows.push(el('div', { class: 'dsh-ot-ai-danger' },
      icon('warning', 16),
      el('div', {},
        el('div', { style: { 'font-weight': '600' } }, t('ai.dangerTitle')),
        el('div', {}, t('ai.dangerBody', { reasons: job.risk.reasons.join(', ') })))))
  }
  const actions = [
    running
      ? button({ label: t('ai.cancel'), onClick: () => void apiPost('/ai/cancel', { jobId: job.id }).catch(toastError) })
      : undefined,
  ]
  if (!running && job.kind === 'command' && job.text.length > 0) {
    actions.push(button({
      label: t('ai.insert'),
      variant: 'primary',
      disabled: tab === undefined || tab.kind !== 'terminal',
      onClick: () => insertAiCommand(job, tab, false),
    }))
    actions.push(button({
      label: t('ai.run'),
      icon: 'play',
      disabled: tab === undefined || tab.kind !== 'terminal',
      onClick: () => void insertAiCommand(job, tab, true),
    }))
    actions.push(button({ label: t('ai.copy'), icon: 'copy', onClick: () => void copyText(job.text, t('main.copied')) }))
  }
  if (!running) {
    actions.push(button({
      label: t('ai.discard'),
      onClick: () => {
        model.aiJobId = null
        emit()
      },
    }))
  }
  rows.push(el('div', { class: 'dsh-ot-ai-row', style: { 'justify-content': 'flex-end' } }, ...actions.filter((row) => row !== undefined)))
  return rows
}

/** Show or hide the bar. */
function toggleAiBar(open) {
  model.aiOpen = open === undefined ? !model.aiOpen : open === true
  emit()
}

/** Start one job for the active terminal. */
async function startAiJob(kind) {
  const tab = activeTab()
  if (tab === undefined || tab.kind !== 'terminal') {
    toast(t('ai.needTerminal'), 'warning')
    return
  }
  const ask = String(model.aiAsk ?? '').trim()
  if (kind === 'command' && ask.length === 0) return
  try {
    const job = await apiPost('/ai/start', {
      kind,
      sessionId: tab.sessionId,
      ask,
      language: pref('aiLanguage', 'zh'),
      withContext: true,
    })
    mergeJob(job)
    model.aiJobId = job.id
    if (kind === 'command') model.aiAsk = ''
    emit()
  } catch (error) {
    toast(t('ai.failed', { message: friendlyError(error) }), 'error')
  }
}

/**
 * Put one suggested command into the terminal.
 *
 * Inserting sends the text with NO newline: the command lands at the prompt and the
 * user presses Enter. Running appends the newline, and a command on the danger list
 * has to be confirmed first (unless the user turned that off).
 */
async function insertAiCommand(job, tab, run) {
  const text = job.text
  if (run === true && job.risk !== undefined && job.risk.dangerous === true && pref('confirmDangerousCommands', true) === true) {
    const go = await confirmDialog({
      title: t('ai.dangerTitle'),
      message: text,
      detail: t('ai.dangerBody', { reasons: job.risk.reasons.join(', ') }),
      danger: true,
      confirmLabel: t('ai.dangerConfirm'),
    })
    if (!go) return
  }
  const sent = typeIntoTerminal(tab.id, run === true ? text + '\n' : text)
  if (!sent) {
    toast(t('main.terminalNotReady'), 'warning')
    return
  }
  model.aiJobId = null
  emit()
}

// ===== src/client/settings.js =====
/**
 * The settings dialog: the reference's one SSH setting, the terminal preferences that
 * used to be hard-coded, and the host-key list.
 *
 * The reference's dialog had exactly one control (what happens to tabs when a
 * connection closes) plus three lines of explanation, and the explanation is
 * reproduced verbatim. The terminal tab exists because the reference's font size,
 * scrollback and cursor blink were constants in its component; there is no reason for
 * them to be.
 */

/** Open the dialog. */
function openSettingsDialog() {
  let activeTab = 'ssh'
  const body = el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '12px' } })
  openDialog({
    title: t('settings.dialogTitle'),
    build: () => body,
    footer: (close) => [button({ label: t('main.close'), variant: 'primary', onClick: () => close(undefined) })],
  })
  const repaint = () => {
    const tabs = [
      { id: 'ssh', label: t('settings.tabSsh') },
      { id: 'terminal', label: t('settings.tabTerminal') },
      { id: 'hostkeys', label: t('settings.tabHostKeys') },
    ]
    fill(body,
      el('div', { class: 'dsh-ot-toolbar-group' }, ...tabs.map((tab) => button({
        label: tab.label,
        active: activeTab === tab.id,
        onClick: () => {
          activeTab = tab.id
          repaint()
        },
      }))),
      activeTab === 'ssh' ? renderSshSettings() : undefined,
      activeTab === 'terminal' ? renderTerminalSettings(repaint) : undefined,
      activeTab === 'hostkeys' ? renderHostKeySettings(repaint) : undefined)
  }
  repaint()
}

/** The one setting the reference had, with its three hint lines. */
function renderSshSettings() {
  return el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '12px' } },
    field(t('settings.closeBehavior'), radioGroup('dsh-ot-close', [
      { id: 'close-tabs', label: t('settings.closeTabs') },
      { id: 'keep-tabs', label: t('settings.keepTabs') },
    ], pref('closeBehavior', 'close-tabs'), (value) => void savePrefs({ closeBehavior: value }))),
    el('div', { class: 'dsh-ot-section', style: { gap: '6px' } },
      el('div', { class: 'dsh-ot-field-hint' }, t('settings.hint1')),
      el('div', { class: 'dsh-ot-field-hint' }, t('settings.hint2')),
      el('div', { class: 'dsh-ot-field-hint' }, t('settings.hint3'))),
    el('div', { class: 'dsh-ot-field-hint' },
      model.local.pty === true
        ? t('settings.ptyOk')
        : t('settings.ptyMissing', { reason: model.local.reason ?? '' })))
}

/** Terminal look and feel. */
function renderTerminalSettings(repaint) {
  return el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '12px' } },
    el('div', { class: 'dsh-ot-row' },
      field(t('settings.fontSize'), input({
        type: 'number',
        value: pref('fontSize', 13),
        min: 8,
        max: 32,
        onChange: (event) => {
          void savePrefs({ fontSize: Number(event.target.value) }).then(restyleTerminals)
        },
      })),
      field(t('settings.scrollback'), input({
        type: 'number',
        value: pref('scrollback', 2000),
        min: 200,
        max: 200000,
        onChange: (event) => {
          void savePrefs({ scrollback: Number(event.target.value) }).then(restyleTerminals)
        },
      }))),
    field(t('main.theme'), select(themeOptions().map((name) => ({ id: name, label: name })), pref('themeName', 'default'), (value) => {
      void savePrefs({ themeName: value }).then(restyleTerminals)
    })),
    checkbox(t('settings.cursorBlink'), pref('cursorBlink', true) === true, (value) => {
      void savePrefs({ cursorBlink: value }).then(restyleTerminals)
    }),
    checkbox(t('settings.copyOnSelect'), pref('copyOnSelect', false) === true, (value) => void savePrefs({ copyOnSelect: value })),
    checkbox(t('settings.confirmDangerous'), pref('confirmDangerousCommands', true) === true, (value) => (
      void savePrefs({ confirmDangerousCommands: value })
    )),
    field(t('settings.localShell'), input({
      value: pref('localShell', ''),
      placeholder: t('settings.localShellPlaceholder'),
      onChange: (event) => void savePrefs({ localShell: event.target.value }),
    }), model.local.shell),
    field(t('settings.aiLanguage'), select([
      { id: 'zh', label: '中文' },
      { id: 'en', label: 'English' },
    ], pref('aiLanguage', 'zh'), (value) => void savePrefs({ aiLanguage: value }))))
}

/** The accepted host keys, with a delete button each. */
function renderHostKeySettings(repaint) {
  const rows = model.knownHosts.length === 0
    ? [el('div', { class: 'dsh-ot-empty' }, t('hostkey.empty'))]
    : model.knownHosts.map((row) => el('div', { class: 'dsh-ot-section', style: { gap: '6px' } },
      el('div', { class: 'dsh-ot-section-head' },
        el('span', { class: 'dsh-ot-section-title' }, row.endpoint),
        button({
          label: t('hostkey.forget'),
          variant: 'danger',
          onClick: async () => {
            const parsed = parseEndpoint(row.endpoint)
            try {
              await apiPost('/connection/host-key/forget', parsed)
              await loadState()
              repaint()
              toast(t('hostkey.forgotten'), 'success')
            } catch (error) {
              toastError(error)
            }
          },
        })),
      el('div', { class: 'dsh-ot-mono' }, (row.keyType ?? '') + ' ' + row.fingerprint),
      el('div', { class: 'dsh-ot-field-hint' }, formatTime(row.acceptedAt))))
  return el('div', { style: { display: 'flex', 'flex-direction': 'column', gap: '10px' } },
    el('div', { class: 'dsh-ot-section-title' }, t('hostkey.listTitle')),
    ...rows)
}

/** `[host]:port` or `host` back into `{host, port}`. */
function parseEndpoint(endpoint) {
  const match = String(endpoint).match(/^\[(.+)\]:(\d+)$/)
  if (match !== null) return { host: match[1], port: Number(match[2]) }
  return { host: String(endpoint), port: 22 }
}

// ===== src/client/shell.js =====
/**
 * The shell: sidebar, toolbar, tab strip, panes, drawer and AI bar — the layout of
 * the reference's Term.vue.
 *
 * The one structural difference from the sibling plugins: this panel cannot repaint
 * everything. A live xterm and an editor caret do not survive being rebuilt, so the
 * PANES are reconciled (`syncPanes`: create the missing, drop the closed, toggle
 * visibility) while the chrome around them — sidebar, toolbar, tab strip, drawer — is
 * rebuilt wholesale on every change, which keeps the state handling simple where it
 * can be.
 */

let panelEl = null
let sideEl = null
let serversEl = null
let sftpEl = null
let toolbarEl = null
let tabsEl = null
let panesEl = null
let drawerEl = null
let aiEl = null
let entryEl = null
let viewEl = null

/** Build the panel shell once. */
function buildPanel(view) {
  sideEl = el('div', { class: 'dsh-ot-side' })
  serversEl = el('div', { class: 'dsh-ot-servers' })
  sftpEl = el('div', { class: 'dsh-ot-sftp' })
  toolbarEl = el('div', { class: 'dsh-ot-toolbar' })
  tabsEl = el('div', { class: 'dsh-ot-tabs' })
  panesEl = el('div', { class: 'dsh-ot-panes' })
  drawerEl = el('div', { class: 'dsh-ot-drawer', 'data-hidden': 'true' })
  aiEl = el('div', { class: 'dsh-ot-ai', 'data-hidden': 'true' })

  const splitter = resizeHandle({
    axis: 'y',
    min: SERVER_LIST_MIN_HEIGHT,
    max: () => Math.max(SERVER_LIST_MIN_HEIGHT, (sideEl?.clientHeight ?? 600) - SFTP_PANEL_MIN_HEIGHT),
    value: () => serversEl.getBoundingClientRect().height,
    onMove: (value) => {
      serversEl.style.height = value + 'px'
      serversEl.style.flex = '0 0 auto'
    },
    onCommit: (value) => void savePrefs({ serverListHeight: Math.round(value) }),
  })
  const body = el('div', { class: 'dsh-ot-side-body' }, serversEl, splitter, sftpEl)
  sideEl.append(el('div', { class: 'dsh-ot-side-head' }), body)

  const sideSplitter = resizeHandle({
    axis: 'x',
    min: SIDEBAR_MIN_WIDTH,
    max: () => SIDEBAR_MAX_WIDTH,
    value: () => sideEl.getBoundingClientRect().width,
    onMove: (value) => {
      sideEl.style.width = value + 'px'
    },
    onCommit: (value) => void savePrefs({ sidebarWidth: Math.round(value) }),
    onReset: () => {
      sideEl.style.width = SIDEBAR_DEFAULT_WIDTH + 'px'
      void savePrefs({ sidebarWidth: SIDEBAR_DEFAULT_WIDTH })
    },
  })
  const main = el('div', { class: 'dsh-ot-main' }, toolbarEl, tabsEl,
    el('div', { class: 'dsh-ot-body' }, panesEl, drawerEl), aiEl)
  panelEl = el('div', { class: 'dsh-ot-panel', 'data-dsh-ot-panel': '' }, sideEl, sideSplitter, main)
  view.append(panelEl)
}

/** Repaint the chrome and reconcile the panes. */
function renderPanel() {
  if (panelEl === null || !panelEl.isConnected) return
  sideEl.style.width = pref('sidebarWidth', SIDEBAR_DEFAULT_WIDTH) + 'px'
  setData(sideEl, 'hidden', pref('sidebarVisible', true) !== true)
  renderSideHead()
  renderServerList()
  renderSftpSection()
  renderToolbar()
  renderTabs()
  syncPanes()
  renderDrawer(drawerEl)
  renderAiBar(aiEl)
  renderEntry()
}

/** The sidebar heading: title, subtitle, settings and add. */
function renderSideHead() {
  fill(sideEl.querySelector('.dsh-ot-side-head'),
    el('div', { class: 'dsh-ot-side-copy' },
      el('h2', { class: 'dsh-ot-side-title' }, t('main.title')),
      el('div', { class: 'dsh-ot-side-sub' }, t('main.subtitle'))),
    el('div', { class: 'dsh-ot-side-actions' },
      iconButton('settings', { title: t('main.settings'), onClick: () => openSettingsDialog() }),
      iconButton('plus', { variant: 'primary', title: t('main.newConnection'), onClick: () => openServerDialog(undefined) })))
}

/** The server rows. */
function renderServerList() {
  const rows = allServers()
  const height = pref('serverListHeight', 220)
  if (sftpVisible()) {
    serversEl.style.height = height + 'px'
    serversEl.style.flex = '0 0 auto'
  } else {
    serversEl.style.height = ''
    serversEl.style.flex = '1 1 0%'
  }
  const filter = el('div', { style: { padding: '6px 8px' } }, input({
    value: model.serverFilter,
    placeholder: t('main.searchServers'),
    onInput: (event) => {
      model.serverFilter = event.target.value
      renderServerList()
    },
  }))
  fill(serversEl, filter, ...rows.map((server) => renderServerRow(server)))
  if (rows.length === 0) serversEl.append(el('div', { class: 'dsh-ot-side-empty' }, t('main.emptySelectServer')))
}

/** One server row. */
function renderServerRow(server) {
  const state = connectionState(server.id)
  const meta = server.id === LOCAL_SERVER_ID
    ? (model.local.shell ?? '') + (model.local.pty === true ? '' : ' · ' + t('term.compatibility'))
    : protocolLabel(server.protocol) + ' · ' + (server.username.length > 0 ? server.username + '@' : '') + server.host + ':' + server.port
  return el('div', {
    class: 'dsh-ot-server',
    'data-active': model.selectedServerId === server.id ? 'true' : undefined,
    onClick: () => selectServer(server.id),
    onContextmenu: (event) => {
      event.preventDefault()
      openServerMenu(server, event.clientX, event.clientY)
    },
  },
  el('div', { class: 'dsh-ot-server-main' },
    el('div', { class: 'dsh-ot-server-row' },
      el('span', { class: 'dsh-ot-dot', 'data-state': state }),
      el('span', { class: 'dsh-ot-server-name', title: server.name }, server.name)),
    el('div', { class: 'dsh-ot-server-meta', title: meta }, meta)),
  el('div', { class: 'dsh-ot-server-badges' },
    hasTunnels(server.id) ? tag('', 'primary', { title: t('main.menu.forwarding') }) : undefined,
    el('span', {
      class: 'dsh-ot-menu-btn',
      title: t('main.menu.editConnection'),
      onClick: (event) => {
        event.stopPropagation()
        const box = event.currentTarget.getBoundingClientRect()
        openServerMenu(server, box.left, box.bottom + 4)
      },
    }, icon('more', 16))))
}

/** The protocol label, with the reference's wording. */
function protocolLabel(protocol) {
  if (protocol === 'rdp') return t('main.serverDialog.protocolRdp')
  if (protocol === 'vnc') return t('main.serverDialog.protocolVnc')
  return t('main.serverDialog.protocolSsh')
}

/** Whether the SFTP half of the sidebar is showing. */
function sftpVisible() {
  return sftpState().serverId.length > 0
}

/** The SFTP section (hidden when no server has it open). */
function renderSftpSection() {
  if (!sftpVisible()) {
    fill(sftpEl)
    sftpEl.style.display = 'none'
    return
  }
  sftpEl.style.display = 'flex'
  renderSftpPane(sftpEl)
}
/** The toolbar: collapse, title, the three popover buttons, the theme picker. */
function renderToolbar() {
  const server = selectedServer()
  const sidebarOn = pref('sidebarVisible', true) === true
  const subtitle = server === undefined
    ? t('main.toolbarSubtitleDefault')
    : (server.id === LOCAL_SERVER_ID
      ? (model.local.shell ?? '')
      : protocolLabel(server.protocol) + ' · ' + (server.username.length > 0 ? server.username + '@' : '') + server.host + ':' + server.port)
  fill(toolbarEl,
    el('div', { class: 'dsh-ot-toolbar-left' },
      iconButton(sidebarOn ? 'sidebar-collapse' : 'sidebar-expand', {
        title: sidebarOn ? t('main.collapseSidebar') : t('main.expandSidebar'),
        onClick: () => void savePrefs({ sidebarVisible: !sidebarOn }),
      }),
      el('div', { style: { 'min-width': '0' } },
        el('div', { class: 'dsh-ot-toolbar-title' }, server === undefined ? t('main.toolbarDefaultTitle') : server.name),
        el('div', { class: 'dsh-ot-toolbar-sub', title: subtitle }, subtitle))),
    el('div', { class: 'dsh-ot-toolbar-group' },
      button({
        icon: 'commands',
        iconOnly: true,
        title: t('main.favoriteCommands'),
        onClick: (event) => openCommandsMenu(event),
      }),
      button({
        icon: 'folder-star',
        iconOnly: true,
        title: t('main.favoriteDirectories'),
        disabled: !canUseSftp(server),
        onClick: (event) => openFavoriteDirsMenu(event),
      }),
      button({
        icon: 'sparkles',
        iconOnly: true,
        title: t('ai.title'),
        active: model.aiOpen,
        onClick: () => toggleAiBar(),
      }),
      button({
        icon: 'upload',
        label: t('main.transferTasks'),
        badge: runningTaskCount(),
        active: model.drawerOpen,
        onClick: () => {
          model.drawerOpen = !model.drawerOpen
          emit()
        },
      })),
    el('div', { class: 'dsh-ot-toolbar-group' },
      model.busy > 0 ? el('span', { class: 'dsh-ot-field-hint' }, t('main.connecting')) : undefined,
      select(themeOptions().map((name) => ({ id: name, label: name })), pref('themeName', 'default'), (value) => {
        void savePrefs({ themeName: value }).then(restyleTerminals)
      })))
}

/** The tab strip: terminals, editors, the desktop card, and the add button. */
function renderTabs() {
  const server = selectedServer()
  const rows = model.tabs.filter((tab) => server === undefined || tab.serverId === server.id)
  fill(tabsEl, ...rows.map((tab) => renderTab(tab)),
    canOpenTerminal(server)
      ? el('button', {
        type: 'button',
        class: 'dsh-ot-tab dsh-ot-tab-add',
        title: t('term.newTab'),
        onClick: () => addTerminalTab(server.id),
      }, '+')
      : undefined)
}

/** One tab button. */
function renderTab(tab) {
  const session = tab.kind === 'terminal' ? sessionById(tab.sessionId) : undefined
  const label = (tab.kind === 'editor' && tab.dirty === true ? '* ' : '') + tab.name
  return el('button', {
    type: 'button',
    class: 'dsh-ot-tab',
    'data-active': model.activeTabId === tab.id ? 'true' : undefined,
    title: tab.kind === 'editor' ? tab.filePath : tab.name,
    onClick: () => activateTab(tab.id),
    onContextmenu: (event) => {
      event.preventDefault()
      openContextMenu(event.clientX, event.clientY, [
        { label: t('term.closeTab'), icon: 'close', onClick: () => void closeTab(tab.id) },
      ], tab.name)
    },
  },
  tab.kind === 'terminal'
    ? el('span', { class: 'dsh-ot-tab-dot', 'data-state': session === undefined ? 'closed' : session.status })
    : icon(tab.kind === 'editor' ? 'file' : 'monitor', 13),
  el('span', { class: 'dsh-ot-tab-label' }, label),
  el('span', {
    class: 'dsh-ot-tab-close',
    title: t('term.closeTab'),
    onClick: (event) => {
      event.stopPropagation()
      void closeTab(tab.id)
    },
  }, '×'))
}

/**
 * Reconcile the panes.
 *
 * Every tab owns one pane element, created on first sight and kept until the tab
 * closes. Only the `data-active` flag moves, so switching tabs never re-creates an
 * xterm (which would clear the screen) or a textarea (which would lose the caret).
 */
function syncPanes() {
  const server = selectedServer()
  const wanted = model.tabs.filter((tab) => server === undefined || tab.serverId === server.id)
  const seen = new Set()
  for (const tab of wanted) {
    seen.add(tab.id)
    let pane = panesEl.querySelector('[data-tab="' + cssEscape(tab.id) + '"]')
    if (pane === null) {
      pane = tab.kind === 'terminal'
        ? buildTerminalPane(tab)
        : (tab.kind === 'editor' ? buildEditorPane(tab) : buildDesktopPane(tab))
      panesEl.append(pane)
    }
    const active = model.activeTabId === tab.id
    setData(pane, 'active', active)
    if (tab.kind === 'editor') renderEditorChrome(tab)
    if (tab.kind === 'desktop') renderDesktopPane(pane, tab)
    if (active && tab.kind === 'terminal') focusTerminal(tab.id)
  }
  // Panes whose tab is gone (or belongs to another server) are dropped; the terminal
  // ones keep their host session, so re-selecting the server re-attaches.
  for (const pane of [...panesEl.children]) {
    const id = pane.dataset.tab
    if (id !== undefined && !seen.has(id)) {
      if (terminalOf(id) !== undefined) disposeTerminal(id)
      else if (editors.has(id)) disposeEditor(id)
      else pane.remove()
    }
  }
  if (wanted.length === 0) {
    const empty = el('div', { class: 'dsh-ot-pane', 'data-active': 'true', 'data-tab': '__empty__' },
      el('div', { class: 'dsh-ot-empty' },
        el('span', {}, server === undefined ? t('main.emptySelectServer') : t('main.emptyNoTab')),
        model.vendor.ready === false ? el('span', {}, t('term.vendorMissing')) : undefined))
    fill(panesEl, empty)
  }
}

/** Minimal CSS.escape for the ids this panel mints. */
function cssEscape(value) {
  return String(value).replace(/["\\]/g, '\\$&')
}
// -------------------------------------------------------------- selection
/**
 * Select one server row. Selecting a row that can hold a terminal and has none opens
 * one, which is what the reference did (and is why clicking a server feels like
 * connecting to it).
 */
function selectServer(serverId) {
  model.selectedServerId = serverId
  storeSet(STORE_KEYS.selectedServer, serverId)
  const server = serverById(serverId)
  const own = model.tabs.filter((tab) => tab.serverId === serverId)
  if (own.length === 0) {
    if (canLaunchDesktop(server)) addDesktopTab(serverId)
    else if (canOpenTerminal(server)) addTerminalTab(serverId)
  } else if (own.every((tab) => tab.id !== model.activeTabId)) {
    model.activeTabId = own[0].id
  }
  if (canUseSftp(server) && sftpState().serverId !== serverId && pref('sftpVisible', true) === true) {
    openSftpFor(serverId)
  }
  emit()
  scheduleWorkspaceSave()
}

/** Open a new terminal tab on one server. */
function addTerminalTab(serverId, options) {
  const server = serverById(serverId)
  if (!canOpenTerminal(server)) return undefined
  const count = model.tabs.filter((tab) => tab.serverId === serverId && tab.kind === 'terminal').length
  const tab = {
    id: newId('tab'),
    kind: 'terminal',
    serverId,
    sessionId: newId('sess'),
    name: count > 0 ? server.name + ' (' + (count + 1) + ')' : server.name,
    cwd: options?.cwd,
    initialCommand: options?.initialCommand,
  }
  model.tabs = [...model.tabs, tab]
  model.activeTabId = tab.id
  model.selectedServerId = serverId
  emit()
  scheduleWorkspaceSave()
  return tab
}

/** Open (or focus) an editor tab for one remote file. */
function openRemoteFile(serverId, path) {
  const existing = model.tabs.find((tab) => tab.kind === 'editor' && tab.serverId === serverId && tab.filePath === path)
  if (existing !== undefined) {
    model.activeTabId = existing.id
    model.selectedServerId = serverId
    emit()
    return existing
  }
  const tab = {
    id: newId('tab'),
    kind: 'editor',
    serverId,
    filePath: path,
    name: baseName(path),
    content: '',
    original: '',
    dirty: false,
    loading: true,
  }
  model.tabs = [...model.tabs, tab]
  model.activeTabId = tab.id
  model.selectedServerId = serverId
  emit()
  scheduleWorkspaceSave()
  return tab
}

/** Open a terminal already `cd`-ed into one directory. */
function openTerminalAt(serverId, path) {
  const server = serverById(serverId)
  if (server === undefined) return
  // The path is single-quoted for the remote shell: a directory called `it's here`
  // must not end the argument early.
  const quoted = "'" + String(path).replace(/'/g, "'\\''") + "'"
  const tab = addTerminalTab(serverId, { cwd: path, initialCommand: 'cd ' + quoted })
  if (tab !== undefined) tab.name = server.name + ' (' + baseName(path) + ')'
  emit()
}

/** Open the desktop card for an RDP/VNC row. */
function addDesktopTab(serverId) {
  const server = serverById(serverId)
  if (!canLaunchDesktop(server)) return undefined
  const existing = model.tabs.find((tab) => tab.kind === 'desktop' && tab.serverId === serverId)
  if (existing !== undefined) {
    model.activeTabId = existing.id
    emit()
    return existing
  }
  const tab = { id: newId('tab'), kind: 'desktop', serverId, name: server.name }
  model.tabs = [...model.tabs, tab]
  model.activeTabId = tab.id
  emit()
  return tab
}

/** Switch tabs. */
function activateTab(tabId) {
  const tab = tabById(tabId)
  if (tab === undefined) return
  model.activeTabId = tabId
  model.selectedServerId = tab.serverId
  emit()
  scheduleWorkspaceSave()
}

/**
 * Close one tab.
 *
 * A terminal tab closes its HOST session too: leaving it running would be a shell
 * nobody can reach. An editor with unsaved changes asks first.
 */
async function closeTab(tabId) {
  const tab = tabById(tabId)
  if (tab === undefined) return
  if (tab.kind === 'editor' && tab.dirty === true) {
    const go = await confirmDialog({
      title: t('term.closeTab'),
      message: t('editor.discardConfirm', { name: tab.name }),
      danger: true,
    })
    if (!go) return
  }
  if (tab.kind === 'terminal') {
    try {
      await apiPost('/terminal/close', { sessionId: tab.sessionId })
    } catch { /* a session that already ended is fine */ }
    disposeTerminal(tabId)
  }
  if (tab.kind === 'editor') disposeEditor(tabId)
  model.tabs = model.tabs.filter((row) => row.id !== tabId)
  if (model.activeTabId === tabId) {
    const sibling = model.tabs.find((row) => row.serverId === tab.serverId) ?? model.tabs[0]
    model.activeTabId = sibling === undefined ? null : sibling.id
  }
  emit()
  await pushSubscriptions()
  scheduleWorkspaceSave()
}

/** Rewrite the tabs after a rename (the reference did this for its editor tabs). */
function renamePathInTabs(from, to, isDirectory) {
  for (const tab of model.tabs) {
    if (tab.kind !== 'editor') continue
    if (tab.filePath === from) {
      tab.filePath = to
      tab.name = baseName(to)
      continue
    }
    if (isDirectory && tab.filePath.startsWith(from + '/')) {
      tab.filePath = to + tab.filePath.slice(from.length)
      tab.name = baseName(tab.filePath)
    }
  }
  emit()
}

/** Close the editor tabs under a deleted path. */
function closeTabsUnder(path, isDirectory) {
  const doomed = model.tabs.filter((tab) => tab.kind === 'editor' &&
    (tab.filePath === path || (isDirectory && tab.filePath.startsWith(path + '/'))))
  for (const tab of doomed) {
    disposeEditor(tab.id)
    model.tabs = model.tabs.filter((row) => row.id !== tab.id)
    if (model.activeTabId === tab.id) model.activeTabId = model.tabs[0]?.id ?? null
  }
  if (doomed.length > 0) emit()
}
// ------------------------------------------------------------------ menus
/** The per-server menu: the reference's dropdown, by protocol. */
function openServerMenu(server, x, y) {
  const items = []
  if (server.id === LOCAL_SERVER_ID) {
    items.push({ label: t('main.menu.newTerminal'), icon: 'plus', onClick: () => addTerminalTab(server.id) })
    openContextMenu(x, y, items, server.name)
    return
  }
  if (server.protocol === 'ssh') {
    const sftpOpen = sftpState().serverId === server.id
    items.push({ label: t('main.menu.newTerminal'), icon: 'plus', onClick: () => addTerminalTab(server.id) })
    items.push({ label: t('main.menu.openSftp'), icon: 'folder', onClick: () => openSftpFor(server.id) })
    items.push({ label: t('main.menu.closeSftp'), icon: 'close', disabled: !sftpOpen, onClick: () => closeSftp() })
    items.push({ label: t('main.menu.forwarding'), icon: 'tunnel', onClick: () => openForwardingDialog(server.id) })
    items.push({ separator: true })
    items.push({ label: t('main.menu.reconnect'), icon: 'refresh', onClick: () => void connectServer(server.id) })
    items.push({
      label: t('main.menu.disconnect'),
      icon: 'close',
      disabled: connectionState(server.id) === 'disconnected' && !hasTunnels(server.id),
      onClick: () => void disconnectServer(server.id),
    })
  } else {
    items.push({ label: t('main.menu.openDesktop'), icon: 'monitor', onClick: () => addDesktopTab(server.id) })
  }
  items.push({ separator: true })
  items.push({ label: t('main.menu.editConnection'), icon: 'edit', onClick: () => openServerDialog(server) })
  items.push({ label: t('main.menu.duplicate'), icon: 'copy', onClick: () => openServerDialog({ ...server, id: '', name: server.name + ' copy' }) })
  items.push({ label: t('main.menu.deleteConnection'), icon: 'trash', tone: 'danger', onClick: () => void deleteServer(server) })
  openContextMenu(x, y, items, server.name)
}

/** The saved-command list, as a menu anchored under the toolbar button. */
function openCommandsMenu(event) {
  const box = event.currentTarget.getBoundingClientRect()
  const items = model.favoriteCommands.map((row, index) => ({
    label: row.name.length > 0 ? row.name + ' — ' + row.command : row.command,
    icon: 'play',
    onClick: () => void runFavoriteCommand(row.command),
    // A right-click on the row removes it; the menu has no room for a second button.
    onContext: index,
  }))
  if (items.length === 0) items.push({ label: t('main.noFavoriteCommands'), disabled: true, onClick: () => {} })
  items.push({ separator: true })
  items.push({ label: t('main.addFavoriteCommandTitle'), icon: 'plus', onClick: () => openAddCommandDialog() })
  if (model.favoriteCommands.length > 0) {
    items.push({ label: t('main.delete') + '…', icon: 'trash', tone: 'danger', onClick: () => openManageCommandsDialog() })
  }
  openContextMenu(box.left, box.bottom + 4, items, t('main.favoriteCommands'))
}

/** Send one saved command to the active terminal. */
async function runFavoriteCommand(command) {
  const tab = activeTab()
  if (tab === undefined || tab.kind !== 'terminal') {
    toast(t('main.switchToTerminalFirst'), 'warning')
    return
  }
  if (!typeIntoTerminal(tab.id, command)) toast(t('main.terminalNotReady'), 'warning')
}

/** Add one saved command. */
function openAddCommandDialog() {
  let name = ''
  let command = ''
  openDialog({
    title: t('main.addFavoriteCommandTitle'),
    size: 'small',
    build: () => [
      field(t('main.name'), input({
        placeholder: t('main.favoriteCommandNamePlaceholder'),
        onInput: (event) => {
          name = event.target.value
        },
      })),
      field(t('main.command'), input({
        placeholder: t('main.favoriteCommandPlaceholder'),
        onInput: (event) => {
          command = event.target.value
        },
      })),
    ],
    footer: (close) => [
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      button({
        label: t('main.add'),
        variant: 'primary',
        onClick: async () => {
          if (command.trim().length === 0) return
          try {
            await apiPost('/commands', { commands: [...model.favoriteCommands, { name: name.trim(), command: command.trim() }] })
            await loadState()
            close(undefined)
          } catch (error) {
            toastError(error)
          }
        },
      }),
    ],
  })
}

/** Remove saved commands. */
function openManageCommandsDialog() {
  const keep = new Set(model.favoriteCommands.map((row, index) => index))
  openDialog({
    title: t('main.favoriteCommands'),
    build: () => model.favoriteCommands.map((row, index) => el('div', { class: 'dsh-ot-section', style: { gap: '4px' } },
      checkbox(row.name.length > 0 ? row.name : row.command, true, (on) => {
        if (on) keep.add(index)
        else keep.delete(index)
      }),
      el('div', { class: 'dsh-ot-mono' }, row.command))),
    footer: (close) => [
      button({ label: t('main.cancel'), onClick: () => close(undefined) }),
      button({
        label: t('main.save'),
        variant: 'primary',
        onClick: async () => {
          try {
            await apiPost('/commands', { commands: model.favoriteCommands.filter((row, index) => keep.has(index)) })
            await loadState()
            close(undefined)
          } catch (error) {
            toastError(error)
          }
        },
      }),
    ],
  })
}

/** The saved-directory list. */
function openFavoriteDirsMenu(event) {
  const box = event.currentTarget.getBoundingClientRect()
  const server = selectedServer()
  if (!canUseSftp(server)) return
  const rows = favoriteDirsOf(server.id)
  const items = rows.map((path) => ({
    label: path,
    icon: 'folder',
    onClick: () => {
      openSftpFor(server.id)
      void expandThenEmit(path)
    },
  }))
  if (items.length === 0) items.push({ label: t('main.noFavoriteDirectories'), disabled: true, onClick: () => {} })
  items.push({ separator: true })
  items.push({
    label: t('main.favoriteCurrentDirectory'),
    icon: 'plus',
    disabled: sftpState().serverId !== server.id || sftpState().path.length === 0,
    onClick: () => void favoriteDirectory(sftpState().path),
  })
  for (const path of rows) {
    items.push({ label: t('main.delete') + ' ' + path, icon: 'trash', tone: 'danger', onClick: () => void unfavoriteDirectory(server.id, path) })
  }
  openContextMenu(box.left, box.bottom + 4, items, t('main.favoriteDirectories'))
}
// ------------------------------------------------------------ connections
/** Connect (or reconnect) one server, showing the host-key dialog when asked. */
async function connectServer(serverId) {
  try {
    await withBusy(() => apiPost('/connection/connect', { serverId }))
    toast(t('main.connected'), 'success')
    await loadState()
  } catch (error) {
    if (codeOf(error) === 'host_key') {
      openHostKeyDialog({ ...error, serverId })
      return
    }
    toastError(error)
  }
}

/**
 * Disconnect one server: its terminals, tunnels and SFTP channel.
 *
 * The confirmation is the reference's, including the three warning fragments it
 * assembled, and it only appears under the `close-tabs` behaviour — under
 * `keep-tabs` nothing is lost by disconnecting.
 */
async function disconnectServer(serverId) {
  const closeTabs = pref('closeBehavior', 'close-tabs') === 'close-tabs'
  if (closeTabs) {
    const warnings = []
    if (hasDirtyEditors(serverId)) warnings.push(t('main.disconnect.unsavedFiles'))
    if (model.tabs.some((tab) => tab.kind === 'terminal' && tab.serverId === serverId)) {
      warnings.push(t('main.disconnect.openTerminals'))
    }
    if (runningTaskCount() > 0) warnings.push(t('main.disconnect.runningTransfers'))
    if (warnings.length > 0) {
      const go = await confirmDialog({
        title: t('main.disconnect.confirmTitle'),
        message: t('main.disconnect.confirmMessage', { warnings: warnings.join('、'), action: t('main.menu.disconnect') }),
        confirmLabel: t('main.disconnect.continue'),
        danger: true,
      })
      if (!go) return
    }
  }
  try {
    await withBusy(() => apiPost('/connection/disconnect', { serverId }))
  } catch (error) {
    toastError(error)
  }
  if (closeTabs) {
    for (const tab of model.tabs.filter((row) => row.serverId === serverId)) {
      if (tab.kind === 'terminal') disposeTerminal(tab.id)
      if (tab.kind === 'editor') disposeEditor(tab.id)
    }
    model.tabs = model.tabs.filter((row) => row.serverId !== serverId)
    if (sftpState().serverId === serverId) closeSftp()
    model.activeTabId = model.tabs[0]?.id ?? null
  }
  toast(t('main.disconnected'), 'success')
  await loadState()
  await pushSubscriptions()
  scheduleWorkspaceSave()
}

/** Delete one stored connection, after the reference's confirmation. */
async function deleteServer(server) {
  const go = await confirmDialog({
    title: t('main.deleteServerTitle'),
    message: t('main.deleteServerConfirm', { name: server.name }),
    danger: true,
    confirmLabel: t('main.delete'),
  })
  if (!go) return
  try {
    await apiPost('/servers/delete', { serverId: server.id })
    for (const tab of model.tabs.filter((row) => row.serverId === server.id)) {
      if (tab.kind === 'terminal') disposeTerminal(tab.id)
      if (tab.kind === 'editor') disposeEditor(tab.id)
    }
    model.tabs = model.tabs.filter((row) => row.serverId !== server.id)
    if (sftpState().serverId === server.id) closeSftp()
    if (model.selectedServerId === server.id) model.selectedServerId = ''
    model.activeTabId = model.tabs[0]?.id ?? null
    await loadState()
    scheduleWorkspaceSave()
  } catch (error) {
    toastError(error)
  }
}

/** The right-click menu inside a terminal. */
function openTerminalMenu(entry, x, y) {
  const term = entry.term
  const selection = term === undefined ? '' : term.getSelection()
  openContextMenu(x, y, [
    {
      label: t('term.copySelection'),
      icon: 'copy',
      disabled: selection.length === 0,
      onClick: () => void copyText(selection, t('main.copied')),
    },
    {
      label: t('term.paste'),
      icon: 'file',
      onClick: async () => {
        try {
          const text = await navigator.clipboard.readText()
          if (typeof text === 'string' && text.length > 0) sendInput(entry.sessionId, text)
        } catch (error) {
          toastError(error)
        }
      },
    },
    { separator: true },
    { label: t('term.clear'), icon: 'trash', onClick: () => term?.clear() },
    { label: t('term.searchPlaceholder'), icon: 'search', onClick: () => void findInTerminal(entry) },
    { label: t('ai.explain'), icon: 'sparkles', onClick: () => {
      toggleAiBar(true)
      void startAiJob('explain')
    } },
  ], t('main.title'))
}

// ------------------------------------------------------- workspace snapshot
let workspaceTimer = null

/**
 * Remember the layout, host-side.
 *
 * Only what a fresh panel needs to look the same: which server was selected, which
 * tabs existed (and their session ids, so a reload re-attaches to the same shells
 * rather than starting new ones), and the SFTP path.
 */
function workspaceSnapshot() {
  return {
    version: 1,
    selectedServerId: model.selectedServerId,
    activeTabId: model.activeTabId,
    tabs: model.tabs.slice(0, 24).map((tab) => ({
      id: tab.id,
      kind: tab.kind,
      serverId: tab.serverId,
      sessionId: tab.sessionId,
      name: tab.name,
      filePath: tab.filePath,
      cwd: tab.cwd,
    })),
    sftp: { serverId: sftpState().serverId, path: sftpState().path },
  }
}

/** Debounced save. */
function scheduleWorkspaceSave() {
  if (!model.booted) return
  if (workspaceTimer !== null) clearTimeout(workspaceTimer)
  workspaceTimer = setTimeout(() => {
    workspaceTimer = null
    void apiPost('/workspace', { workspace: workspaceSnapshot() }).catch(() => {})
  }, 400)
}

/** Restore one snapshot. Sessions that no longer exist are dropped. */
function restoreWorkspace(snapshot) {
  if (snapshot === null || typeof snapshot !== 'object') return
  const live = new Set(model.sessions.map((row) => row.sessionId))
  const tabs = []
  for (const row of Array.isArray(snapshot.tabs) ? snapshot.tabs : []) {
    if (typeof row !== 'object' || row === null) continue
    const server = serverById(row.serverId)
    if (server === undefined) continue
    if (row.kind === 'terminal') {
      // A terminal tab is only worth restoring when its session is still running:
      // otherwise the user gets a tab that has to be reconnected by hand.
      if (typeof row.sessionId !== 'string' || !live.has(row.sessionId)) continue
      tabs.push({ id: row.id ?? newId('tab'), kind: 'terminal', serverId: row.serverId, sessionId: row.sessionId, name: row.name ?? server.name, cwd: row.cwd })
      continue
    }
    if (row.kind === 'editor' && typeof row.filePath === 'string') {
      tabs.push({
        id: row.id ?? newId('tab'),
        kind: 'editor',
        serverId: row.serverId,
        filePath: row.filePath,
        name: row.name ?? baseName(row.filePath),
        content: '',
        original: '',
        dirty: false,
        loading: true,
      })
      continue
    }
    if (row.kind === 'desktop') tabs.push({ id: row.id ?? newId('tab'), kind: 'desktop', serverId: row.serverId, name: row.name ?? server.name })
  }
  model.tabs = tabs
  model.activeTabId = tabs.some((tab) => tab.id === snapshot.activeTabId) ? snapshot.activeTabId : (tabs[0]?.id ?? null)
  if (typeof snapshot.selectedServerId === 'string' && serverById(snapshot.selectedServerId) !== undefined) {
    model.selectedServerId = snapshot.selectedServerId
  }
  const sftp = snapshot.sftp
  if (sftp !== null && typeof sftp === 'object' && typeof sftp.serverId === 'string' && sftp.serverId.length > 0) {
    if (canUseSftp(serverById(sftp.serverId))) openSftpFor(sftp.serverId, typeof sftp.path === 'string' ? sftp.path : undefined)
  }
}
// ---------------------------------------------------------------- seat mount
/** The sidebar entry button that opens the panel. */
function createEntry() {
  return el('button', {
    type: 'button',
    class: 'dsh-ot-entry',
    'data-dsh-ssh-entry': '',
    'aria-label': t('main.title'),
    onClick: () => setOpen(!model.open),
  },
  el('span', { class: 'dsh-ot-entry-icon', 'aria-hidden': 'true' }, icon('terminal', 15)),
  el('span', { class: 'dsh-ot-entry-label' }, t('main.title')),
  el('span', { class: 'dsh-ot-entry-stats' }))
}

/** Keep the entry's badge in step: live terminals and running transfers. */
function renderEntry() {
  if (entryEl === null) return
  setData(entryEl, 'active', model.open)
  const stats = entryEl.querySelector('.dsh-ot-entry-stats')
  if (stats === null) return
  const live = model.sessions.filter((row) => row.status === 'running').length
  const moving = runningTaskCount()
  const parts = []
  if (live > 0) parts.push(String(live))
  if (moving > 0) parts.push('↑' + moving)
  fill(stats, parts.join(' '))
}

/** The DSH shell's sidebar root, across its layout generations. */
function sidebarRoot() {
  const column = document.querySelector(SIDEBAR_SELECTOR)
  if (column === null) return undefined
  const logoRow = column.querySelector('[class*="logoRow"]')
  return (logoRow !== null ? logoRow.parentElement : column.firstElementChild) ?? undefined
}

/** The centre column the panel takes over. */
function conversationColumn() {
  const column = document.querySelector(CONVERSATION_SELECTOR)
  return column === null ? undefined : column
}

/** Put the entry with the other panel plugins' entries. */
function placeEntry() {
  if (entryEl === null) return
  const root = sidebarRoot()
  if (root === undefined || !root.isConnected) return
  if (entryEl.parentElement === root) return
  const family = Array.from(root.children).filter((child) =>
    child instanceof HTMLElement && child.matches(ENTRY_SELECTOR + ', ' + SIBLING_ENTRIES))
  if (family.length > 0) {
    root.insertBefore(entryEl, family[family.length - 1].nextElementSibling)
    return
  }
  const nested = root.querySelector('button[class*="newSession"]')
  const row = nested === null || nested.parentElement === null
    ? null
    : (nested.parentElement === root ? nested : nested.closest('[class*="logoRow"]'))
  if (row !== null) {
    root.insertBefore(entryEl, row.nextElementSibling)
    return
  }
  root.append(entryEl)
}

/** Attach (or re-attach) the view as a trailing child of the centre column. */
function ensureView() {
  const column = conversationColumn()
  if (column === undefined) return
  if (viewEl === null || !viewEl.isConnected) {
    if (viewEl !== null) viewEl.remove()
    viewEl = el('div', { class: 'dsh-ot-view', 'data-dsh-ot-view': '' })
    panelEl = null
  }
  if (viewEl.parentElement !== column) column.append(viewEl)
  if (panelEl === null || !panelEl.isConnected) {
    viewEl.replaceChildren()
    buildPanel(viewEl)
    renderPanel()
  }
}

/** Open or close the panel, telling the sibling panels to stand down. */
function setOpen(open) {
  model.open = open
  if (open) {
    document.documentElement.setAttribute(OPEN_ATTR, '')
    document.dispatchEvent(new CustomEvent(ACTIVATE_EVENT, { detail: PANEL_NAME }))
    void bootData()
  } else {
    document.documentElement.removeAttribute(OPEN_ATTR)
    closeMenu()
  }
  emit()
}

/** First-open data load; subsequent opens only refresh. */
let dataBooted = false
async function bootData() {
  if (dataBooted) {
    await loadState()
    // Every terminal was hidden while the panel was closed, so xterm never measured
    // itself; a fit on reopen is what keeps the grid honest.
    for (const tab of model.tabs) if (tab.kind === 'terminal') focusTerminal(tab.id)
    return
  }
  dataBooted = true
  await loadState()
  const remembered = storeGet(STORE_KEYS.selectedServer, '')
  if (typeof remembered === 'string' && remembered.length > 0 && serverById(remembered) !== undefined) {
    model.selectedServerId = remembered
  }
  restoreWorkspace(model.workspace)
  model.booted = true
  emit()
  await pushSubscriptions()
}

// ===== src/client/boot.js =====
/**
 * Boot: mount the seats, wire the listeners, and export the plugin shape.
 *
 * Failure policy mirrors the sibling plugins: DOM and panel problems are logged, never
 * thrown — the web shell must not fail its boot because a panel seat is missing on
 * some DSH layout.
 */
const bootState = { running: false }

/** DSH web-shell entry: mount the sidebar entry + panel view, then listen. */
function apply(ctx) {
  if (typeof window === 'undefined' || typeof document === 'undefined') return
  if (bootState.running) return
  bootState.running = true
  const state = { disposed: false }
  let observer = null
  let timer = null
  let unbindModel = null
  let onActivate = null
  let onDocClick = null
  let onKeyDown = null
  let onResize = null
  let themeObserver = null

  function ensureMounted() {
    if (state.disposed) return
    try {
      if (entryEl === null || !entryEl.isConnected) entryEl = createEntry()
      placeEntry()
      if (conversationColumn() !== undefined) ensureView()
    } catch (error) {
      console.warn(LOG + ' seat mount failed:', messageOf(error))
    }
  }

  function dispose() {
    if (state.disposed) return
    state.disposed = true
    if (timer !== null) clearInterval(timer)
    if (observer !== null) {
      try {
        observer.disconnect()
      } catch { /* already gone */ }
    }
    if (themeObserver !== null) {
      try {
        themeObserver.disconnect()
      } catch { /* already gone */ }
    }
    if (onActivate !== null) document.removeEventListener(ACTIVATE_EVENT, onActivate)
    if (onDocClick !== null) document.removeEventListener('click', onDocClick, true)
    if (onKeyDown !== null) document.removeEventListener('keydown', onKeyDown)
    if (onResize !== null) window.removeEventListener('resize', onResize)
    if (unbindModel !== null) unbindModel()
    closeAllOverlays()
    closeMenu()
    stopSse()
    stopSocket()
    // Every widget goes; the HOST sessions stay, which is the point of keeping them
    // there — reopening the panel re-attaches to the same shells.
    for (const tabId of [...terminals.keys()]) disposeTerminal(tabId)
    for (const tabId of [...editors.keys()]) disposeEditor(tabId)
    document.documentElement.removeAttribute(OPEN_ATTR)
    model.open = false
    model.connected = false
    model.booted = false
    dataBooted = false
    if (entryEl !== null) {
      try {
        entryEl.remove()
      } catch { /* already gone */ }
      entryEl = null
    }
    if (viewEl !== null) {
      try {
        viewEl.remove()
      } catch { /* already gone */ }
      viewEl = null
    }
    panelEl = null
    sideEl = null
    serversEl = null
    sftpEl = null
    toolbarEl = null
    tabsEl = null
    panesEl = null
    drawerEl = null
    aiEl = null
    const style = document.getElementById(STYLE_ID)
    if (style !== null) style.remove()
    bootState.running = false
  }

  try {
    lang.current = normalizeLang(typeof navigator === 'undefined' ? null : navigator.language) ?? 'zh'
    injectStyles()
    unbindModel = onModel(() => {
      try {
        renderPanel()
      } catch (error) {
        console.error(LOG + ' render failed:', messageOf(error))
      }
    })
    ensureMounted()
    renderEntry()
    const streamHandlers = {
      onOutput: (sessionId, data, frame) => handleOutput(sessionId, data, frame),
      onSession: (record) => refreshTerminalStatus(record),
      onOverflow: (sessionId) => noteOverflow(sessionId),
      onHello: () => {
        if (model.open && !dataBooted) void bootData()
      },
    }
    startSse(streamHandlers)
    // The socket is the preferred path for terminal bytes; startSocket() is a no-op on
    // a build (or a browser) without one, and the SSE stream carries them instead.
    startSocket(streamHandlers)
    // The DSH shell re-renders its own tree; both a mutation observer and a slow
    // interval keep the seats attached across those repaints.
    observer = new MutationObserver(() => ensureMounted())
    observer.observe(document.body ?? document.documentElement, { childList: true, subtree: true })
    timer = setInterval(() => ensureMounted(), 3000)

    // The terminal theme follows the shell's light/dark state when it is left on
    // `default`, so a theme switch has to repaint the live terminals.
    themeObserver = new MutationObserver(() => {
      if (pref('themeName', 'default') === 'default') restyleTerminals()
    })
    themeObserver.observe(document.documentElement, { attributes: true, attributeFilter: ['class', 'data-theme'] })

    onActivate = (event) => {
      if (state.disposed) return
      if (event.detail !== undefined && event.detail !== PANEL_NAME && model.open) setOpen(false)
    }
    onDocClick = (event) => {
      if (state.disposed) return
      const target = event.target
      if (!(target instanceof Element)) return
      // A click anywhere outside an open menu closes it, but must not reach the menu's
      // own item handler.
      if (target.closest('.dsh-ot-menu') === null) closeMenu()
    }
    onKeyDown = (event) => {
      if (state.disposed) return
      if (event.key === 'Escape') {
        if (closeTopOverlay()) {
          event.preventDefault()
          event.stopPropagation()
        }
        return
      }
      if (!model.open) return
      // Ctrl/Cmd+Shift+F opens the AI bar; a bare Ctrl+key belongs to the shell
      // running inside the terminal, so the panel takes nothing else.
      if ((event.ctrlKey || event.metaKey) && event.shiftKey && event.key.toLowerCase() === 'f') {
        event.preventDefault()
        toggleAiBar(true)
      }
    }
    onResize = () => {
      for (const entry of terminals.values()) fitTerminal(entry)
    }
    document.addEventListener(ACTIVATE_EVENT, onActivate)
    document.addEventListener('click', onDocClick, true)
    document.addEventListener('keydown', onKeyDown)
    window.addEventListener('resize', onResize)

    if (ctx !== undefined && ctx !== null && typeof ctx.effect === 'function') {
      ctx.effect(() => dispose, PLUGIN_ID + ': client mount')
    } else {
      window.addEventListener('beforeunload', dispose, { once: true })
    }
  } catch (error) {
    console.error(LOG + ' client boot failed:', messageOf(error))
    dispose()
  }
}

/** Close the topmost overlay (menu first, then dialog). Returns whether it did. */
function closeTopOverlay() {
  if (openMenu !== null) {
    closeMenu()
    return true
  }
  if (overlays.length > 0) {
    overlays[overlays.length - 1].close(undefined)
    return true
  }
  if (model.drawerOpen) {
    model.drawerOpen = false
    emit()
    return true
  }
  return false
}

// ---------------------------------------------------------------- export
// The wrap step evaluates this file inside window.__ModuleLoader__.load with a
// CommonJS `module` in scope; a plain Node require() gets the same shape.
if (typeof module !== 'undefined' && module !== null && module.exports !== undefined) {
  module.exports = {
    name: PLUGIN_ID + '/client',
    inject: [],
    apply,
  }
}
    })();
    return module.exports;
  }
});
